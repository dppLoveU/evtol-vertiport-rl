# Progress Log

This file is appended to after every completed sub-task by Claude Code.
Format: `- YYYY-MM-DD [stageN] description (any caveats)`

- 2026-05-14 [stage1] bootstrapped python env: `.venv` from system Python 3.10.12 (apt installed `python3.10-venv` + `python3-pip`); pip-installed pandas 2.3.3, pyarrow 24.0.0, numpy 2.2.6, pytest 9.0.3, pyyaml 6.0.3 (caveat: 3.10 not 3.11 — see decisions log).
- 2026-05-14 [stage1] scaffolded package: `src/{constants,data,utils}`, `tests/`, `tests/fixtures/` with `__init__.py`; filled `pyproject.toml` with deps + pytest config.
- 2026-05-14 [stage1] task 1 (load_raw): implemented `src/data/clean.py::load_raw` with explicit `RAW_DTYPES` from `src/data/schema.py`; supports chunked read (default 500k) for the 4M-row file.
- 2026-05-14 [stage1] task 2 (coord fix): `src/data/clean.py::fix_coordinates` divides Int32 coords by `COORD_SCALE` in float64 then casts float32 (avoids precision loss on 9-digit ints); drops raw `fDep*` / `fDest*` columns.
- 2026-05-14 [stage1] task 3 (time parse): `src/data/clean.py::parse_times` uses `%Y/%m/%d %H:%M:%S` (deviation from plan — see decisions log), `errors="coerce"`, drops NaT rows and returns the dropped count for audit.
- 2026-05-14 [stage1] tests: `tests/fixtures/orders_sample.csv` (30 hand-crafted rows covering bbox / time / fare / age edge cases); `tests/test_clean.py` covers haversine, load_raw, fix_coordinates, parse_times, and an end-to-end pipeline check on the fixture.
- 2026-05-14 [meta] doc sync: updated CLAUDE.md §2/§3 (stage 1 in-progress), §5 (Python 3.10 reality, Hydra deferred to PyYAML for now, new "Test scope = current task" rule); fixed `docs/plan/stage1_data_cleaning.md` schema/pitfalls (datetime `%H:%M:%S`, fWaitTime float, encoding confirmed UTF-8); wrote setup section in `README.md`.
- 2026-05-14 [stage1] task 4 (derived cols): `src/data/clean.py::add_derived_columns` adds `duration_min` (from `dest_time - dep_time`), `drive_min` (from `fDriveTime / 60`), `geo_dist_km` (haversine on float32 coords); all three float32, drops `fDriveTime`.
- 2026-05-14 [stage1] task 5 (outlier filters): `apply_outlier_filters` runs (a)-(f) in plan order, returns `(df, audit)` with row counts after each step (`initial`, `after_bbox`, `after_duration`, `after_drive_km`, `after_geo_dist`, `after_geo_drive_ratio`, `after_fare`); thresholds default to plan values, overridable via kwargs.
- 2026-05-14 [stage1] task 6 (column selection): introduced `apply_simple_renames` as a separate step between derive and filter (filters need output names like `drive_km` / `fare_yuan`); `select_output_columns` now does dropna(OUTPUT_COLUMNS) → age clip[0,127] + cast int8 → string/float32 cast → reorder per `OUTPUT_COLUMNS`. Caveat: dropna step covers residual NAs the 6 filters don't touch (NA-age, NA-gender, etc.) so the plan's "isna().sum() == 0" acceptance criterion holds; not a plan deviation, just an implementation choice.
- 2026-05-14 [stage1] tests: added `test_derived_columns_compute_correctly`, `test_outlier_filter_each_step_drops_expected_rows` (asserts the exact audit dict on the fixture: 28→26→24→22→21→20→18), `test_column_selection_dtypes_and_order` (asserts column order, all dtypes, no NaN, NA-age dropped, age=999 clipped to 127, final 17 rows). 15/15 pass.
- 2026-05-15 [stage1] EDA + bbox refinement: `experiments/run_stage1_area_bounds.py` produces `results/stage1/eda/area_bounds.csv` (per-AreaName p1/p99 of all 4 coords). Confirmed data spans 5 AreaNames = full Suzhou metropolitan area; old `SUZHOU_BBOX` (City proper) dropped 47.56% of rows. Widened to `lon[120.37, 121.33], lat[30.88, 32.01]` (union p1/p99 + 2 km padding). Pressure test: 4,257,529 → 4,050,523 rows (4.86% drop, bbox alone now 0.74%). Acceptance HI revised 4.05M → 4.15M to match reality (decisions log + plan acceptance criterion updated). 16/16 tests still pass.
- 2026-05-15 [stage1] task 7 (CLI + parquet): `experiments/run_stage1_clean.py` reads `configs/preprocess.yaml`, runs the full task-1..6 pipeline, prints per-step audit, writes `data/processed/orders_clean.parquet` with snappy + row_group=100k. Verified end-to-end on full 4.26M-row CSV: 20s wall time, 4,050,523 rows, 317.5 MB on disk, 41 row groups, 0 NA in all 16 columns, acceptance PASS.
- 2026-05-15 [stage1] task 8 (EDA): `experiments/run_stage1_eda.py` produces 5 hand-picked plots (PNG@300dpi + vector PDF) — duration before/after, drive_km hist, fare_yuan log-y hist, geo-vs-drive scatter (50k sample), hourly volume — plus `results/stage1/eda_summary.html` via ydata-profiling on a 200k sample (full 4M is too slow). End-to-end: ~33s. Added `matplotlib>=3.7` and `ydata-profiling>=4.6` to `pyproject.toml`.
- 2026-05-15 [stage1] **STAGE 1 COMPLETE (8/8)**. All plan acceptance criteria met: parquet exists at `data/processed/orders_clean.parquet` (4,050,523 rows in [3.62M, 4.15M] target window, 0 NA, all 4 coords inside metro `SUZHOU_BBOX`, dtypes match plan schema exactly); EDA HTML + 5 plots exist under `results/stage1/`; `pytest tests/test_clean.py -v` reports 16/16 green. Three plan deviations recorded in `docs/decisions.md` (datetime format `%H:%M:%S` not `%H:%M`; Python 3.10 not 3.11; `wait_min` filter (g) added; `select_output_columns` raises on NA; `SUZHOU_BBOX` widened to metropolitan area; acceptance HI revised 4.05M → 4.15M). Next: stage 2 — spatial discretization (H3 level 7, ~250 hexagons, candidate vertiport set).
- 2026-05-18 [stage2] task 1 (build_zones): `src/data/zones.py::build_zones` aggregates the union of O/D endpoints into H3 res-7 cells (endpoints deduped at 5 dp before indexing), keeps cells with >= `min_orders_per_zone` endpoints, assigns `zone_id` by lexicographic H3-index sort; writes `data/processed/zones.geojson` + `n_orders` column for the task-6 density map. `min_orders_per_zone` raised 50 -> 2000 on real data -> |Z|=530 (see docs/decisions.md 2026-05-18).
- 2026-05-18 [stage2] task 2 (pull_poi): `src/data/candidates.py::pull_poi` fetches OSM POIs via osmnx v2, classifies by source, size-filters hospital/industrial polygons, dedups to one POI per H3 res-8 cell by source priority (airport>subway>mall>hospital>industrial). Fixed plan tag bug (`subway=yes` -> `station=subway`) and raised industrial floor 50000 -> 200000 m^2 -> 1383 POIs (caveat: re-pull needs the cache deleted; see docs/decisions.md 2026-05-18).
- 2026-05-18 [stage2] task 3 (add_grid_seeds): uniform ~3 km grid inside `SUZHOU_BBOX`, dropping points outside demand zones or within 1 km of a POI -> 99 grid candidates.
- 2026-05-18 [stage2] task 4 (finalize_candidates): merges POI + grid, assigns `zone_id` by spatial join (drops 669 candidates outside demand zones), assigns `cand_id` by lexicographic (lon, lat) sort; writes `data/processed/candidates.geojson`. Final |C|=813 (1482 pre-finalize) — passes revised [600,1500] acceptance. `pytest tests/test_candidates.py` 6/6 green; `tests/test_zones.py` 4/4 green.
- 2026-05-18 [stage2] task 5 (build_matrices): `src/data/spatial.py::build_matrices` builds `dist_zone_zone` [530,530], `dist_zone_cand` [530,813], `cand_covers_zones` [813,530] via `haversine_km` over zone/candidate coordinates only (no order-level loop). Zone-zone matrix symmetrized exactly (`0.5*(D+D.T)`) with a forced-zero diagonal; coverage mask derived from the stored float32 `dist_zone_cand` so it matches a from-disk recompute. Wired into `run_stage2_build.py`: writes the three `.npy` files + `data/processed/spatial_meta.json` (n_zones, n_candidates, walk_radius_km=5.0, h3_resolution=7, source_counts, coverage_ratio, build_timestamp). `coverage_ratio=0.9962` (>= 0.85 acceptance). `tests/test_spatial.py` 6/6 green; full stage-2 suite 16/16. No plan deviations.
- 2026-05-18 [stage2] task 6 (folium maps): `experiments/run_stage2_maps.py` renders 3 interactive maps to `results/stage2/maps/` — `zones_map.html` (530 H3 zones shaded by `n_orders`, quantile-binned YlOrRd since demand is skewed), `candidates_map.html` (813 candidates as source-colored CircleMarkers + HTML legend), `coverage_map.html` (highest-coverage candidate `cand_id=254` covering 23 zones within the 5 km walk radius, covered zones highlighted, walk-radius disk drawn). Map center auto-computed from mean zone centroid; walk radius read from `spatial_meta.json`. Added a `maps.output_dir` key to `configs/spatial.yaml` (config wiring, not a plan deviation).
- 2026-05-18 [stage2] **STAGE 2 COMPLETE (6/6)**. Final results: |Z|=530 demand zones (H3 res-7), |C|=813 candidate vertiport sites, `coverage_ratio=0.9962` (>= 0.85 acceptance). Folium maps generated under `results/stage2/maps/` (zones / candidates / coverage). Tests `tests/test_zones.py` + `tests/test_candidates.py` + `tests/test_spatial.py` = 16/16 pass. Fixed `.gitignore`: `/results/` was missing despite CLAUDE.md §4 — added it and ran `git rm -r --cached results/` to untrack the 13 stage-1 files (local files kept). Next: stage 3 — OD tensor construction & eVTOL trip filtering.
- 2026-05-18 [stage3] R1 (tasks 1-2): `configs/od.yaml` (full Stage-3 source-of-truth: time bin, eVTOL thresholds, weight column, output/eda/sensitivity paths); `src/data/od.py::assign_time_bin` (`slot = floor((dep_time - t0)/bin)`, out-of-range slots are split into a separate `out_of_range` frame (plan Common Pitfalls' "clamp or drop" resolved as drop); later R1.5 revised the final OD window to T=528); `src/data/od.py::assign_zones` + `build_zone_lookup` (O/D H3 res-7 indexing, reuses zones.py's 5-dp endpoint rounding so cells match Stage 2 exactly; unknown-zone orders split into `dropped`, both functions return `(kept, dropped, stats)`). `tests/test_od.py` 5/5 green (synthetic data only, no full-parquet read). Caveat: plan Inputs calls `dist_zone_zone.npy` an input "for filter (b)", but task 4 filter (b) is the order-level `duration_min >= 25` — `dist_zone_zone` does not participate in the eVTOL filter and is unused in R1 (confirmed with user; recorded here, not a code/design change). Not done in R1: OD tensor build, `od_full.npy`, EDA.
- 2026-05-18 [stage3] R1.5 full smoke (`experiments/run_stage3_smoke.py`): **OD time window revised to 11 full calendar days** `[2023-07-10 00:00:00, 2023-07-21 00:00:00)`, **T=528** (planned 336 was wrong — data spans 12.45 days, not 7). Synced `NUM_TIME_BINS` 336→528 in `src/constants.py`, `configs/od.yaml::time` (now has `start_datetime`/`end_datetime`/`time_bin_min`/`num_time_bins`), and CLAUDE.md §7. Part A (full `dep_time`, 4,050,523 rows): only 22 orders out of window (0.0005% — the two residual-day tails) → PASS. Part B (full, all orders): zone-assignment **drop_rate = 10.19%** (412,856 dropped of 4,050,501 in-window; root cause = Stage 2's 530-zone discretization leaving 891 low-density H3 cells unmapped). Plan acceptance ≤2% → **≤12%** (decision: keep |Z|=530, do not retune Stage 2 — see docs/decisions.md 2026-05-18 ×2 and updated `docs/plan/stage3_od_construction.md`). Only 14.24% of dropped orders meet the eVTOL base condition (≥15 km & ≥25 min), so eVTOL-demand impact is limited. `pytest tests/test_od.py` 5/5 green. R1 + R1.5 complete; clear to enter R2 (tasks 3-4: build od_full + eVTOL filter).
- 2026-05-18 [stage3] R2 (tasks 3-4 core): `src/data/od.py` gains `is_evtol_eligible` (mask: `geo_dist_km>=min` & `duration_min>=min` & `o_zone!=d_zone`) and `build_od_tensor` (groupby `(slot,o_zone,d_zone)` → dense `[T,|Z|,|Z|]`; `value_col=None` count→int32, `value_col` set sum→float32; range-checks slot/zone ids, raises on out-of-range / NaN / negative; returns shape/dtype/sum/nonzero/memory stats). `experiments/run_stage3_build.py`: SMOKE (`--nrows>0`) builds tensors in memory + prints stats, writes NOTHING (a partial-data tensor must not land at `data/processed/od_*.npy`); FULL (`--nrows 0`) writes the 3 `.npy` + `od_meta.json` after acceptance checks pass. `tests/test_od.py` 11/11 green (7 new: eVTOL filter dist/duration/intra-zone, count + weighted aggregation, zero-diagonal end-to-end, out-of-range raise). 200k-row smoke: drop_rate 10.16%, evtol_trip_count 9582, **evtol_share 5.33%** (in [0.03,0.20] → PASS), tensors `[528,530,530]` 593 MB each (~1.78 GB total), od_evtol nonzero_ratio 0.0061%, diagonal 0, no NaN/negative — all acceptance checks PASS, 1.2s. Caveat: evtol_share 5.33% sits at the low end of the plan's expected 5–15%; the 200k rows are a non-random head sample (R1.5 found this slice is hour-skewed), so a full run is needed to confirm the real share. Not done: full build, EDA, sensitivity. No design deviation.
- 2026-05-18 [stage3] R2 full build (`python -m experiments.run_stage3_build`, FULL mode): built and wrote the three OD tensors + meta to `data/processed/`. n_orders_total 4,050,523 → n_time_in_range 4,050,501 → n_zone_assigned 3,637,645 (zone drop_rate 10.19%). evtol_trip_count 188,699, **evtol_share 5.19%** (in [0.03,0.20] → PASS). od_full `[528,530,530]` int32 sum 3,637,645 nonzero 1,837,574 (1.24%); od_evtol int32 sum 188,699 nonzero 173,264 (0.117%); od_evtol_weighted float32 sum 14,915,827 (fare_yuan) nonzero 173,264 (0.117%). Each `.npy` = 593.26 MB on disk (~1.78 GB total, all gitignored under `data/processed/`); `od_meta.json` written (T=528, n_zones=530, time_bin_min=30, window 2023-07-10..2023-07-21, evtol params 15 km / 25 min, weight=fare_yuan, share_of_evtol_trips=0.0519). od_evtol diagonal = 0, no NaN, no negative — all acceptance checks PASS, 11.3 s wall. `pytest tests/test_od.py` 11/11 green. **R2 (Stage-3 tasks 3-6) complete.** Next: R3 — share-by-hour stats (task 7) + EDA figures (task 8) and/or the sensitivity sweep (task 9).
- 2026-05-18 [stage3] R3 (task 9): `experiments/run_stage3_sensitivity.py` sweeps `EVTOL_MIN_DIST_KM ∈ {10,12,15,18,20}` km (duration fixed 25 min, 11-day window, 530 zones; reuses `assign_time_bin`/`assign_zones`/`is_evtol_eligible`, rebuilds no `.npy`). `eligible_share_of_zone_assigned`: 10 km **10.04%**, 12 km **7.98%**, 15 km **5.19%**, 18 km **3.36%**, 20 km **2.47%**. A higher threshold yields fewer eligible trips and fewer nonzero OD pairs (66,045 → 34,116) but higher `normalized_od_entropy` (0.902 → 0.930) — short-haul concentration is filtered out, leaving a more spread long-haul OD distribution. 15 km consistency check: sweep `eligible/zone_assigned` = 0.051874 == `od_meta.json` `share_of_evtol_trips` 0.051874 (abs diff 0.000000) → PASS, identical scope to the R2 full build. **15 km kept as the provisional baseline** (NOT proven optimal) — its 5.19% share is inside the [0.03,0.20] acceptance window and consistent with the already-built `od_evtol.npy`/`od_meta.json`. **12 km (7.98%) is the designated fallback**: if Stage-4/5/6 underperforms due to eVTOL OD sparsity, regenerate the tensors at 12 km. The `15 km / 25 min` cut is a threshold-based proxy treated as a sensitivity-tested assumption, not a settled standard — see docs/decisions.md 2026-05-18. Table written to `results/stage3/sensitivity.csv` (5 rows; `results/` is gitignored). No design deviation. **R3 (task 9) complete.** Remaining Stage-3 work: share-by-hour stats (task 7) + EDA figures (task 8).
- 2026-05-19 [stage4] 4A (dataset/normalization/padding smoke): updated `docs/plan/stage4_diffusion.md` to the Stage-3 reality (T 336→528, |Z| ≈250→530, `od_samples.npy` `[64,528,530,530]`, padding to a multiple of 16 not next-pow2, `base_channels` smoke 16/32 not 64, staged 4A/4B execution, new `od_samples_agg.npy` `[N_ω,530,530]` int32 output so Stage 5 never loads the ~19 GB 4-D array). New `configs/diffusion.yaml` (input/data/model/diffusion/train/sample sections; data section is the 4A source of truth, the rest are smoke defaults). New `src/data/od_dataset.py` — `ODDataset` (mmap load of `od_evtol.npy`, log1p+global-scalar-standardize+clip+scale norm, zero-pad |Z|→`pad_size`, `(hour,day_of_week,is_weekend)` condition, `inverse_transform`, JSON norm-stats save/load); pure NumPy, no torch (works as a torch Dataset by duck typing in 4B). `tests/test_od_dataset.py` 19/19 green (pad/unpad + inverse round-trips, split lengths, condition correctness, no-NaN/in-range). Smoke (`experiments/run_stage4_dataset_smoke.py` on the real tensor): T=528, |Z|=530, **pad_size=544** (530→544, padded sample `[1,544,544]` 1.18 MB), splits train/val/test = 432/48/48 samples, nonzero_ratio 0.1168%. **FINDING** (see docs/decisions.md 2026-05-19): the plan's `log1p`+standardize+clip[-3,3] norm degenerates on the 0.117%-sparse eVTOL tensor — train stats `mu=0.00104`, `sigma=0.02787`, so every nonzero entry saturates to +1.0 and the normalized tensor is effectively binary (inverse-transform `max|err|≈3.9`). Stage 4A plumbing is correct; the normalization scheme needs a Stage-4B decision (raise/drop clip, nonzero-only standardize, or switch to the 12 km denser fallback) — flagged for the user, not auto-changed. Three plan-assumption changes recorded in docs/decisions.md (pad-to-16, JSON norm stats, 9/1/1-day split).
- 2026-05-18 [stage3] R4 (tasks 7-8): `src/data/eda_plots.py` (house figure style + PNG/PDF save + zone-choropleth helper; pure matplotlib, no seaborn package — `seaborn-v0_8-whitegrid` style ships with matplotlib) and `experiments/run_stage3_eda.py`. Task 7: share_total = 5.19% (== `od_meta.json` share_of_evtol_trips); `results/stage3/eda/share_by_hour.csv` written (24 rows: full/evtol counts + all/weekday/weekend share); `od_meta.json` gains `share_by_hour` / `share_by_hour_weekday` / `share_by_hour_weekend` keys (existing fields preserved, still valid JSON). Task 8: 7 figures × (PNG 300 dpi + PDF) = 14 files in `results/stage3/eda/` — od_share_by_hour, top20_od_pairs, heatmap_o_volume, heatmap_d_volume, temporal_pattern, distance_distribution, fare_distribution. Key observations: eVTOL share_by_hour spans ~3.4%–9.9%, peaks at 05:00 (weekday 9.9% — pre-dawn long-haul / airport trips), bottoms at 00:00–03:00 (~3.4%), steady ~6% across 12:00–16:00; weekday share runs slightly above weekend. The `15 km / 25 min` eVTOL cut stays a **provisional baseline** — figure titles label it so, distance_distribution marks the 15 km cut-off — see docs/decisions.md 2026-05-18. `results/` is gitignored. No design deviation. **R4 (Stage-3 tasks 7-8) complete — Stage 3 tasks 1-9 all done.**
- 2026-05-20 [stage4] 4B-0 (normalization ablation): `experiments/run_stage4_norm_ablation.py` compares 5 normalization schemes on the train split (slots 0..431, 9 days) without modifying `ODDataset`. Schemes — **A** `current_global_clip3` (log1p + global mu/sigma + clip=3 + /clip → [-1,1]); **B** `global_clip30`; **C** `global_clip100`; **D** `nonzero_global_clip3` (mu/sigma over nonzero log1p only, clip=3, zeros pinned to -1.0); **E** `log1p_minmax_p999_nonzero` (linear scale via p99.9 of nonzero log1p as vmax). Stats: global `mu=0.001038`, `sigma=0.027869`; nonzero `mu_nz=0.7272`, `sigma_nz=0.1260` (173,264 nonzero train cells); all-cells log1p p99.9 ≈ 0.693 (degenerate, ~0); nonzero log1p p99.9 = 1.6094. **Result**: only **C** distinguishes all five probe counts {1, 2, 5, 10, 20} — A saturates everything to +1, B saturates from count=2, D saturates from count=5, E saturates from count=5 (p99.9 of nonzero log1p ≈ log1p(4) so the linear scale clips at count ≈ 4). C maps {1, 2, 5, 10, 20} → {0.248, 0.394, 0.643, 0.860, 1.000}, min adjacent gap 0.140; 0% real-train saturation (pct_sat=0.00%); mean inverse-transform error ≈ 0 on train. Caveat: scheme C uses only the upper half of [-1, 1] (zeros at -0.0004, nonzeros at 0.25..1.0) because global mu is tiny on this sparse data — the zero/nonzero gap is ~0.25 rather than ~2. **Proposed for Stage 4B model smoke**: `clip_val=100` (scheme C) as the primary baseline (see `docs/decisions.md` 2026-05-20). Table written to `results/stage4/norm_ablation.csv` (5 rows, gitignored). `ODDataset` defaults unchanged; the 4A `clip_val=3.0` config value will need to be updated to 100 if/when the user confirms. No new deps.
- 2026-05-20 [stage4] 4B-0 (clip100 adoption): `configs/diffusion.yaml::data.clip_val` flipped `3.0 → 100.0` per the 4B-0 ablation (decisions.md 2026-05-20 promoted from "proposed" to "accepted for Stage 4B model smoke"). Stale `data/processed/od_norm_stats.json` (computed at clip=3) deleted; re-cached at clip=100 by the next smoke run. **Re-runs**: (a) `experiments/run_stage4_dataset_smoke.py` → norm stats `mu=0.001038, sigma=0.027869, clip_val=100.0`, train[0] normalized range `[-0.0004, 0.3938]` (was saturated at +1 under clip=3), **inverse round-trip on train sample 10 `max|err| = 0.0000`** (was ~3.9). Splits 432/48/48 unchanged. (b) `pytest tests/test_od_dataset.py -v` → **19/19 PASS** (no test depends on the config; `ODDataset.__init__` keeps `clip_val=3.0` as its in-code default, used by tests via the synthetic fixture). (c) `experiments/run_stage4_norm_ablation.py` re-run → scheme C still `primary`, identical numbers as before (deterministic given the same train tensor). `ODDataset` defaults unchanged. No new deps.
- 2026-05-20 [stage4] 4B-1 prep: PyTorch CUDA environment installed and verified ahead of writing any 4B-1 model code. WSL2 sees the RTX 5070 Ti via the Windows-side driver 591.86 (CUDA 13.1 host); installed `torch-2.11.0+cu128` from the cu128 wheel index (`pip install torch --index-url https://download.pytorch.org/whl/cu128`) — needed because RTX 50-series is Blackwell sm_120, which only the cu128 toolchain ships kernels for. Verified `torch.cuda.is_available()=True`, `get_device_capability(0)=(12,0)`, 1024×1024 GPU matmul + sync OK. Added `torch>=2.11` to `pyproject.toml` (no `torchvision` / `torchaudio` — Stage 4 does not need them). Pulled triton 3.6.0 and the nvidia-cu128 runtime wheels (~1.7 GB total disk). See `docs/decisions.md` 2026-05-20 (cu128 wheel rationale). No 4B-1 model code written yet — environment-only step.
- 2026-05-20 [stage4] 4B-1 (small U-Net + DDPM forward/sample smoke): added `src/models/__init__.py`, `src/models/unet_od.py` (sinusoidal time emb + `ConditionEmbedding` for hour/dow/is_weekend + `ResBlock` (GroupNorm + 2× Conv3×3 + FiLM-style emb bias) + `UNetOD` (input_conv → 2 down-blocks → bottom → 2 up-blocks with skip-concat → out_conv)), `src/models/diffusion.py` (`GaussianDiffusion` Module: cosine/linear betas, `add_noise`, `training_loss` epsilon-MSE, `ddim_sample` deterministic DDIM with `clip_sample` on by default — see `docs/decisions.md` 2026-05-20). Added `einops>=0.8` to `pyproject.toml` (small; the U-Net does not currently import it but it's the standard companion lib for the diffusion code path and is reserved for the production U-Net). **Tests** `tests/test_diffusion.py` 15/15 green on tiny [2,1,32,32] fake tensors: sinusoidal+condition embed shapes, ResBlock residual, U-Net forward/backward, cosine/linear schedule shapes + monotonicity, `add_noise` at t=0 is identity-like, training loss finite + backward, DDIM sample shape no-NaN, bad inference-step rejection, `.to(device)` moves buffers. **Smoke** `experiments/run_stage4_model_smoke.py` against the real `od_evtol.npy` train[0] on GPU (RTX 5070 Ti, sm_120): model 304,137 params (1.22 MB float32); DDPM `num_train_timesteps=100`, cosine betas, `alpha_cumprod_T=2.4e-7`; 3 AdamW steps lr=2e-4 → loss 1.118 → 0.952 → 0.870, grad norms 3.57/2.96/2.94 (step 0 wall=734 ms cold, step 1-2 wall=16 ms each); DDIM 5-step sample wall=67 ms, range `[-1.029, 1.024]` (clip_sample working — uncapped values blew to ±10k); inverse transform `unpad → expm1` → shape `[1, 530, 530]` float64, nonneg + finite, min=0, max=16.35 counts, mean=3.81 counts. **GPU peak** 638.6 MB allocated / 843.1 MB reserved — comfortable under the 16 GB budget. NO checkpoint saved, NO `models/` write, NO `data/synthetic/` write, NO `od_samples.npy`. Stage 4C (real training) still gated on user go-ahead.
- 2026-05-20 [stage4] 4B-2 (training-loop smoke): added `src/models/ema.py` (`EMA(nn.Module)` with `update` / `copy_to` / `store` / `restore`; shadows live as `register_buffer`s so `.to(device)` moves them and `state_dict` rides along with checkpoints — parameter names with `.` are mapped to `_` for buffer naming, rebuilt at construction from the model), `tests/test_ema.py` (10/10 pass: buffer count = trainable params, init buffers equal model params, decay correctness on the `0.9*init + 0.1*0` math, copy_to / store / restore, state_dict round-trip, `.to()` moves buffers, invalid-decay + restore-before-store rejections), and `tests/test_diffusion.py::test_checkpoint_save_load_roundtrip` (full `{model, ema, optim, step, val_loss}` save+reload via `weights_only=True`, forward outputs bit-equal after reload). New `experiments/run_stage4_train_smoke.py` exercises the full training scaffold on the real `od_evtol.npy`: DataLoader (wrapped `_TorchODDataset`, `batch_size=1`, `num_workers=0`) → `torch.amp.autocast(device_type="cuda", dtype=bfloat16)` → DDPM training loss → backward → `clip_grad_norm_(1.0)` → AdamW step → `EMA.update` → every `eval_every=10` swap EMA weights in via `store`/`copy_to`/`restore` and eval over the 48-sample val split with a fixed-seed `t` generator → every `save_every=10` write `last.pt` + (if val improved) `best.pt`. Outputs land under `models/diffusion_od_smoke/`: `last.pt`, `best.pt`, `config.yaml`, `train_log.jsonl` (`models/` is gitignored — nothing reaches commits). bf16 chosen over fp16+GradScaler because Blackwell sm_120 supports bf16 natively and skipping the scaler keeps the loop simpler. **Smoke run** on RTX 5070 Ti, `max_steps=20`, `num_train_timesteps=100`, `ema_decay=0.99`, seed=42: model 304,137 params; step-1 wall 1307 ms cold (CUDA + DataLoader spin-up), steps 2-20 wall 14-18 ms each; train loss `1.043 → 0.307` (descending cleanly), grad norms `3.31 → 1.48` (no spikes, no clipping required), val_loss_ema `0.990 (step 10) → 0.914 (step 20)`, all finite. Checkpoint files: `best.pt` 4.965 MB, `last.pt` 4.965 MB, `config.yaml` 0.001 MB, `train_log.jsonl` 0.003 MB. **Best.pt reload check**: load fresh `UNetOD` + `EMA`, `load_state_dict` for both, `ema.copy_to(model)`, re-evaluate on val with the same fixed-seed `t` → saved 0.914288, reloaded 0.914612, `|diff| = 3.24e-4` (within the 1e-3 tolerance — bf16 autocast is not bit-deterministic across runs but the gap is well under one part in 2000). GPU peak: **532.3 MB allocated / 755.0 MB reserved** on a 16 GB card. NO real training, NO `od_samples.npy`, NO sampling, NO Stage-5 entry. **Tests**: `pytest tests/test_diffusion.py tests/test_ema.py -v` → 26/26 PASS. Plan deviation: none structural; the `bf16 + no GradScaler` choice is a routine AMP-dtype call and is mentioned here but not promoted to `docs/decisions.md`.
- 2026-05-20 [stage4] 4B-3 PR-1 (training entry point + CFG + metrics, NO real training): four confirmed decisions (1) bf16 / no GradScaler, (2) TensorBoard logging, (3) pilot uses `base=32` `mults=(1,2,4)` `bs=4`, (4) full run 12-hr budget at 20k steps; see plan emitted in chat. Added classifier-free guidance backward-compatibly: `src/models/unet_od.py::UNetOD.forward` now accepts an optional `cond_drop_mask: torch.Tensor | None` (per-sample bool; `True` zeros the conditional embedding); `src/models/diffusion.py::training_loss` accepts `cond_dropout_prob: float = 0.0` (default unchanged → smoke bit-equal), and `ddim_sample` accepts `guidance_scale: float = 1.0` with an explicit `scale == 1.0` branch that takes the original single-forward path verbatim and a `scale > 1.0` branch that does cond + uncond forwards and combines as `eps_uncond + scale * (eps_cond - eps_uncond)`. New `src/utils/metrics_dist.py` (`nonzero_ratio`, `row_sums`, `col_sums`, `ks_stat_1d`, `marginal_compare`) — pure-NumPy 1-D K-S statistic and marginal stat dictionary, no scipy. New `tests/test_metrics_dist.py` (13 tests: KS identical → 0, disjoint → 1, separated → high, close → low, empty rejected; `marginal_compare` real-vs-self → 0, all-zero gen distinguishable, returns flat floats, low-dim rejected, unequal-N allowed). Extended `tests/test_diffusion.py` with 5 CFG tests (cond_drop_mask changes output, per-sample mask is per-sample, full cond_dropout=1.0 still finite + backward, guided sample shape + finite, **`guidance_scale=1.0` bit-equal to no-arg via `torch.equal`**). New `experiments/run_stage4_train.py`: profile table (`pilot`/`medium`/`full` with the four agreed configs), `--profile` required, `--resume <ckpt.pt>` optional, TensorBoard writer at `results/stage4/train_<profile>/tb/`, linear LR warmup, per-step / per-eval / per-save event logging to `train_log.jsonl`, in-loop sample diagnostics via `_sample_diag` (EMA-swap → chunked DDIM with `guidance_scale=2.0` → `inverse_transform` → `marginal_compare` vs `_collect_real_val_counts(val_base)`), end-of-run plots (`loss_curve.png`, `sample_grid.png`, `marginal_match.png`) and `metrics.json` under `results/stage4/train_<profile>/`, checkpoints under `models/diffusion_od_<profile>/` (gitignored). Pilot profile diag fires once at the final step (`diag_at_end_only=True`); medium / full diag every save. `configs/diffusion.yaml`: comment-only block at top describing the three profiles and stating that the YAML values remain SMOKE defaults (`run_stage4_train_smoke.py` / `run_stage4_model_smoke.py` still rely on them). `pytest tests/test_diffusion.py tests/test_ema.py tests/test_metrics_dist.py -v` → **44/44 PASS** (16 + 10 diffusion+ema → 21+10+13). 4B-2 smoke regression `python -m experiments.run_stage4_train_smoke` → reproduces previous numbers exactly: train_loss range `[0.3074, 1.0429]`, val_loss 0.9143, reload diff 3.24e-4, GPU peak 532.3 MB — CFG path is bit-equivalent when `cond_dropout_prob=0` and `guidance_scale=1`. **NO real training launched**, no `od_samples.npy`, no Stage-4C entry. CFG implementation is a textbook Ho-Salimans-2022 plumbing addition with no plan deviation; `docs/decisions.md` unchanged. Ready for Stage 4B-3 PR-2 (pilot run) on user go-ahead.
- 2026-05-20 [stage4] 4B-3 PR-2 attempt 1 (pilot under OLD 9/1/1 split — **invalidated**): pilot run launched `python -m experiments.run_stage4_train --profile pilot`, 1000 steps, 98s wall, train_loss `1.34→0.002`, val_loss_ema `0.77→0.024`, best.pt saved at step 1000. End-of-run `_sample_diag` reported `real_nonzero_ratio=1.0`, `gen_nonzero_ratio=23.14%`, `row_sum_ks_stat=1.0`, `col_sum_ks_stat=1.0`. Both numbers looked wrong; investigation followed.
- 2026-05-20 [stage4] 4B-3 PR-2 diagnostics: built `experiments/run_stage4_diag_posthoc.py` (load `models/diffusion_od_pilot/best.pt`, EMA weights, DDIM N=16 sample with `guidance_scale=2.0` / 50 inference steps, inverse_transform, dual nonzero ratio + quantile + per-sample stats). Result: continuous-float `nonzero_ratio=25.32%` (matches in-loop figure), rounded-int-count `nonzero_ratio=0.0754%`, value_counts `{0:4 491 009, 1:3 383, 2:8}`, gen_max=2. Root-cause analysis identified **two** independent issues: (i) `_collect_real_val_counts` round-trips through float32, so `inverse_transform` of an integer 0 returns ~1e-9 and `np.count_nonzero` reads 100% of cells as nonzero, masking the real sparsity; (ii) the in-loop ratio compares **continuous** gen floats to the noisy "real" reference, not rounded integer counts. **AND** per-day raw OD inspection (`data/processed/od_evtol.npy`) showed days 0–6 carry essentially all signal (22–27k nonzeros/day), day 7 has only 40 nonzeros, days 8–10 are **identically zero** — the old `train_days [0,9]/val_days [9,10]/test_days [10,11]` split had train day 8 = 0 contribution, val + test = 0 ground truth, so val_loss decay was meaningless (model just denoising back to the zero slice). Documented in `docs/decisions.md` 2026-05-20 ("Stage 4 split revision" + "Stage 4B-3 metric口径"). **Pilot run is invalidated as acceptance evidence.**
- 2026-05-20 [stage4] 4B-3 PR-2 fixes (user-approved, no retrain yet): (a) `configs/diffusion.yaml::data.split` now `train_days [0,5] / val_days [5,6] / test_days [6,7]` — Mon–Fri train, Sat val, Sun test; day 7 dropped (40-nonzero tail), days 8–10 excluded. Deleted stale `data/processed/od_norm_stats.json`. `python -m experiments.run_stage4_dataset_smoke` re-run on new split: lengths train=240 / val=48 / test=48, raw nonzero ratio train **0.1897%** (127 885), val **0.1729%** (23 313), test **0.1634%** (22 026), train sum 139 116 / val 25 365 / test 24 177, train inverse round-trip `max|err|=0.0000`, all splits non-empty. New norm stats `mu=0.001378, sigma=0.032088, clip_val=100.0` (was `mu=0.001038, sigma=0.027869` under the 9-day train). Caveat: val/test are both weekend now (was both weekday); cross-pattern generalization is not measured under the new split — recorded in decisions.md, not gated. (b) `experiments/run_stage4_train.py::_collect_real_val_counts` rewritten to read raw int64 OD slices directly from the memmap (no normalization round-trip). (c) `_sample_diag` now rounds gen counts to int64 before calling `marginal_compare`; acceptance keys (`gen_nonzero_ratio`, row/col mean/std, `row_sum_ks_stat`, `col_sum_ks_stat`, `gen_min/max/mean`) are integer-domain; continuous-side stats moved under `gen_cont_nonzero_ratio` / `gen_cont_min/max/mean`. `_plot_marginal_match` consumes rounded counts; `_plot_sample_grid` keeps continuous (log1p heatmap smoother that way). (d) Renamed `results/stage4/train_pilot/` → `results/stage4/train_pilot_invalid_old_split_debug/` so the directory cannot be mistaken for a Stage 4B-3 acceptance pilot; the same is true for `models/diffusion_od_pilot/` → `models/diffusion_od_pilot_invalid_old_split_debug/`. No commit. `pytest tests/test_metrics_dist.py tests/test_diffusion.py tests/test_ema.py` still green (the口径 fix is on the caller side; `marginal_compare` itself is unchanged). NO retrain launched; a fresh pilot on the new split is gated on user sign-off.
- 2026-05-20 [stage4] 4B-3 PR2b (fixed-split pilot run): `python -m experiments.run_stage4_train --profile pilot` on the PR2a split `train_days [0,5] / val_days [5,6] / test_days [6,7]`, 1000 steps, 105.3 s wall, RTX 5070 Ti / bf16 / seed 42, 920k params. Train loss `1.3434 → 0.0028` (smooth decrease then noisy plateau ~0.002–0.010, no NaN). val_loss_ema strictly decreasing every eval: `0.7775 → 0.3453 → 0.1367 → 0.0556 → 0.0280` (best == final == 0.0280). End-of-run `_sample_diag` (4 EMA-DDIM samples, `guidance_scale=2.0`, 50 inference steps) under the PR2a口径 fix: **`gen_nonzero_ratio = 0.0535%`** (rounded-int basis) vs real val `0.1729%` (≈3.2× sparser than ground truth); `gen_cont_nonzero_ratio = 12.17%`; gen rounded `min=0, max=3, mean=5.40e-4`; `row_sum_ks_stat = 0.211`, `col_sum_ks_stat = 0.345` (col further off — gen_col_sum_std 5.13 vs real 2.74, heavy single-column tail). GPU peak allocated **5428.8 MB** / reserved 5809.1 MB. Outputs landed at `results/stage4/train_pilot/` (`loss_curve.png`, `marginal_match.png`, `metrics.json`, `sample_grid.png`, `tb/`, `train_log.jsonl`) and `models/diffusion_od_pilot/` (`best.pt` = `last.pt` = 14.819 MB, both saved at step 1000; `config.yaml`, `profile.yaml`, `train_log.jsonl`; gitignored). No `od_samples.npy`, no medium/full launched. **Conclusion**: pipeline and PR2a fixed diagnostics (split + rounded-count口径 + raw-int real reference) pass end-to-end on the new split and produce non-negative integer counts with no NaN; generated OD is still sparser than real val at this step budget — consistent with 1000-step under-fitting / mode-bias toward zero on a 0.17%-sparse target (`docs/plan/stage4_diffusion.md` §Common Pitfalls). This run is a **pilot baseline**, not Stage 4 final model acceptance.
- 2026-05-20 [stage4] 4B-3 PR3a (medium architecture canary + CLI overrides): added three debug-only CLI flags to `experiments/run_stage4_train.py` — `--eval_every`, `--save_every`, `--output_suffix` (`--max_steps` was already present from PR1). `--output_suffix` (string, default `""`, leading underscore included by the caller) is appended to both `run_dir` and `out_dir` basenames so a debug run cannot clobber the canonical profile dirs (`models/diffusion_od_medium/` etc.). Smoke-checked via `python -m experiments.run_stage4_train --help`. Canary run: `python -m experiments.run_stage4_train --profile medium --max_steps 20 --eval_every 10 --save_every 20 --output_suffix _canary`, 53.1 s wall, RTX 5070 Ti / bf16 / seed 42. Model: **13,340,281 params** (53.36 MB, vs pilot 920k); architecture `base=64, mults=(1,2,4,8), batch=2`, profile warmup_steps=500 so LR ramps `4e-7 → 8e-6` over the 20-step canary (target 2e-4). **No OOM**: GPU peak allocated **7455.7 MB** / reserved 7780.4 MB on the 16 GB card. train_loss `1.1717 → 0.9073` (modest decrease, LR still in warmup ramp). val_loss_ema essentially flat: `1.1440 (step 10) → 1.1418 (step 20)` — EMA buffer still ~98% random init at decay=0.999. Backward normal (grad_norm 5.0–8.3, finite). EMA `update()` every step + EMA-swap eval at steps 10/20 both ran. Checkpoint plumbing OK: `best.pt` and `last.pt` (213.6 MB each, 14× pilot) saved at step 20 under `models/diffusion_od_medium_canary/` (gitignored). In-loop `_sample_diag` (16 samples, DDIM 50 steps, cfg=2.0) executed without OOM and reported `nonzero_ratio=42.62%`, `row_ks=1.000`, `col_ks=1.000` — **garbage as expected** (model effectively untrained at 20 warmup-only steps); not acceptance evidence, only plumbing-runs-without-crashing evidence. Step time ~125–140 ms steady-state (first step 1052 ms = JIT/cudnn warmup), projecting **~11 min training + ~4 min for 5 in-loop sample_diags = ~15 min total** for the full medium 5000-step run (well under the file header's "~1-2 hr" budget). Artifacts at `results/stage4/train_medium_canary/` (`loss_curve.png`, `marginal_match.png`, `metrics.json`, `sample_grid.png`, `tb/`, `train_log.jsonl`). Canonical `models/diffusion_od_medium/` was NOT touched. **Conclusion**: memory and training/eval/save/EMA/sample_diag plumbing all pass on the medium architecture; safe to proceed to medium 5000 steps. Reminder: drop `--eval_every` / `--save_every` overrides for the full medium run so the profile defaults (`eval_every=500`, `save_every=1000`) apply. No `od_samples.npy`, no Stage-4C entry, no full medium launched.
- 2026-05-20 [stage4] 4B-4A (12 km eVTOL fallback scenario — config + data scaffold, NO training): the 15 km PR3b failure (next entry) is the documented trigger for invoking the **12 km fallback** designated in docs/decisions.md 2026-05-18. Stage 4B-4A scaffolds the fallback as an **independent scenario** that does NOT overwrite the 15 km baseline files; only configs + new `.npy` files were created, NO model training, NO `od_samples.npy`. Two new YAMLs (`configs/od_12km.yaml`, `configs/diffusion_12km.yaml`) — siblings of the 15 km configs, identical except for the threshold (`min_dist_km 15.0 → 12.0`) and the output / norm-stats paths (`*_12km.npy`, `od_meta_12km.json`, `od_norm_stats_12km.json`). NO Python / src changes — `experiments/run_stage3_build.py` and `experiments/run_stage4_train.py` were already fully config-driven, and `run_stage4_train.py`'s existing `--output_suffix` (added in PR3a) lets a future 12 km training pass write into `models/diffusion_od_*_12km/` and `results/stage4/train_*_12km/` siblings of the 15 km dirs. **Stage-3 build** `python -m experiments.run_stage3_build --config configs/od_12km.yaml` (13.0 s wall, FULL mode): `n_orders_total = 4 050 523 → n_time_in_range = 4 050 501 → n_zone_assigned = 3 637 645` (zone drop_rate 10.19%, same as 15 km); **`evtol_trip_count = 290 160`** (vs 15 km 188 699, ~1.54× more), **`evtol_share = 7.9766%`** (matches the 2026-05-18 R3 sensitivity-sweep row 7.98% to 4 dp, consistency check PASS, inside `[0.03, 0.20]`); `od_evtol_12km.npy [528, 530, 530] int32 sum 290 160 nonzero 258 358 ratio 0.1742%` (vs 15 km nonzero 173 264 ratio 0.117% — **~1.49× denser**); `od_full_12km.npy` and `od_evtol_weighted_12km.npy` (float32 weight sum 19 777 814 fare_yuan) likewise written; diagonal=0, no NaN, no negative — all acceptance checks PASS. **Per-day occupancy** (raw int counts on `od_evtol_12km.npy`): days 0–6 active (33k–40k nonzeros / day), day 7 = 56 (was 40 at 15 km — minor uptick under the looser threshold), day 8 = 1, days 9–10 = 0. **Same active-day pattern as the 15 km baseline**, so the existing `data.split [0,5]/[5,6]/[6,7]` (Mon–Fri train / Sat val / Sun test) is retained in `configs/diffusion_12km.yaml`. **Per-split raw-int nonzero confirmation**: train 240/189 413/0.2810%, val 48/35 616/0.2642%, test 48/33 272/0.2468% (15 km was 0.1897/0.1729/0.1634% — ~1.5× each, mirroring the tensor-wide ratio); all three splits non-empty. **Dataset smoke** `python -m experiments.run_stage4_dataset_smoke --config configs/diffusion_12km.yaml`: train/val/test = 240/48/48 (same as 15 km), pad_size=544, norm stats `mu=0.002074, sigma=0.039875, clip_val=100.0` (15 km was `mu=0.001378, sigma=0.032088` — both larger because 12 km is denser, neither clip-bound), normalized `train[0]` range `[-0.0005, 0.2750]`, inverse round-trip `train[10] max|err|=0.0000`, conditions correct for Mon/Sat/Sun samples; norm stats cached to `data/processed/od_norm_stats_12km.json` (15 km cache `od_norm_stats.json` left intact). **15 km baseline hard-separation verified**: `data/processed/od_full.npy / od_evtol.npy / od_evtol_weighted.npy / od_meta.json` mtimes unchanged from 2026-05-18 20:00 (not touched by today's build), `od_norm_stats.json` mtime unchanged from 2026-05-20 16:40. **Status**: 12 km is a **fallback / robustness scenario**, NOT a new standard; the 15 km baseline remains authoritative. **Next** (gated on user go-ahead): first 12 km acceptance datapoint = pilot run via `python -m experiments.run_stage4_train --profile pilot --config configs/diffusion_12km.yaml --output_suffix _12km` (~2 min, writes to `models/diffusion_od_pilot_12km/` and `results/stage4/train_pilot_12km/`). NO commit. See docs/decisions.md 2026-05-20 "Stage 4B-4: 12 km eVTOL fallback activated as an independent scenario".
- 2026-05-20 [stage4] 4B-3 PR3b (15 km medium 5000-step run — **failed diagnostic, not acceptance**): `python -m experiments.run_stage4_train --profile medium` on the PR2a fixed split with the 15 km eVTOL OD tensor, 5000 steps, **916.1 s wall (15.3 min)** (matches PR3a canary projection ~15 min), 13.34 M params (base=64, mults=(1,2,4,8), batch=2), seed 42, bf16. Average wall-per-step 183 ms (incl. 10 evals + 5 in-loop sample_diags); pure step time ~123 ms steady-state. GPU peak allocated **7455.7 MB** / reserved 7780.4 MB (identical to canary). Train loss `1.1717 → 1.9e-4` (smooth monotone decrease across all milestones: step 50 = 0.321, 500 = 0.054, 1000 = 0.009, 2500 = 0.0013, 5000 = 0.00019). val_loss_ema strictly decreasing every eval over 10 evals: `0.2861 → 0.0390 → 0.00904 → 0.00547 → 0.00421 → 0.00363 → 0.00312 → 0.00308 → 0.00296 → 0.00213` (final == best == step 5000 = 0.00213); `best.pt` was overwritten 5× because val_loss kept dropping. **In-loop sample_diag (every 1000 steps, EMA-DDIM 16 samples, cfg=2.0, rounded-int口径) tells a completely different story** — generated OD became progressively **denser**, not sparser, as training progressed: gen_nonzero_ratio `2.78% (step 1000) → 28.72% (2000) → 87.39% (3000) → 78.64% (4000) → 12.68% (5000)`; row_ks `0.962 → 1.000 → 0.876 → 0.853 → 0.290`; col_ks `0.965 → 1.000 → 0.875 → 0.837 → 0.242`. Final step-5000 numbers vs real val (0.1729 %, std mean 0.997, std ≈ 2.07 row / 2.74 col): **`gen_nonzero_ratio = 12.68%` ≈ 73× over-dense vs real 0.173%**; `gen_cont_nonzero_ratio = 99.98%` (essentially every cell carries continuous mass > 0); gen rounded `min=0, max=6, mean=0.127`; gen continuous `min=0, max=6.22, mean=0.337`; **`row_sum_ks_stat = 0.2905`** (vs pilot 0.211 — slightly worse); **`col_sum_ks_stat = 0.2416`** (vs pilot 0.345 — better); `gen_row_sum_mean = gen_col_sum_mean = 67.3` (vs real 0.997 — ~67× too much mass per row/col); gen_row_sum_std 172.8, gen_col_sum_std 173.1 (vs real 2.07 / 2.74). **Two red flags**: (i) val noise-MSE is misaligned with sample quality on this 0.17 %-sparse target (model "improves" by 130× on val while sample density swings 30× the wrong way); (ii) `n_samples_diag = 16` gives a noisy diag — the 7× swing from step 4000 (78.6 %) to step 5000 (12.68 %) over essentially-flat val_loss (0.00308 → 0.00213) reflects either real sample-distribution shift between save points or large estimator variance. This is the exact "mode collapse to dense" pitfall flagged in `docs/plan/stage4_diffusion.md` §Common Pitfalls (the dual of "mode collapse to zeros"), and is consistent with the `docs/decisions.md` 2026-05-20 §Stage-4 normalization note that scheme C's zero/nonzero gap is only ~0.25 — noise-MSE rewards predictions in the same flat region whether they emit zeros or small dense floats. **Outputs archived (renamed to prevent overwrite when re-running)**: `results/stage4/train_medium/` → `results/stage4/train_medium_15km_failed_dense_debug/` (`loss_curve.png`, `marginal_match.png`, `metrics.json`, `sample_grid.png`, `tb/`, `train_log.jsonl`); `models/diffusion_od_medium/` → `models/diffusion_od_medium_15km_failed_dense_debug/` (gitignored; `best.pt`=`last.pt`=213.6 MB at step 5000, `config.yaml`, `profile.yaml`, `train_log.jsonl`). **Conclusion**: the 15 km medium run is **not** Stage-4B acceptance evidence — it is a failed diagnostic that confirms the val-MSE / sample-quality misalignment on the 15 km eVTOL OD's 0.117 %-sparse target. **Next step decision**: switch to the **12 km eVTOL fallback** (7.98 % share, ~3× denser; designated fallback recorded in `docs/decisions.md` 2026-05-18). No commit of `models/`, no `od_samples.npy`, no Stage-4C entry, no further training launched.
- 2026-05-20 [stage4] 4B-4B (12 km pilot run — first 12 km acceptance datapoint): `python -m experiments.run_stage4_train --profile pilot --config configs/diffusion_12km.yaml --output_suffix _12km`, 1000 steps, **107.8 s wall (1.8 min)**, RTX 5070 Ti / bf16 / seed 42. Model: pilot profile = `base=32, mults=(1,2,4), batch=4`, 920,025 params (3.68 MB). Train loss `1.3434 → 0.0029` (smooth monotone decrease then noisy plateau ~0.002–0.009, no NaN, all grad_norms ≤ 1.0 clip cap). val_loss_ema strictly decreasing every eval over 5 evals: `0.7786 (step 200) → 0.3458 (400) → 0.1375 (600) → 0.0550 (800) → 0.0259 (1000)` (final == best == 0.0259); essentially identical descent to 15 km PR2b pilot (`0.7775 → 0.3453 → 0.1367 → 0.0556 → 0.0280`) — same arch + same schedule + similar normalized signal scale. End-of-run `_sample_diag` (4 EMA-DDIM samples, `guidance_scale=2.0`, 50 inference steps, rounded-int口径 from PR2a): **`gen_nonzero_ratio = 0.1436%`** vs 12 km real val **`0.2642%`** — gen/real ratio = **0.54×** (gen is ~1.84× sparser than real). Compare 15 km PR2b pilot: gen 0.0535% / real 0.1729% = 0.31× (3.2× sparser). **12 km pilot is markedly better-calibrated than 15 km pilot on the same step budget** — the denser ground truth helps the model produce non-zero predictions even at the 1000-step pilot horizon. `gen_cont_nonzero_ratio = 27.51%` (continuous floats > 0 before rounding); rounded gen `min=0, max=5, mean=0.00148`; continuous gen `min=0, max=4.52, mean=0.0202` — nothing exploded, `clip_sample` (decisions.md 2026-05-20) holds. `row_sum_ks_stat = 0.191` (vs 15 km pilot 0.211), `col_sum_ks_stat = 0.209` (vs 15 km pilot 0.345 — much better); real row/col-sum mean both 1.572, gen row/col-sum mean both 0.784 (gen totals ~half real, consistent with the 0.54× nonzero ratio). GPU peak allocated **5428.8 MB** / reserved 5809.1 MB — identical to 15 km PR2b (same arch + batch + diag size; tensor density does not change activation memory). Outputs landed at `results/stage4/train_pilot_12km/` (`loss_curve.png` 45 KB, `marginal_match.png` 31 KB, `metrics.json` 1 KB, `sample_grid.png` 452 KB, `tb/` 4 KB, `train_log.jsonl` 165 KB) and `models/diffusion_od_pilot_12km/` (`best.pt` = `last.pt` = 14.819 MB both at step 1000, `config.yaml`, `profile.yaml`, `train_log.jsonl`; gitignored). The 15 km baseline dirs (`results/stage4/train_pilot/`, `models/diffusion_od_pilot/`) were NOT touched (separate `_12km` paths). No `od_samples.npy`, no medium/full launched. **Conclusion**: 12 km pilot **passes pilot-level acceptance** — pipeline end-to-end without crash, no NaN, val_loss_ema monotone, gen/real same OOM (0.54×), KS stats non-degenerate (row 0.19, col 0.21), recovered counts non-negative bounded integers (min 0, max 5), no GPU OOM (5.4 GB / 16 GB), 15 km baseline untouched. This is **NOT Stage-4 final-model acceptance** — pilot is a 1000-step under-fit baseline; the medium-step / longer-schedule run on the 12 km tensor is the next datapoint to determine whether 12 km also tames the val-MSE / sample-quality misalignment that broke 15 km medium PR3b. **Next** (gated on user go-ahead): 12 km medium run via `python -m experiments.run_stage4_train --profile medium --config configs/diffusion_12km.yaml --output_suffix _12km` (~15 min wall projected from PR3a canary; writes to `models/diffusion_od_medium_12km/` and `results/stage4/train_medium_12km/`).
- 2026-05-20 [stage4] 4B-4C (12 km medium 5000-step run — **failed diagnostic, not acceptance**): `python -m experiments.run_stage4_train --profile medium --config configs/diffusion_12km.yaml --output_suffix _12km`, 5000 steps, **921.8 s wall (15.4 min)** (matches the 15 km PR3b 916.1 s — same arch, same step count, same eval/save cadence), RTX 5070 Ti / bf16 / seed 42, 13.34 M params (medium profile defaults `base=64, mults=(1,2,4,8), batch=2`; no CLI overrides), avg step time ~125–138 ms steady-state. Train loss `1.1717 → 0.0002` (smooth monotone descent into the noise floor with two absorbed spikes at step 850 = 0.149 and step 3450 = 0.124; 1.0 grad-clip held). val_loss_ema strictly decreasing across all 10 evals **except a single eval-4000 bump** `0.0039 → 0.0047`: `0.2865 → 0.0390 → 0.0094 → 0.0058 → 0.0046 → 0.0040 → 0.0039 → 0.0047 → 0.0032 → 0.0025` (best == final == step 5000 = 0.00253); `best.pt` overwritten 5×. **In-loop sample_diag (every 1000 steps, EMA-DDIM 16 samples, cfg=2.0, rounded-int口径) confirms the same val-MSE / sample-quality misalignment as 15 km PR3b**: gen_nonzero_ratio swung wildly while val_loss kept dropping — step 1000 = 9.58%, step 2000 = 36.74%, step 3000 = **99.99%**, step 4000 = 6.55% (brief calibrated dip: row_ks 0.235, col_ks 0.162 — looked momentarily healthy), step 5000 = **68.43%** (collapsed back to dense). Final step-5000 vs 12 km real val (`0.2642%`, row/col-sum mean 1.572): **`gen_nonzero_ratio = 68.43% ≈ 259× over-dense vs real 0.2642%`** (15 km PR3b was 73× over-dense at the same step count — **12 km medium is worse on this axis**); `gen_cont_nonzero_ratio = 99.998%` (essentially every cell carries continuous mass > 0); gen rounded `min=0, max=9, mean=0.685`; gen continuous `min=0, max=9.47, mean=0.611`; **`row_sum_ks_stat = 0.770`** (15 km PR3b 0.291), **`col_sum_ks_stat = 0.773`** (15 km PR3b 0.242); `gen_row_sum_mean = gen_col_sum_mean = 363.1` (real 1.572 → ~231× too much mass per row/col), gen_row_sum_std 240.3 / gen_col_sum_std 240.4 (real 3.35 / 4.38). GPU peak allocated **7455.7 MB** / reserved 7780.4 MB (identical to 15 km PR3b — same arch + batch + diag size; tensor density does not change activation memory). **Outputs archived (renamed to prevent overwrite on any rerun)**: `results/stage4/train_medium_12km/` → `results/stage4/train_medium_12km_failed_dense_debug/` (`loss_curve.png`, `marginal_match.png`, `metrics.json`, `sample_grid.png`, `tb/`, `train_log.jsonl`); `models/diffusion_od_medium_12km/` → `models/diffusion_od_medium_12km_failed_dense_debug/` (gitignored; `best.pt` = `last.pt` = 213.6 MB at step 5000, `config.yaml`, `profile.yaml`, `train_log.jsonl`). 15 km baseline dirs untouched; no `od_samples.npy` written, no Stage-4C entry. **Conclusion**: 12 km medium **does NOT pass Stage-4 acceptance** — same failure mode as 15 km PR3b (mode-collapse-to-dense under val-MSE / sample-quality misalignment), and quantitatively worse at the final step (259× vs 73× over-dense, KS ~0.77 vs ~0.27). The 4B-4B pilot improvement (gen/real ratio 0.54× vs 15 km pilot 0.31×) was real but **did not extrapolate to the medium scale**. The 2026-05-18 hypothesis "if Stage-4 underperforms due to OD sparsity, switch to 12 km" is **falsified for the medium architecture under the current normalization scheme** — densifying the data ~1.5× was not the lever. **Next**: do NOT continue threshold tuning. Pivot to a normalization / loss / sampler ablation; candidate directions recorded in docs/decisions.md 2026-05-20 "Stage 4B-4C: 12 km fallback hypothesis falsified at the medium architecture" (A. nonzero-aware / zero-pinned normalization, B. weighted loss on nonzero entries, C. lower `guidance_scale` / guidance ablation, D. sample-quality-based checkpoint selection / early stopping, E. bootstrap-resampling baseline as the documented Stage-4 robustness fallback).
- 2026-05-20 [stage4] 4B-5A (posthoc guidance / sampler ablation — no training, no retrain, no `od_samples.npy`): new `experiments/run_stage4_guidance_ablation.py` (CLI: `--config`, `--ckpt`, `--profile`, `--n_samples`, `--guidance_scales`, `--num_inference_steps`, `--output_dir`, optional `--no_grid` / `--seed`; loads a checkpoint, installs EMA weights, sweeps `guidance_scale` with `seed=42` fixed init noise across scales, reports rounded-int & continuous nonzero ratios, row/col KS, gen min/max/mean, row/col mean/std real vs gen, per-scale wall time, NaN check; writes `metrics.csv` + `metrics.json` + `sample_grid.png`). Run on the **12 km medium failed-dense-debug `best.pt`** (step 5000, val_loss_ema=0.0025): `python -m experiments.run_stage4_guidance_ablation --config configs/diffusion_12km.yaml --ckpt models/diffusion_od_medium_12km_failed_dense_debug/best.pt --profile medium --n_samples 64 --guidance_scales 0.0 0.5 1.0 1.5 2.0 --num_inference_steps 50 --output_dir results/stage4/guidance_ablation_12km_medium_failed --seed 42`. Wall **685.9 s (~11.4 min)** for all 5 scales × 64-sample DDIM-50 sweep on RTX 5070 Ti; n_samples capped to **48** (val loader exhausted at 48; the warning printed). Real val nonzero (12 km, int reference) `0.2642%`; reference unchanged across scales. **Sweep result (rounded-int口径)**: gs **0.00** → gen_nz **6.806%** (25.8× real), row_ks 0.102, col_ks 0.110, gen_max 11, gen_row_sum_mean 36.4; gs **0.50** → **12.916%** (48.9×), row_ks 0.160, col_ks 0.163, gen_row 68.8; gs **1.00** (no CFG) → **21.446%** (81.2×), row_ks 0.280, col_ks 0.285, gen_row 113.9; gs **1.50** → **32.880%** (124.5×), row_ks 0.374, col_ks 0.384, gen_row 174.5; gs **2.00** (training default) → **44.200%** (167.3×), row_ks 0.482, col_ks 0.489, gen_row 234.6. Real row-sum mean 1.572 in all rows. **No NaN at any scale**; continuous nonzero stays 95-99.9% across scales (the rounding to int is what kills small floats and converts the model's continuous over-population to fewer integer nonzeros). gen_max 10-12 at all scales — opposite tail behavior to real OD (peak hundreds), so the failure mode is "too many small values everywhere", not "huge spikes". Note: in-loop step-5000 diag reported `gen_nonzero_ratio=68.43%` at the same gs=2.0 / `n_samples=16`; here `gs=2.0 / n_samples=48` gives 44.2% — small-sample / seed variance of the 16-sample diag, but qualitative conclusion (deeply over-dense at gs=2.0) is the same. **Conclusions**: (i) **lowering `guidance_scale` monotonically and substantially reduces over-density** (~6.5× drop in gen_nonzero, ~4.7× drop in row_ks from 2.0 → 0.0) — guidance IS a real contributor; (ii) **no `guidance_scale` in `[0, 2]` reaches the real 0.2642 %** — even pure unconditional (gs=0.0) is **25.8× too dense**, so guidance alone CANNOT fix the medium checkpoint, the model has internalized over-population; (iii) **guidance ablation is a necessary but not sufficient fix**; the remaining gap must be closed at the normalization / loss layer. **Recommended next step (gated on user go-ahead)**: enter **normalization + weighted-loss ablation** (decisions.md 2026-05-20 "Stage 4B-4C: 12 km fallback hypothesis falsified" → directions A "nonzero-aware / zero-pinned normalization" + B "weighted loss on nonzero entries"); use `guidance_scale=1.0` (no CFG) or 0.0 (pure uncond) as the new training default while the loss/normalization is being changed, so a future medium retrain starts ~4× closer to ground truth than the current 2.0 default. Outputs at `results/stage4/guidance_ablation_12km_medium_failed/` (`metrics.csv` 1.8 KB, `metrics.json` 4.4 KB, `sample_grid.png` 364 KB; all gitignored under `results/`); checkpoint untouched; 15 km baseline untouched; no `od_samples.npy`. See `docs/decisions.md` 2026-05-20 "Stage 4B-5A: posthoc guidance ablation".
- 2026-05-20 [stage4] 4B-5B PR5B-2 (dataset smoke + 20-step train smoke under the new zpin config — NO 1000-step pilot, NO medium, NO `od_samples.npy`, NO Stage 4C entry): config under test is `configs/diffusion_12km_zpin_weighted.yaml` (sibling of `configs/diffusion_12km.yaml`; deltas: `data.scheme=zero_pinned_nonzero`, `data.clip_val=20.0`, `data.norm_stats_path=data/processed/od_norm_stats_12km_zpin.json`, `diffusion.guidance_scale=1.0`, `train.loss_weight=null`; PR5B-1 plumbing committed in `78f08e6`). Minimal patch to `experiments/run_stage4_dataset_smoke.py` (uncommitted prior to this entry — bundled into the PR5B-2 commit): reads `data.scheme` (default `global_clip` so old `configs/diffusion.yaml` and `configs/diffusion_12km.yaml` behave bit-identically), threads `scheme=...` into all three `ODDataset()` constructors, branches the norm-stats print between `mu`/`sigma` (`global_clip`) and `mu_nz`/`sigma_nz` (`zero_pinned_nonzero`), adds a `zpin pin check` block that verifies raw-zero entries map to exactly -1.0 inside `[Z, Z]` and across the padded strip, extends the inverse round-trip section to confirm raw-zero entries restore to exactly 0. **Dataset smoke** (`python -m experiments.run_stage4_dataset_smoke --config configs/diffusion_12km_zpin_weighted.yaml`): `scheme=zero_pinned_nonzero`, `norm_stats` keys `['clip_val','mu_nz','scheme','sigma_nz']`, **`mu_nz=0.738038`**, **`sigma_nz=0.150805`**, `clip_val=20.0`, cached to `data/processed/od_norm_stats_12km_zpin.json` (123 B JSON). Splits 240/48/48 (Mon..Fri / Sat / Sun), pad 530→544 unchanged. `train[0]` normalized min/max/mean = **-1.0000 / 0.1195 / -0.9994** (one-sided toward -1.0 as expected for 99.83 % zero density); `val[0]` similar (-1 / 0.1195 / -0.9990); `test[0]` (-1 / 0.2889 / -0.9989). **Zero-pin check on train sample 10**: 280 730 raw-zero entries all at exactly -1.0; 15 232 padded entries all at -1.0; 170 nonzero in the slice. **Inverse round-trip max|err| = 0.0000** (12 km counts are small — log1p(count)/`sigma_nz` ≪ `clip_val=20`, nothing saturates); all 280 730 raw-zero entries restored to exact 0.0. Pre-/post-smoke baseline-cache mtimes verified unchanged (`od_norm_stats.json` 16:40, `od_norm_stats_12km.json` 17:45) — neither 15 km nor 12 km baseline was overwritten. **20-step train smoke** (`python -m experiments.run_stage4_train --profile pilot --config configs/diffusion_12km_zpin_weighted.yaml --max_steps 20 --eval_every 10 --save_every 20 --output_suffix _zpin_smoke`): runtime confirms `data.scheme=zero_pinned_nonzero`, effective `guidance_scale=1.0` (YAML override of profile default 2.0 takes effect), `loss_weight (cfg)=None (plumbing only, not enabled)` — the script's `NotImplementedError` guard remains armed against any non-null `loss_weight`. Device cuda / RTX 5070 Ti, torch 2.11.0+cu128, bfloat16 AMP. Model 920 025 params (3.68 MB) — pilot profile, base=32, mults=(1,2,4). **Train loss** finite throughout, descended **1.336 → 0.126** over the 20 steps; **`val_loss_ema`** 1.302 @ step 10, 1.278 @ step 20, both finite; **grad_norm** in [1.04, 8.86], all finite. Wall 6.5 s. **GPU peak allocated 5 428.8 MB, reserved 5 809.1 MB.** No NaN/inf at any step; no `data/synthetic/od_samples.npy` written (the `data/synthetic/` directory remained empty). Checkpoints at `models/diffusion_od_pilot_zpin_smoke/` (gitignored): `best.pt` 14.8 MB, `last.pt` 14.8 MB, `config.yaml`, `profile.yaml`, `train_log.jsonl`. Tracked artifacts at `results/stage4/train_pilot_zpin_smoke/`: `loss_curve.png`, `marginal_match.png`, `metrics.json`, `sample_grid.png`, `train_log.jsonl` (the `tb/` event-dir is gitignored). End-of-run `sample_diag` reported `gen_nonzero_ratio=77.88 %`, `row_ks=1.000`, `col_ks=1.000` — **these are wiring-check numbers, NOT acceptance evidence**: 20 gradient steps from random init cannot calibrate the sample distribution; the diag pipeline running end-to-end on the new scheme is the only thing this smoke proves about samples. Minor cosmetic note: step 14 console line shows `dt=-6446 ms`, a wall-time arithmetic artifact (likely a `torch.cuda.synchronize()` measurement disturbed by the eval/ckpt buffer flush at the surrounding step); forward/backward themselves stayed finite (loss=0.1987, grad_norm=2.02 at that step). **PR5B-2 acceptance: PASS** on every gate from the plan — dataset smoke prints sensible `mu_nz`/`sigma_nz`, inverse round-trip exact, zero pin exact, scheme tag persisted to the cache; train smoke survives 20 steps with finite loss/grad, scheme/guidance/loss_weight wired correctly, isolated output dirs, no NaN/inf, no `od_samples.npy`; the two existing baseline norm_stats caches byte-for-byte unchanged. Safe to proceed to **PR5B-3 (1000-step 12 km zpin pilot)** after commit/push/clear; PR5B-3 is gated on user go-ahead and is **not** launched in this entry. See `docs/decisions.md` 2026-05-20 "Stage 4B-5A: posthoc guidance ablation" for the immediate predecessor; the PR5B-2 result itself is not yet a decisions.md entry (no plan deviation — purely the plumbing/smoke validation step from the PR5B-1 plan).
- 2026-05-20 [stage4] 4B-5B PR5B-3 (1000-step 12 km zpin pilot — **failed diagnostic, not acceptance; NO medium, NO Stage 4C, NO `od_samples.npy`, NO weighted loss**): `python -m experiments.run_stage4_train --profile pilot --config configs/diffusion_12km_zpin_weighted.yaml --output_suffix _zpin`. Config under test: `configs/diffusion_12km_zpin_weighted.yaml` with `data.scheme=zero_pinned_nonzero`, `data.clip_val=20.0`, `data.norm_stats_path=data/processed/od_norm_stats_12km_zpin.json`, `diffusion.guidance_scale=1.0`, `train.loss_weight=null` (PR5B-1 plumbing only; weighted loss not enabled — the `NotImplementedError` guard in `experiments/run_stage4_train.py` remained armed and stayed silent against the null setting). Profile pilot: max_steps=1000, base=32, mults=(1,2,4), batch=4, ema_decay=0.999, eval_every=200, save_every=500, warmup=0, n_samples_diag=4, diag_at_end_only=True, num_inference_steps=50; YAML `diffusion.guidance_scale=1.0` overrides the profile default 2.0 (verified at runtime via the "profile" print block). Device cuda / RTX 5070 Ti, torch 2.11.0+cu128, bfloat16 AMP. Model 920 025 params (3.68 MB). Splits 240/48/48 (Mon..Fri / Sat / Sun), pad 530→544. **Wall 100.5 s (1.7 min)** — well under the 10–20 min pilot budget. **Train loss**: 1.3363 @ step 1 → ~0.024 @ step 50 → oscillates in 0.003–0.06 thereafter → **0.0121 @ step 1000** (`train_loss_last`); rapid collapse to ~0.01 within the first 50 steps then flat, typical eps-MSE behaviour at 240 train samples; finite throughout, grad_norm in [0.04, 8.86] all finite, no NaN/inf at any step. **`val_loss_ema` monotone-decreasing across every eval**: 0.8099 @ 200 → 0.4280 @ 400 → 0.2134 @ 600 → 0.1087 @ 800 → **0.0606 @ 1000** (best = final). **End-of-run `sample_diag`** (n=4, DDIM 50 steps, EMA weights, guidance_scale=1.0): **`gen_nonzero_ratio=34.0277 %`** on rounded ints vs **real 12 km val nonzero_ratio=0.2642 %**, i.e. **128.8 × over-dense** — the failure mode is qualitatively the same as the 4B-4C / 4B-5A baselines and **worse** than the 4B-5A no-CFG posthoc lower bound (21.4 %, 81.2 × at gs=0.0 on the global_clip medium ckpt). **`gen_cont_nonzero_ratio=73.96 %`** (continuous floats pre-round): almost every cell is non-zero before rounding; only the [-0.5, 0.5] band collapses to 0 on `np.rint`. **Recovered counts** (rounded ints): min 0, **max 34**, mean 0.4546; continuous: min 0, max 33.54, mean 0.5003. **gen_max=34** is substantially above the 4B-5A guidance-ablation ceiling (~10–12) — the zpin scheme escaped the global_clip saturation cap (mu_nz≈0.738, sigma_nz≈0.151 admits per-cell counts well past clip_val=20 in normalised space), but the over-density problem moved from "everywhere saturated near the ceiling" to "fewer huge spikes plus widespread small counts" with total mass still ~153 × too high. **Marginal KS**: `row_sum_ks_stat=1.000`, `col_sum_ks_stat=1.000` (maximally mismatched); real row_sum_mean=1.572 vs gen row_sum_mean=240.91 (~153×); real col_sum_mean=1.572 vs gen col_sum_mean=240.91 (~153×). **GPU peak allocated 5 428.8 MB, reserved 5 809.1 MB**; nvidia-smi reported 12 929 MB free at launch so the run was nowhere near OOM. Tracked artifacts at `results/stage4/train_pilot_zpin/` (this commit): `loss_curve.png` 48 KB, `marginal_match.png` 29 KB, `metrics.json` 1.4 KB, `sample_grid.png` 680 KB, `train_log.jsonl` 164 KB (the `tb/` event-dir is gitignored). Checkpoints at `models/diffusion_od_pilot_zpin/` (gitignored): `best.pt` 14.8 MB (== last.pt at step 1000, both correspond to the only save), `last.pt` 14.8 MB, `config.yaml`, `profile.yaml`, `train_log.jsonl`. No `data/synthetic/od_samples.npy` written. **PR5B-3 acceptance gates** (`pass: 0.05 % < gen_nonzero_ratio < 2.6 %`; `mild: 2.6 %–6.6 %`; `fail: > 6.6 % or < 0.05 %`): **FAIL at 34.0277 %**. Interpretation: zero-pinned normalization **alone**, with `guidance_scale=1.0` and no weighted loss, **does not** close the over-density gap on the 0.117 %-sparse 12 km eVTOL tensor — it removes the count ceiling (gen_max 10–12 → 34) but worsens off-zero mass allocation. The 4B-5A working hypothesis (zpin necessary but not sufficient) is **confirmed**; the next lever to test is the weighted ε-loss (PR5B-3b), **gated on user go-ahead and not launched in this entry**. Cosmetic note: step 1000 console line shows `dt=-6372 ms`, same wall-time arithmetic artifact as PR5B-2 step 14 (likely a `torch.cuda.synchronize()` measurement disturbed by the end-of-run eval/diag/ckpt flush at the surrounding step); forward/backward themselves stayed finite (loss=0.0121, grad_norm=0.169 at that step). See `docs/decisions.md` 2026-05-20 "Stage 4B-5B PR5B-3: zero-pinned normalization alone falsified" for the falsification record and the explicit gating boundary against PR5B-3b.
- 2026-05-20 [stage4] 4B-5B PR5B-3b-1 (weighted-loss plumbing implementation + tests — **NO training launched**): commit `2fdafb7`, 6 files / +310 -28. `src/data/od_dataset.py::ODDataset.__init__` gains `return_raw: bool = False` (stored as `self.return_raw`); `__getitem__` returns the existing 2-tuple `(x, cond)` by default and a 3-tuple `(x, cond, raw_padded)` when `return_raw=True`, where `raw_padded = padded.astype(np.float32)` is the un-normalised, already-padded count tensor of shape `[window, pad_size, pad_size]` (padded entries exact 0.0 from `pad_hw`). `src/models/diffusion.py` gains the pure helper `build_weight_map(raw_padded, cfg)` which computes `w = 1 + alpha * I(raw>0) + beta * log1p(raw)` and divides by `w.mean()` when `cfg.normalize == "mean"`; the result is fp32, on `raw_padded`'s device, computed inside `torch.no_grad()` and `.detach()`-ed (belt-and-suspenders so weight never participates in autograd). `experiments/run_stage4_train.py`: `_TorchODDataset.__getitem__` branches on `len(item) == 3` to forward the raw tensor; the old `NotImplementedError` guard is replaced with `weight_enabled = yaml_loss_weight is not None`, mode/normalize front-loaded validation, train_base built with `return_raw=weight_enabled` (val_base untouched), training loop unpacks the 3-tuple only when enabled and forwards `weight_map=build_weight_map(raw_padded, yaml_loss_weight)` to `diff.training_loss`; the build call sits *outside* the bfloat16 autocast block so the weight is computed in fp32. `_evaluate` is unchanged — it keeps passing `weight_map=None` so `val_loss_ema` stays directly comparable to PR5B-2 / PR5B-3 across configs. `configs/diffusion_12km_zpin_weighted.yaml::train.loss_weight` flipped from `null` to `{mode: nonzero_log1p, alpha: 2.0, beta: 0.5, normalize: mean}` with an inline rationale block (mild α=2/β=0.5 chosen over α=5/β=1 to avoid the symmetric "hot-spot collapse" failure mode); other configs (`configs/diffusion.yaml`, `configs/diffusion_12km.yaml`, `configs/diffusion_15km.yaml` if present) carry no `loss_weight` key and therefore hit the unweighted F.mse_loss codepath bit-equally. New tests: `tests/test_od_dataset.py` (+5) covers default 2-tuple, opt-in 3-tuple, raw matches padded counts via `np.testing.assert_array_equal`, padding-is-zero, `window>1` shape; `tests/test_diffusion.py` (+8) covers `raw=0 -> w=1`, count=1 closed-form `1 + alpha + beta*log1p(1)`, `normalize=mean` yields `w.mean()==1` with hot-cell monotonicity, detached (no grad leak into `raw`, gradients still flow through `pred`), broadcasts to x0 in a real loss, fp64-input fp32-output dtype + same-device guarantee, rejects unsupported `mode`, rejects unsupported `normalize`. Suite ran **133 passed in 3.87 s** — the +13 new tests on top of the existing 120. No training, no smoke, no pilot launched in this entry; the diff was reviewed before commit and exactly the whitelist of 6 files staged (no `models/`, `data/`, `.claude/`, `docs/handoff/`, `results/`, `tb/`, `od_samples.npy`). PR5B-3b-2 (integration smoke) and PR5B-3b-3 (1000-step weighted pilot) are gated on user go-ahead and are **not** launched here. No `docs/decisions.md` entry — this is pure implementation/plumbing matching the PR5B-3 decisions.md plan ("Direction B: weighted ε-loss as a separate PR5B-3b"); a decisions.md entry lands only when the 1000-step pilot produces falsification-worthy evidence.
- 2026-05-20 [stage4] 4B-5B PR5B-3b-2 (weighted-loss 20-step integration smoke — **NOT acceptance evidence, NO 1000-step pilot, NO medium, NO Stage 4C, NO `od_samples.npy`**): `python -m experiments.run_stage4_train --profile pilot --config configs/diffusion_12km_zpin_weighted.yaml --max_steps 20 --eval_every 10 --save_every 20 --output_suffix _zpin_weighted_smoke`. Runtime confirms `data.scheme=zero_pinned_nonzero` and the new run-summary line **`loss_weight (cfg): enabled  mode=nonzero_log1p  alpha=2.0  beta=0.5  normalize=mean`**, so the YAML → flag → print path is hooked up correctly. Effective profile: pilot base=32, mults=(1,2,4), batch=4, ema_decay=0.999, eval_every=10, save_every=20, warmup=0, n_samples_diag=4, diag_at_end_only=True, num_inference_steps=50; YAML `guidance_scale=1.0` override of profile default 2.0 verified. Device cuda / RTX 5070 Ti, torch 2.11.0+cu128, bfloat16 AMP. Model 920 025 params (3.68 MB). Splits 240/48/48, pad 530→544. **Weighted-loss path verified end-to-end**: `train_base.return_raw=True` (smoke would crash on tuple-unpacking if False since training loop expects 3-tuple under `weight_enabled=True`), `raw_padded` flowed from `_TorchODDataset` through DataLoader's default-collate stack into the training loop, `build_weight_map(raw_padded, yaml_loss_weight)` produced a fp32 weight tensor on cuda matching `[B=4, 1, 544, 544]`, and `diff.training_loss(..., weight_map=weight_map)` consumed it inside the bfloat16 autocast block. **Train loss** finite throughout, descended **1.3358 (step 1) → 0.1524 (step 20)** — magnitude near-identical to the PR5B-2 unweighted 20-step smoke (1.336 → 0.126) because mean-normalisation pins `w.mean()≈1`; intermediate steps 1.121 / 0.994 / 0.897 / 0.792 / 0.751 / 0.603 / 0.520 / 0.486 / 0.406 / 0.333 / 0.460 / 0.370 / 0.199 / 0.295 / 0.272 / 0.140 / 0.224 / 0.115 all finite. **grad_norm finite throughout, range [0.96, 8.85]** — within the PR5B-2 unweighted band [1.04, 8.86]; the α=2/β=0.5 mild weighting did not destabilise gradients. **`val_loss_ema`** at fixed-seed t: **step 10 = 1.3018**, **step 20 = 1.2783** — bit-near-identical to PR5B-2 unweighted (1.302 / 1.278) because `_evaluate` still calls `training_loss(..., weight_map=None)` so val stays unweighted and comparable. No NaN/inf at any step in forward / backward / grad-clip / EMA-update / eval. **GPU peak allocated 5 433.2 MB, reserved 5 830.1 MB** — +4.4 MB / +21.0 MB over PR5B-2 (5 428.8 / 5 809.1), consistent with one extra fp32 raw-padded tensor (`[4, 1, 544, 544]` ≈ 4.7 MB) per step plus the transient weight_map. Card has 12 929 MB free → nowhere near OOM. Wall **12.8 s** (vs PR5B-2 6.5 s — roughly +6 s for the raw-padded data movement + 20× weight-map builds + bfloat16 autocast on a wider input pipe, all well within smoke budget). Checkpoints under `models/diffusion_od_pilot_zpin_weighted_smoke/` (gitignored): `best.pt` 14.820 MB, `last.pt` 14.820 MB, `config.yaml` 1.0 KB, `profile.yaml` 270 B, `train_log.jsonl` 4.2 KB (best == last; single save at step 20). Tracked artifacts at `results/stage4/train_pilot_zpin_weighted_smoke/`: `loss_curve.png` 40 KB, `marginal_match.png` 28 KB, `metrics.json` 1.1 KB, `sample_grid.png` 1.3 MB, `train_log.jsonl` 4.2 KB (the `tb/` event-dir is gitignored). **No `data/synthetic/od_samples.npy` written** — `data/synthetic/` mtime untouched since 2026-05-14. End-of-run `sample_diag` (n=4, DDIM 50 steps, EMA weights, gs=1.0) reported `gen_nonzero_ratio=77.8764 %`, `row_ks=1.000`, `col_ks=1.000`, `gen_max=44`, `gen_mean=12.78` — **THIS IS NOT ACCEPTANCE EVIDENCE**: 20 gradient steps from random init cannot calibrate the sample distribution under any normalisation/loss scheme; the PR5B-2 unweighted 20-step smoke reported exactly the same 77.88 % gen_nonzero (both runs share seed=42 and the model has barely moved from the init prior). The smoke gates only on **plumbing/finite-loss/no-OOM**, all of which **PASS**. **PR5B-3b-2 conclusion**: the weighted-loss integration is wired correctly and runs stably end-to-end; the actual sample-quality acceptance evidence for direction B must come from **PR5B-3b-3** — a 1000-step pilot under the same `configs/diffusion_12km_zpin_weighted.yaml`, gated on user go-ahead and **not** launched in this entry. Falsification target carried over from `docs/decisions.md` 2026-05-20 "Stage 4B-5B PR5B-3" Findings 2: PR5B-3b-3 must move `gen_nonzero_ratio` strictly below 21.45 % (the 4B-5A no-CFG posthoc lower bound on the global_clip medium ckpt) at the same pilot architecture/step budget to count as direction-B evidence; the formal acceptance band stays `pass: 0.05 % < gen_nonzero_ratio < 2.6 %`, `mild: 2.6 %–6.6 %`, `fail: > 6.6 % or < 0.05 %` from the PR5B-3 plan. No `docs/decisions.md` entry needed here — this is pure plumbing validation, no plan deviation.
- 2026-05-20 [stage4] 4B-5B PR5B-3b-3 (1000-step 12 km zpin + weighted-loss pilot — **failed diagnostic, not acceptance; NO medium, NO Stage 4C, NO `od_samples.npy`, NO further alpha/beta tuning**): `python -m experiments.run_stage4_train --profile pilot --config configs/diffusion_12km_zpin_weighted.yaml --output_suffix _zpin_weighted`. Effective config at runtime: `data.scheme=zero_pinned_nonzero`, `diffusion.guidance_scale=1.0`, `train.loss_weight={mode: nonzero_log1p, alpha: 2.0, beta: 0.5, normalize: mean}`, profile pilot (max_steps=1000 / batch=4 / base=32 / mults=(1,2,4) / 920 025 params / ema_decay=0.999 / eval_every=200 / save_every=500 / warmup=0 / n_samples_diag=4 / diag_at_end_only=True / num_inference_steps=50). Device cuda / RTX 5070 Ti, torch 2.11.0+cu128, bfloat16 AMP. Splits 240/48/48, pad 530→544. **Wall 100.1 s (1.7 min)** — essentially identical to PR5B-3 zpin-only (100.5 s); the extra `raw_padded` data movement + per-step `build_weight_map` is free at this batch size. **Train loss** finite throughout: 1.3358 (step 1, grad_norm 8.85, cold start) → 0.0475 (step 200) → 0.0119 (step 400) → 0.0157 (step 600) → 0.0398 (step 800) → **0.0216 (step 1000, `train_loss_last`)**; rapid collapse to ~0.02 within 200 steps then flat, near-isomorphic to PR5B-3 zpin-only (1.336 → 0.012) — expected because `normalize: mean` pins `w.mean()≈1` so the gross loss scale tracks the unweighted F.mse_loss case. `grad_norm` finite throughout, no NaN/inf. **`val_loss_ema`** monotone-decreasing: 0.8167 @ 200 → 0.4388 @ 400 → 0.2204 @ 600 → 0.1121 @ 800 → **0.0601 @ 1000** (best = final). Bit-near-identical to PR5B-3 zpin-only (0.8099 / 0.4280 / 0.2134 / 0.1087 / 0.0606) — confirms `_evaluate` keeps the unweighted path (`weight_map=None`) so val stays directly comparable across PR5B-3 / PR5B-3b-3. End-of-run `sample_diag` (n=4, DDIM 50 steps, EMA weights, gs=1.0): **`gen_nonzero_ratio=37.7617 %`** on rounded ints vs **real 12 km val nonzero_ratio=0.2642 %**, i.e. **143.0 × over-dense**; **`gen_cont_nonzero_ratio=75.03 %`** (continuous floats pre-round, vs PR5B-3 zpin-only 73.96 %). Recovered counts rounded int: min 0, **max 45**, mean **0.5356**; continuous: min 0, max 44.75, mean 0.5768. Marginal KS: `row_sum_ks_stat=1.000`, `col_sum_ks_stat=1.000` (maximally mismatched); real row_sum_mean=1.572 vs gen row_sum_mean **283.87** (~181 ×); real col_sum_mean=1.572 vs gen col_sum_mean 283.87 (~181 ×). **Direct comparison vs PR5B-3 zpin-only pilot** (same architecture / step budget / split / seed, only difference: `train.loss_weight` populated): gen_nonzero_ratio went **34.0277 % → 37.7617 %** (+3.7 pp **worse**), gen/real **128.8 × → 143.0 ×** (+11 %), gen_max **34 → 45** (+11), gen_mean **0.4546 → 0.5356** (+18 %), gen_row_sum_mean **240.9 → 283.9** (+18 %). **Falsification target check** (carried over from `docs/decisions.md` 2026-05-20 "Stage 4B-5B PR5B-3" Findings 2, "must move gen_nonzero_ratio strictly below 21.45 % — the 4B-5A no-CFG posthoc lower bound on the global_clip medium ckpt"): 37.76 % ≫ 21.45 % → target **missed by +16.3 pp**. **GPU peak allocated 5 433.2 MB, reserved 5 830.1 MB** (matches the PR5B-3b-2 smoke; the raw-padded path costs ~+5 MB vs PR5B-3 unweighted, far from the 12 929 MB free budget). Tracked artifacts at `results/stage4/train_pilot_zpin_weighted/` (this commit): `loss_curve.png` 51 KB, `marginal_match.png` 29 KB, `metrics.json` 1.4 KB, `sample_grid.png` 675 KB, `train_log.jsonl` 164 KB (the `tb/` event-dir is gitignored). Checkpoints under `models/diffusion_od_pilot_zpin_weighted/` (gitignored): `best.pt` 14.820 MB (== last.pt at step 1000), `last.pt` 14.820 MB, `config.yaml`, `profile.yaml`, `train_log.jsonl`. **`data/synthetic/od_samples.npy` NOT written** (`data/synthetic/` empty). PR5B-3b-3 **acceptance** (`pass: 0.05 % < r < 2.6 %`; `mild: 2.6 %–6.6 %`; `fail: > 6.6 % or < 0.05 %`): **FAIL at 37.76 %**, 5.7 × the upper edge of the fail band; **extra falsification target `< 21.45 %`: MISSED**. **Conclusion**: mild `nonzero_log1p` weighted loss with `alpha=2.0 / beta=0.5 / normalize=mean` did **not** reduce over-density at the pilot architecture / 1000-step budget — it slightly worsened every density metric vs zpin-only. The mechanism is consistent: `w.mean()≈1` (mean-norm) preserves the unweighted loss landscape near-isomorphically and the `~3.3 × w_zero` ratio on nonzero pixels is too symmetric to break the "many small positives everywhere" attractor. **Do NOT proceed to medium under the current α/β setting** — the same architecture/loss already failed at the falsification surface chosen by the PR5B-3 plan. **Do NOT blindly retune α/β** without first writing a new diagnostic/strategy plan (see `docs/decisions.md` 2026-05-20 "Stage 4B-5B PR5B-3b-3: mild weighted loss falsified" for the next-step decision boundary). PR5B-4 / PR5B-5 / next direction is **deferred** pending the strategy plan.
- 2026-05-21 [stage4] 4B-5C PR5C-2A (conditional bootstrap sampler module + tests — **NO training, NO diffusion run, NO posthoc calibration, NO Stage-5 work, NO real `od_samples_agg*.npy` generated**): new file `src/data/bootstrap_od.py` (no edits to existing modules) implements `ConditionalBootstrapSampler` together with three helpers `split_train_slots_into_day_blocks(train_slots, slots_per_day)`, `aggregate_day_blocks(od, selected_blocks)`, `compute_bootstrap_summary(samples)`. Sampler signature: `od: np.ndarray | np.memmap [T, Z, Z]`, `train_slots: Sequence[int] | range`, `slots_per_day=48`, `n_days_per_scenario=11`, `n_omega=64`, `seed=0`, `mode="day_block"` (the only mode in PR5C-2A; `VALID_MODES == ("day_block",)`). Output contract from `sample()`: `np.ndarray` shape **`[n_omega, Z, Z]`**, **dtype `int32`**, **nonnegative** (defensive `out.min() < 0` raise; relies on Stage-3 OD being nonnegative integer counts). Day-block bootstrap logic: at construction `train_slots` is chopped into consecutive `slots_per_day`-length blocks via `split_train_slots_into_day_blocks` (validated: empty `train_slots`, non-positive `slots_per_day`, or `len(train_slots) % slots_per_day != 0` all raise `ValueError` *at construction* so a partial day-block off-by-one fails before any sample read); each scenario draws `n_days_per_scenario` block indices with `np.random.default_rng(seed).integers(0, n_blocks, ...)` (with replacement), and `aggregate_day_blocks` sums `od[s]` over every slot of every drawn block into an int64 accumulator then casts to int32. **No Poisson perturbation, no demand scaling** — these levers stay deferred to PR5C-2B per the user's scope. **Strict no-leak contract**: `day_blocks` is derived entirely from `train_slots`; after each `sample()` the union of drawn slot indices is recorded as `self.used_slots: set[int]`, and a defensive `used - set(train_slots) == ∅` check raises `RuntimeError` if it ever isn't; `self.selected_day_indices: np.ndarray [n_omega, n_days_per_scenario] int32` indexes into `day_blocks` (NOT into `od`) so a leak would also be visible by `selected_day_indices.max() ≥ n_train_blocks`; out-of-range `train_slots` (any index < 0 or ≥ `T`) also raises at construction. New file `tests/test_bootstrap_od.py` covers the 10 user-requested cases plus 9 supplementary edge cases on a 4-day / 3-zone synthetic OD where each slot `s` has value `s+1` everywhere (so a leak from val/test slots is numerically obvious): `split` cuts into full day blocks (`blocks[0] == range(0, 48)`, `blocks[1] == range(48, 96)`, flattened == `train_slots`); partial-day block raises `ValueError`; empty `train_slots` raises; invalid `slots_per_day=0` raises; sample shape `[64, 3, 3]`; sample dtype `int32`; sample nonnegative; same-seed bit-identical (`np.testing.assert_array_equal`); different seeds yield non-identical output; `used_slots ⊆ set(TRAIN_SLOTS)` and `isdisjoint` of both `VAL_SLOTS` and `TEST_SLOTS`, `selected_day_indices.max() < TRAIN_DAYS=2` and `.min() >= 0`; per-scenario total variance > 0 and per-cell `samples.std(axis=0).max() > 0` (n=64 scenarios with 11 draws over 2 day blocks gives ~12 distinct (k1, k2) compositions → variance is non-trivial); `aggregate_day_blocks` equals manual `sum over od[s].astype(int64) for s in block` over a hand-picked block list `[blocks[0], blocks[1], blocks[0]]` (verifies with-replacement repeats sum correctly); sampler emission per scenario equals manual sum over `selected_day_indices[i]`; sampler ctor rejects partial day-block / empty `train_slots` / unknown `mode="poisson"` / out-of-range `train_slots`; `compute_bootstrap_summary` returns keys `n_omega`/`z`/`total_sum_mean`/`total_sum_std`/`nonzero_ratio_mean`/`nonzero_ratio_std`/`global_min`/`global_max` with `global_min >= 0` and `total_sum_mean > 0`. **Test results**: `pytest -q tests/test_bootstrap_od.py` → **19 passed in 0.26 s**; full suite `pytest -q` → **152 passed in 4.09 s** (+19 over the PR5B-3b-3 baseline 133, no regressions). **No data artifacts written**: `data/synthetic/od_samples_agg.npy` is **not** touched (the frozen Stage-5 input waits for the PR5C-3 unified scenario-generation comparison and a user-confirmed write); the candidate `data/synthetic/od_samples_agg_bootstrap.npy` is **not** written either (PR5C-2B will write it under a guarded path); `data/synthetic/od_samples.npy` (4-D diffusion output) is untouched. No `models/`, no new `results/` artifacts, no `tb/` directories, no `.claude/` files, no `docs/handoff/` changes staged in this commit. **PR5C-2A acceptance: PASS** — bootstrap fallback module is wired and tested; the Stage-4 robustness path documented in `docs/plan/stage4_diffusion.md` "Robustness Note" and `docs/decisions.md` 2026-05-20 PR5B-3b-3 "bootstrap / resampling baseline as a Stage-4 fallback" now has importable code behind it. **Next step is PR5C-2B** (real bootstrap generation against the 12 km train OD tensor + sample-quality metrics vs real val), but **not in this commit** — gated on user go-ahead. No `docs/decisions.md` entry: this is pure implementation/plumbing of an already-accepted fallback direction, no plan deviation.
- 2026-05-21 [stage4] 4B-5C PR5C-2B (real 12 km bootstrap scenario generation + metrics — **NO training, NO diffusion run, NO posthoc calibration, NO Stage-5 work, frozen `data/synthetic/od_samples_agg.npy` NOT generated**): new file `experiments/run_stage4_bootstrap.py` (no edits to existing modules) drives `ConditionalBootstrapSampler` (PR5C-2A, commit `dc4236f`) against the real 12 km eVTOL OD tensor. Command: `python -m experiments.run_stage4_bootstrap --config configs/diffusion_12km.yaml --output data/synthetic/od_samples_agg_bootstrap.npy --results-dir results/stage4/bootstrap --n-omega 64 --seed 42 --n-days-per-scenario 11 --slots-per-day 48`. Effective config: `od_path=data/processed/od_evtol_12km.npy` (`[528, 530, 530]` int32, mmap'd), `meta_path=data/processed/od_meta_12km.json` (T=528, n_zones=530, time_bin_min=30 → slots_per_day=48 matches the CLI), splits from `configs/diffusion_12km.yaml::data.split`: train `[0, 5)` = slots 0..239 (5 day blocks), val `[5, 6)` = slots 240..287, test `[6, 7)` = slots 288..335. CLI carries explicit refusal guards against both the frozen Stage-5 paths `data/synthetic/od_samples_agg.npy` and `data/synthetic/od_samples.npy`; first invocation crashed in `_write_summary` on `Path.relative_to(REPO)` for a relative `--config` argument (cosmetic bug, no data side-effect), fixed by adding a `_rel(p)` helper that falls back to `str(p)` on `ValueError`, re-ran deterministically (seed=42 → bit-identical samples). **Candidate output**: `data/synthetic/od_samples_agg_bootstrap.npy` (untracked: `/data/` is anchored-ignored), shape `(64, 530, 530)`, dtype `int32`, **nonnegative** (`min=0`, `max=1257`, `mean=1.662`, total mass `29 883 272`), on-disk 71.9 MB. Per-scenario total mass min/mean/max = `454 985 / 466 926 / 479 692`; per-scenario nonzero-ratio min/mean/max = `0.140 / 0.174 / 0.183`. **Frozen Stage-5 inputs untouched**: `data/synthetic/od_samples_agg.npy` does NOT exist (refusal guard active), `data/synthetic/od_samples.npy` does NOT exist either, `data/synthetic/` listing shows only `od_samples_agg_bootstrap.npy`. **Leak check (empirical, post-sample)**: `train_slots` = 240 (= 5 days × 48 slots), `used_slots` = 240 (full train coverage from 64 × 11 = 704 day-block draws), `used_slots ⊆ train_slots = True`, `leaked_to_val = 0`, `leaked_to_test = 0`; the sampler's internal `used - set(train_slots) == ∅` defensive `RuntimeError` did not fire. **Acceptance verdict: MILD** (3 of 5 PASS gates met, all 4 MILD gates met). Per-day-equivalent comparisons (bootstrap aggregates 11 calendar days per scenario while `real_test` aggregates 1 day on the 12 km split, so primary KS / total-mass comparisons divide bootstrap by `n_days_per_scenario` and `real_test` by `n_test_days`): **PASS gates met** — `per_day_total_mass_ratio.mean = 1.126` ∈ [0.7, 1.5] ✓; `per_day_row_sum_ks.mean = 0.093` ≤ 0.3 ✓; `per_day_col_sum_ks.mean = 0.078` ≤ 0.3 ✓. **PASS gates missed** — `gen_nonzero_ratio_x_real_test.mean = 2.677` outside [0.7, 1.5] ✗ (intrinsic aggregation effect: union of nonzero cells across 11 train days exceeds the nonzero set of a single test day); `top20_pair_overlap.mean = 11.89 / 20` below ≥ 14 ✗ (Mon-Fri-weighted bootstrap vs Sunday test day; the `top20_vs_top50.mean = 19.83 / 20` confirms ranking is right, only the strict top-20 cutoff is mismatched). **MILD floor met** — bootstrap strictly beats the failed-diffusion PR5B-3b-3 baseline on every headline axis: row_sum_ks 0.093 vs 1.000 (10.8 × better), col_sum_ks 0.078 vs 1.000 (12.8 × better), gen_nonzero_ratio × real 2.68 vs 143.0 (53.4 × better), per-day total_mass_ratio 1.13 vs 181.0 (160 × better). **Diagnostic numbers** (`results/stage4/bootstrap/metrics.json`): `gen_nonzero_ratio.mean = 0.1740` vs `real_test_nonzero_ratio = 0.0650`; `per_day_total_mass.mean = 42 447.8` vs `real_per_day_test_total_mass = 37 710.0`; `gen_max.mean = 1152.6, max = 1257` vs `real_test_max = 121` (consistent with the 11-day aggregation); `entropy.mean = 9.852 nats` vs `real_test_entropy = 9.313 nats`; `total_mass_ratio_raw.mean = 12.38` (matches `n_days_per_scenario / n_test_days = 11 / 1`); `row_sum_ks_stat_raw.mean = 0.805`, `col_sum_ks_stat_raw.mean = 0.820` (dominated by the 11× scale mismatch; kept for transparency, not used for the acceptance verdict). **Tracked artefacts staged in this commit**: `experiments/run_stage4_bootstrap.py` (new, 645 lines), `results/stage4/bootstrap/metrics.json`, `results/stage4/bootstrap/metrics.csv` (one row per scenario, 13 columns), `results/stage4/bootstrap/marginal_match.png` (per-day-equivalent row/col-sum histograms: real_test vs bootstrap mean), `results/stage4/bootstrap/summary.md` (human-readable verdict). **Explicit non-actions**: no commit to `data/synthetic/od_samples_agg_bootstrap.npy` (anchored-ignored by `/data/` in `.gitignore`; regeneration is deterministic via the command above with `seed=42`); no `data/synthetic/od_samples_agg.npy` write; no diffusion training, no posthoc calibration, no Stage-5 code edits; no `tb/` directories, no `models/` artefacts, no `.claude/` files, no `docs/handoff/` changes staged. **Interpretation**: bootstrap is **not the final PASS** but it is a **usable candidate** for the Stage-4 fallback path and is far better than failed diffusion on row/col KS, nonzero ratio, and total mass. **Stage 5 remains blocked** until PR5C-3 selects a scenario source (bootstrap candidate vs PR5C-1 posthoc-calibrated diffusion vs other) and the user confirms copying the chosen file to `data/synthetic/od_samples_agg.npy`. PR5C-3 is **not** entered in this commit — gated on user go-ahead. See `docs/decisions.md` 2026-05-21 "Stage 4B-5C PR5C-2B: bootstrap fallback candidate generated (MILD, not PASS)" for the band definitions and the explicit gating boundary against PR5C-3.
- 2026-05-21 [stage4] 4B-5C PR5C-1A (posthoc calibration module + tests — **NO training, NO diffusion checkpoint loaded, NO sampling, NO npy generated, frozen `data/synthetic/od_samples_agg.npy` NOT touched, `data/synthetic/od_samples_agg_diffusion_calibrated.npy` NOT generated, `experiments/run_stage4_posthoc_calibrate.py` NOT written**): new package `src/eval/` (with empty `__init__.py`) and new module `src/eval/posthoc_calibration.py` (560 lines, pure-function, no torch / no I/O on real OD or model checkpoints; only imports `numpy` and `src.utils.metrics_dist.ks_stat_1d`). Eight public symbols exported: `clip_nonnegative(x)` = `np.maximum(x, 0)` (**explicitly does NOT use `np.abs`**; the test suite pins this against an input `[-3.5, -1.0, 0.0, 2.7]` where `np.maximum` gives `[0, 0, 0, 2.7]` and `np.abs` would give `[3.5, 1.0, 0.0, 2.7]`, and asserts the outputs are *not* equal); `apply_threshold_and_scale(samples_cont, tau, scale) -> int32` (pipeline: clip → mask `x > tau` → `round(mask * x * scale).astype(int32)`, supports `[Z, Z]` and `[N, Z, Z]`, rejects negative `tau` / `scale`, output guaranteed `dtype=int32` and `min >= 0`); `sparse_aggregate_stats(x_int)` (`nonzero_ratio`, `total_mass`, `mean`, `max`, `row_sums`, `col_sums`, `entropy`; 3-D input also returns per-sample arrays + `_mean` / `_std` summaries); `compute_reference_stats(real_agg, real_k=20, exclude_diagonal=True)` (returns real reference scalars + `real_top{real_k}_pairs` set); `topk_pair_overlap(gen_agg, real_agg, real_k=20, gen_k=50, exclude_diagonal=True) -> int` (real top-`real_k` ∩ gen top-`gen_k`, **diagonal excluded by default** consistent with Stage-3 `drop_intra_zone: true`, tie-broken deterministically by stable argsort on flat index ascending); `grid_search_tau_scale(samples_cont, real_train_agg, tau_grid=None, scale_grid=None, lambda_mass=1.0)` with defaults `np.linspace(0.1, 2.0, 20)` × `np.geomspace(1e-3, 2.0, 20)` (400-point grid), objective `|nz_ratio / real_nz_ratio - 1| + lambda_mass * |total_mass / real_total_mass - 1|` (**train target only**: `real_train_agg` must be 2-D `[Z, Z]`, a 3-D `[N, Z, Z]` stack is rejected with `ValueError` by the API contract so callers cannot accidentally pass val/test slices; row/col KS and top-k are **deliberately NOT in the calibration objective** to avoid overfitting marginal shape to the fit target); `evaluate_calibrated_samples(samples_cont, real_ref_agg, tau, scale, exclude_diagonal=True)` (reporting-only path: callers may pass val/test here without a leakage check, since the fit never reads this reference; returns flat dict with `nonzero_ratio_x_real`, `total_mass_ratio`, `row_sum_ks_stat`, `col_sum_ks_stat`, `top20_pair_overlap`, `top20_pair_overlap_against_top50`, `gen_max`, `gen_mean`, `entropy`, plus per-sample arrays and `_mean`/`_std` for 3-D input); `acceptance_verdict(metrics, ...)` (rule-based `pass`/`mild`/`fail` with kw-only parametrisable thresholds: PASS gates `nz_x ∈ [0.7, 1.5]`, `total_mass_ratio ∈ [0.8, 1.2]`, `top20_pair_overlap >= 12`, `row_sum_ks_stat <= 0.3`, `col_sum_ks_stat <= 0.3`; MILD floor strictly beats `DEFAULT_FAILED_DIFFUSION_BASELINE = {row_sum_ks=1.0, col_sum_ks=1.0, nonzero_ratio_x_real=143.0, total_mass_ratio=181.0}` on all four headline axes; reads `<key>_mean` if present so it works on both per-sample-summary and single-sample variants). New file `tests/test_posthoc_calibration.py` covers the 7 user-requested categories on small synthetic matrices (no real data, no checkpoint): `clip_nonnegative` pins literal example `[-2, -0.5, 0, 1] -> [0,0,0,1]`, distinguishability from `np.abs` on `[-3.5, -1.0, 0.0, 2.7]`, 3-D shape preservation; `apply_threshold_and_scale` covers tau filtering, very-high-tau yields all zeros, scale changes magnitude (`scale=0.5/1.0/2.0` round through correctly with banker's rounding noted on the `0.5 -> 0` edge), negatives clipped before round, output `int32` + nonneg, rejects negative tau/scale; `sparse_aggregate_stats` on a hand-checked `3x3` example (`nonzero_ratio = 3/9`, `total_mass = 6`, row sums `[1, 2, 3]`, col sums `[3, 1, 2]`), 3-D summary array shapes + scalars, 1-D rejected; `topk_pair_overlap` on a `5x5` hand-built case with expected `overlap = 3` for `real_k=3, gen_k=5`, diagonal-exclusion behaviour discriminated under multiple `(real_k=1, gen_k=1)` configurations (including a real-with-zero-diag + gen-with-huge-diag case where `exclude_diagonal=True` correctly picks `(0,1)` for both, and `exclude_diagonal=False` picks `(0,0)` from gen and mismatches), tie-determinism on a three-way tie; `grid_search_tau_scale` on a signal-vs-noise synthetic where the optimum is roughly `(tau=1.0, scale=1.0)` — asserts `best_objective < 0.5 * worst_objective` and `best_metrics["objective"] == grid_objectives.min()`, also pins the default `20 x 20 = 400` grid size, rejects 3-D `real_train_agg`, rejects all-zero reference; `evaluate_calibrated_samples` returns all expected keys with finite values for both 2-D and 3-D inputs, and KS / ratio scalars stay finite under the all-zero edge case (`tau=1e9` collapses every cell to 0; `nonzero_ratio_x_real_mean = 0.0` and `total_mass_ratio_mean = 0.0` as expected, KS values finite); `acceptance_verdict` exercises all three branches (PASS on a hand-crafted `(nz=1.0, mass=1.0, top20=16, ks=0.10)` metrics dict; MILD when `nonzero_ratio_x_real_mean = 2.7` (PASS band fails, baseline floor met) and separately when `top20_pair_overlap_mean = 10.0`; FAIL when every axis matches the baseline; bare-key variant without `_mean` suffix accepted; thresholds parametrisable via kw-only args). **Test results**: `pytest -q tests/test_posthoc_calibration.py` → **29 passed in 0.19 s**; full suite `pytest -q` → **181 passed in 4.25 s** (+29 over the PR5C-2B baseline 152, no regressions). Two `RuntimeWarning: Mean of empty slice` warnings fire from the deliberate all-zero-calibration test (`test_evaluate_ks_stats_are_finite_under_zero_calibration`): with `tau=1e9` every calibrated sample is the zero matrix → `_shannon_entropy` returns `NaN` by design (mass=0 → undefined) → `entropy_per_sample` is all-NaN → `np.nanmean` warns. The test's own assertions all pass — this is documented behaviour at the API boundary, not a defect. **Explicit non-actions**: no `experiments/run_stage4_posthoc_calibrate.py` written; no diffusion checkpoint loaded or sampled; no `data/synthetic/od_samples_agg.npy` or `data/synthetic/od_samples_agg_diffusion_calibrated.npy` generated (neither path exists on disk); no Stage-5 code edited; no `models/`, `data/`, `results/`, `.claude/`, or `docs/handoff/` files staged in this commit; no `tb/` directories. **PR5C-1A acceptance: PASS** — posthoc calibration module is wired and tested; the PR5C-1 direction now has importable pure-function code behind it. **Next step is PR5C-1B** (CLI `run_stage4_posthoc_calibrate.py` + real-checkpoint evaluation against the 4B-4C medium failed-debug `best.pt`, producing the candidate `data/synthetic/od_samples_agg_diffusion_calibrated.npy`), but **not in this commit** — gated on user go-ahead. No `docs/decisions.md` entry: this is pure implementation/plumbing of an already-acknowledged fallback direction (`docs/decisions.md` 2026-05-20 PR5B-3b-3 "post-sample thresholding calibrated against real val row-/col-sums"), no plan deviation. The PR5C-3 unified scenario-source comparison remains the gate on `data/synthetic/od_samples_agg.npy`.
- 2026-05-21 [stage4] 4B-5C PR5C-1B (real posthoc calibration evaluation against the PR5B-3b-3 zpin+weighted pilot checkpoint — **NO training, NO new checkpoint, NO Stage-5 work, frozen `data/synthetic/od_samples_agg.npy` NOT generated, `data/synthetic/od_samples_agg_diffusion_calibrated.npy` NOT generated, bootstrap candidate `data/synthetic/od_samples_agg_bootstrap.npy` NOT modified**): new file `experiments/run_stage4_posthoc_calibrate.py` (no edits to existing modules) composes `src/eval/posthoc_calibration.py` (PR5C-1A, commit `c567b80`) with the real PR5B-3b-3 weighted-zpin pilot best.pt to produce a diagnostic report only. Command: `python -m experiments.run_stage4_posthoc_calibrate --config configs/diffusion_12km_zpin_weighted.yaml --ckpt models/diffusion_od_pilot_zpin_weighted/best.pt --profile pilot --output-dir results/stage4/posthoc_calibration_zpin_weighted --n-samples 48 --seed 42 --num-inference-steps 50 --tau-grid-start 0.1 --tau-grid-end 2.0 --tau-grid-size 20 --scale-grid-start 1e-3 --scale-grid-end 2.0 --scale-grid-size 20 --lambda-mass 1.0`. Loaded ckpt: `step=1000, val_loss=0.0601`, profile `pilot` (base=32, mults=(1,2,4), 920 025 params), `data.scheme=zero_pinned_nonzero`, `pad_size=544`, `Z=530`, `guidance_scale=1.0` (YAML override of profile default 2.0 verified at runtime), `num_inference_steps=50`, bfloat16 AMP on RTX 5070 Ti. **Sampling**: 48 continuous OD slices drawn via DDIM with EMA weights, conditions taken from val loader (1 day = 48 slots, full hour grid covered exactly once), then `train_base.inverse_transform()` unpadded and inverted to count space — `samples_cont.shape=(48, 530, 530)`, dtype float64, `min=0.0000`, `max=45.2318`, `mean=0.5898`; sample wall time 26.6 s. **Calibration fit (TRAIN AGGREGATE TARGET ONLY)**: `real_train_agg = od_evtol_12km[train_slots].sum(axis=0)` = `[Z, Z]` int64 with `nonzero_ratio=0.183343, total_mass=212 392` (sum over 240 train slots = 5 days). Grid: `tau ∈ linspace(0.1, 2.0, 20)` × `scale ∈ geomspace(1e-3, 2.0, 20)` = 400 points; objective `|nz_ratio / real_nz - 1| + 1.0 * |total_mass / real_total - 1|` (row/col KS and top-k deliberately NOT in the objective per the PR5C-1A spec — avoids overfitting marginal shape to the fit target). Grid wall 40.3 s. **Best: `tau=1.0000, scale=2.0000` (scale at upper grid edge), train objective `0.046187`**. Test untouched for fitting = **True** (val/test never read by `grid_search_tau_scale`; consumed only by `evaluate_calibrated_samples` for reporting). **Before / after by split** (before = `clip_nonnegative` → `round`; after = `apply_threshold_and_scale(tau=1.0, scale=2.0)`): | split | phase | nz_x_real | mass_ratio | row_ks | col_ks | top20 | top20vs50 | gen_max | gen_mean |. train before 2.067 / 0.727 / 0.495 / 0.480 / 0 / 0 / 42.75 / 0.5497; train after 1.003 / 0.957 / 0.544 / 0.547 / 0 / 0 / 85.58 / 0.7233; val before 5.546 / 3.860 / 0.961 / 0.954 / 0 / 0 / 42.75 / 0.5497; val after 2.691 / 5.080 / 0.970 / 0.969 / 0 / 0 / 85.58 / 0.7233; test before 5.831 / 4.094 / 0.961 / 0.966 / 0 / 0 / 42.75 / 0.5497; **test after 2.829 / 5.388 / 0.971 / 0.969 / 0 / 0 / 85.58 / 0.7233**. (Real reference aggregates: `train.sum=212 392 / 240 slots / 5 days`, `val.sum=40 000 / 48 slots / 1 day`, `test.sum=37 710 / 48 slots / 1 day` — the calibrator is matched to train-aggregate scale so val/test `mass_ratio ≈ 5` by construction, NOT a model failure; the headline diagnostic is the spatial axis.) **Test acceptance verdict: MILD** (0 of 5 PASS gates met, all 4 MILD floor gates met). PASS gates on test (after): nz_x_real 2.829 outside [0.7, 1.5] ✗; mass_ratio 5.388 outside [0.8, 1.2] ✗ (scale mismatch — train-agg vs 1-day test); top20 0 < 12 ✗ (catastrophic); row_ks 0.971 > 0.3 ✗; col_ks 0.969 > 0.3 ✗. MILD floor: row_ks 0.971 < 1.000 ✓; col_ks 0.969 < 1.000 ✓; nz_x_real 2.829 < 143.0 ✓; mass_ratio 5.388 < 181.0 ✓. **Improvement vs uncalibrated PR5B-3b-3 in-loop diag**: gen_nonzero_ratio × real_test 143.0 → 2.83 (50.5 × cut) — the calibrator does fix the over-density. **Spatial structure NOT recovered**: `top20_pair_overlap = 0/20` and `top20_pair_overlap_against_top50 = 0/20` mean the checkpoint produces samples whose top OD pairs have ZERO rank correlation with real_test top pairs; no posthoc thresholding can fix a model with no spatial coherence. **Comparison to bootstrap candidate** (PR5C-2B): bootstrap row_ks 0.093 vs calibrated 0.971 (**10.4 × better for bootstrap**); bootstrap col_ks 0.078 vs 0.969 (**12.4 × better**); bootstrap top20 11.89 / 20 vs calibrated 0 / 20 (**bootstrap wins decisively**); bootstrap per-day mass_ratio 1.13 vs calibrated train-agg mass_ratio 0.957 (both close to 1 on the right scale; not a discriminating axis). Both verdicts are MILD per the rule, but **bootstrap delivers a usable scenario distribution while the calibrated diffusion delivers calibrated marginals on top of a model with no learned spatial structure**. PR5C-1B is best read as a **comparison row** for the PR5C-3 unified comparison, NOT as a candidate competing for the Stage-5 source. **Tracked artefacts staged in this commit**: `experiments/run_stage4_posthoc_calibrate.py` (new, 524 lines), `results/stage4/posthoc_calibration_zpin_weighted/metrics.json`, `results/stage4/posthoc_calibration_zpin_weighted/metrics.csv` (6 rows: 2 phases × 3 splits), `results/stage4/posthoc_calibration_zpin_weighted/calibration_grid.png` (log10 objective heatmap with argmin marker — argmin sits at upper-right corner since `scale=2.0` is the grid edge), `results/stage4/posthoc_calibration_zpin_weighted/marginal_match_before_after.png` (row/col-sum histogram trio: real_test vs before vs after), `results/stage4/posthoc_calibration_zpin_weighted/decision_report.md`. **Safety verification**: `data/synthetic/od_samples_agg.npy` absent (refusal guard against `--output-dir == data/synthetic/` active in the CLI); `data/synthetic/od_samples.npy` absent; `data/synthetic/od_samples_agg_diffusion_calibrated.npy` absent (deliberately NOT written this PR — PR5C-3 decides if/when); `data/synthetic/od_samples_agg_bootstrap.npy` present and unmodified (script never opens it). **Explicit non-actions**: no commit to any `data/synthetic/*.npy`; no new checkpoint trained; no medium run; no Stage-5 code edited; no `models/`, `.claude/`, `docs/handoff/` changes staged; no `tb/` directories (the only `tb/` under `results/stage4/posthoc_calibration_zpin_weighted/` is absent — TensorBoard is not used by this CLI). **Conclusion**: PR5C-1B completed; the diffusion-calibrated path is **MILD** but **inferior to bootstrap** on every shape-and-ranking axis. **Next step is PR5C-3** (unified scenario-source comparison and user-confirmed copy to `data/synthetic/od_samples_agg.npy`), but **not in this commit** — gated on user go-ahead. See `docs/decisions.md` 2026-05-21 "Stage 4B-5C PR5C-1B: posthoc calibration MILD, inferior to bootstrap" for the verdict-level decision record.
- 2026-05-21 [stage4] 4B-5C PR5C-3A (unified scenario-source comparison — **NO training, NO diffusion, NO resampling, NO freeze, frozen `data/synthetic/od_samples_agg.npy` NOT generated, no candidate npy created or overwritten, NO Stage-5 work**): new file `experiments/run_stage4_compare_scenarios.py` (no edits to existing modules) reads the per-source metrics JSON written by PR5C-2B (`results/stage4/bootstrap/metrics.json`) and PR5C-1B (`results/stage4/posthoc_calibration_zpin_weighted/metrics.json`) and produces a unified comparison table + advisory winner recommendation. Command: `python -m experiments.run_stage4_compare_scenarios --bootstrap-metrics results/stage4/bootstrap/metrics.json --posthoc-metrics results/stage4/posthoc_calibration_zpin_weighted/metrics.json --output-dir results/stage4/comparison`. **Outputs**: `results/stage4/comparison/metrics_all.csv` (one row per source, 18 columns), `results/stage4/comparison/metrics_all.json` (strict JSON — `_json_safe` maps non-finite floats to `null` and the dump uses `allow_nan=False`, verified by `python -m json.tool`), `results/stage4/comparison/decision.md` (human-readable winner recommendation + caveats). **Four comparison sources** (sorted by the decision rule): (1) `bootstrap_day_block` — tier MILD, `nz_x_real=2.677`, per-day `mass_ratio=1.126`, per-day `row_ks=0.093`, per-day `col_ks=0.078`, `top20=11.89/20`, `top20vs50=19.83/20`, `gen_max=1152.6`, `gen_mean=1.662`, `entropy=9.852`, `has_candidate_npy=True`, `can_freeze_to_stage5=True`; (2) `diffusion_raw_zpin_weighted_pilot` — PR5C-1B before-calibration (clip+round only), tier MILD, `nz_x_real=5.831`, per-day-equiv `mass_ratio=0.819`, raw `row_ks=0.961`, raw `col_ks=0.966`, `top20=0`, `top20vs50=0`, `gen_max=42.75`, `gen_mean=0.550`, `entropy=11.396`, `has_candidate_npy=False`, `can_freeze_to_stage5=False`; (3) `diffusion_calibrated_zpin_weighted_pilot` — PR5C-1B after-calibration (`tau=1.0, scale=2.0`), tier MILD, `nz_x_real=2.829`, per-day-equiv `mass_ratio=1.078`, raw `row_ks=0.971`, raw `col_ks=0.969`, `top20=0`, `top20vs50=0`, `gen_max=85.58`, `gen_mean=0.723`, `entropy=10.677`, `has_candidate_npy=False`, `can_freeze_to_stage5=False` (PR5C-1B deliberately wrote no npy); (4) `diffusion_failed_baseline_pr5b_3b3` — the PR5B-3b-3 floor record, tier FAIL, `nz_x_real=143.0`, `mass_ratio=181.0`, `row_ks=1.000`, `col_ks=1.000`, `top20=0`, `gen_max/gen_mean/entropy=null` (never measured). **Decision rule** (lowest tuple wins): `can_freeze_to_stage5` (True>False) → `acceptance_tier` (PASS>MILD>FAIL) → `top20_pair_overlap` (higher) → `row_sum_ks_stat` (lower) → `col_sum_ks_stat` (lower) → `|total_mass_ratio - 1|` (closer to 1). **Recommended winner: `bootstrap_day_block`** — it is the **only** candidate with `has_candidate_npy=True` and `can_freeze_to_stage5=True` (the bootstrap candidate `data/synthetic/od_samples_agg_bootstrap.npy` already exists from PR5C-2B), and it dominates the two diffusion rows on every structural axis: `top20` 11.89 vs 0, `row_ks` 0.093 vs 0.97, `col_ks` 0.078 vs 0.97. The `diffusion_calibrated` source is **retained as a comparison row** (useful for the manuscript's scenario-generation comparison) but is **NOT recommended as the Stage-5 source** — posthoc calibration cuts the over-density (`nz_x_real` 143 → 2.83) but cannot recover spatial structure (`top20=0`, `row/col KS ≈ 1`). **Scale caveat documented in decision.md**: the KS / mass_ratio columns are not at one uniform scale (bootstrap uses per-day-equivalent KS; diffusion rows report raw KS at train-aggregate scale, with `mass_ratio` rescaled to per-day-equivalent by ÷5); the headline conclusion holds under any reasonable rescaling because the structural gap is order-of-magnitude on KS and qualitative on top20. **Safety verification**: the CLI records pre-/post-run `data/synthetic/` state and asserts it is unchanged — confirmed `data/synthetic/ state unchanged by this run`; `data/synthetic/od_samples_agg.npy` absent, `data/synthetic/od_samples.npy` absent, `data/synthetic/od_samples_agg_diffusion_calibrated.npy` absent, `data/synthetic/od_samples_agg_bootstrap.npy` present and unmodified (the script never opens it); the CLI also carries a refusal guard against `--output-dir` resolving to `data/synthetic/`. **This PR does NOT freeze any source** — `data/synthetic/od_samples_agg.npy` is still not generated; the freeze (copying the bootstrap candidate to the frozen path) is a separate sub-PR **PR5C-3B** gated on explicit user confirmation. **Tracked artefacts staged in this commit**: `experiments/run_stage4_compare_scenarios.py` (new), `results/stage4/comparison/metrics_all.csv`, `results/stage4/comparison/metrics_all.json`, `results/stage4/comparison/decision.md`. **Explicit non-actions**: no `data/synthetic/*.npy` created or modified; no training; no diffusion run or resampling; no Stage-5 code; no `models/`, `.claude/`, `docs/handoff/`, or `tb/` changes staged. **Conclusion**: PR5C-3A completed; bootstrap is the recommended Stage-5 scenario source. **Next step is PR5C-3B** (user-confirmed freeze of `data/synthetic/od_samples_agg.npy`), but **not in this commit** — gated on user go-ahead. See `docs/decisions.md` 2026-05-21 "Stage 4B-5C PR5C-3A: unified comparison recommends bootstrap" for the decision record.
- 2026-05-21 [stage4] 4B-5C PR5C-3B (freeze bootstrap as Stage-5 scenario source — **NO training, NO Stage-5 work, NO model changes; freeze + checksum + metadata only**): the user confirmed `bootstrap_day_block` (the PR5C-3A recommended winner) as the Stage-5 scenario source. Action: `cp data/synthetic/od_samples_agg_bootstrap.npy data/synthetic/od_samples_agg.npy`. **Verification**: candidate pre-check passed (`shape=(64, 530, 530)`, `dtype=int32`, `min/max/mean=0/1257/1.662`, nonnegative=True); post-copy SHA-256 of source and destination match exactly — `327de8858deed51b0abe2b9018b51e7ddbd93054678429a4164c9db9cf9d2d18`; frozen file re-checked via mmap (`shape=(64, 530, 530)`, `dtype=int32`, `min/max/mean=0/1257/1.662`, nonnegative=True). **Tracking**: `data/synthetic/od_samples_agg.npy` (69 MB) is a local artefact and stays gitignored (`/data/` is anchored-ignored in `.gitignore`) — NOT committed; it is regenerable deterministically from `experiments/run_stage4_bootstrap.py` with `seed=42`, or by re-copying the bootstrap candidate. `data/synthetic/od_samples_agg_source.txt` is a small provenance record committed with `git add -f` (records source name, source/frozen paths, shape, dtype, the SHA-256, and the selection/freeze PR ids). The bootstrap candidate `data/synthetic/od_samples_agg_bootstrap.npy` is left in place unmodified. **Tracked artefacts staged in this commit**: `docs/progress.md`, `docs/decisions.md`, `data/synthetic/od_samples_agg_source.txt` (force-added). **Explicit non-actions**: no training, no diffusion run, no model edits, no Stage-5 code; no `models/`, `results/`, `.claude/`, `docs/handoff/`, or `tb/` changes staged; the `.npy` files themselves are not committed. **Conclusion**: PR5C-3B completed — `data/synthetic/od_samples_agg.npy` now exists as the frozen Stage-5 scenario input (`[N_ω=64, |Z|=530, |Z|=530]` int32, the format `docs/plan/stage5_rl_env.md` expects). **Stage 5 is now unblocked.** See `docs/decisions.md` 2026-05-21 "Stage 4B-5C PR5C-3B: bootstrap frozen as Stage-5 scenario source" for the closure record.
- 2026-05-21 [stage5] PR1 (minimal VertiportEnv scaffold + smoke — **NO PPO training, NO Stage 6 work, NO gymnasium/sb3-contrib install, NO Stage 4 result changes**): new files `configs/env.yaml`, `src/envs/__init__.py`, `src/envs/vertiport_env.py`, `tests/test_vertiport_env.py`, `experiments/run_stage5_env_smoke.py`. Implements the minimal `VertiportEnv`: reads the frozen Stage-5 scenario source `data/synthetic/od_samples_agg.npy` (`[N_ω=64, |Z|=530, |Z|=530]` int32, bootstrap_day_block, frozen by PR5C-3B) and the Stage-2 candidate coverage mask `data/processed/cand_covers_zones.npy` (`[|C|=813, |Z|=530]` bool). **MDP**: action = candidate id (discrete, size 813); already-selected candidates are invalid and under `invalid_action=mask` a repeated `step()` raises `ValueError`; `action_masks()` returns the `[C]` bool selectable mask (`True` = not yet selected). **Observation** = dict `{selected_mask [C] float32, covered_zones [Z] float32, remaining_budget scalar float32, current_coverage_ratio scalar float32}`. **Reward** = incremental bilateral OD coverage — an OD pair `(i,j)` is covered iff both origin zone `i` and destination zone `j` are in the covered zone set; per step `reward = (newly covered OD demand) / total OD demand`, normalized, nonnegative, division-by-zero safe (`total_demand == 0` → reward/coverage 0). `terminated = selected_count >= k_select` (`k_select=10`). Each episode samples one scenario index per `reset()`; `reset(seed)` re-seeds the RNG so `scenario_idx` is reproducible. **PR1 is a deliberate minimal subset of `docs/plan/stage5_rl_env.md`** — see `docs/decisions.md` 2026-05-21 "Stage 5 PR1: minimal VertiportEnv scaffold — deliberate deviations from the Stage-5 plan" for the 5 recorded deviations (simplified observation dict, single frozen scenario source / no `p_real` real-vs-scenario mixing, pure incremental-coverage reward with no overlap/cost penalty, no `vec_env.py`/`coverage.py`/`EnvConfig` dataclass, Gymnasium-style class without importing `gymnasium`). **Tests**: `pytest -q tests/test_vertiport_env.py` → **10 passed** (reset returns obs/info; action_masks shape; step increments selected_count; repeated action masked + raises; reward nonnegative; coverage_ratio monotone non-decreasing; terminated after k steps; hand-computed bilateral coverage on a 4×4 case; seed reproduces scenario_idx; zero-total-demand safe). Full suite `pytest -q` → **191 passed, 2 warnings** (the 2 warnings are pre-existing in `test_posthoc_calibration.py`, unrelated to this PR). **Real-data smoke**: `python -m experiments.run_stage5_env_smoke --config configs/env.yaml` — env built (n_omega=64, n_zones=530, n_candidates=813, k_select=10), `reset(seed=42)` → `scenario_idx=5`, one full random-policy episode (10 steps) ran without exception → `final coverage_ratio=0.1047`, `total reward=0.1047`, `total_covered_demand=48845`. **Explicit non-actions**: no PPO training, no Stage 6 work, no `gymnasium`/`sb3-contrib` install or `pyproject.toml` change, no Stage 4 results modified; no `data/`, `models/`, `results/`, `.claude/`, `docs/handoff/`, or `tb/` changes staged. **Conclusion**: Stage 5 PR1 passes — the minimal `VertiportEnv` is functional and Stage 5 PR2 (Gymnasium / MaskablePPO integration) can proceed.
- 2026-05-21 [stage5] PR2 (Gymnasium + MaskablePPO minimal training smoke — **SMOKE only, NO paper baseline, NO long training, NO CVaR, NO Stage 6 work**): wired the RL training stack onto `VertiportEnv`. **Dependencies**: installed `gymnasium 1.2.3`, `stable-baselines3 2.8.0`, `sb3-contrib 2.8.0` into `.venv`; added `gymnasium>=1.0`, `stable-baselines3>=2.8`, `sb3-contrib>=2.8` to `pyproject.toml` `[project] dependencies` (ray/rllib deliberately NOT added — SB3 covers Stage 5/6). **`src/envs/vertiport_env.py`**: `VertiportEnv` now subclasses `gymnasium.Env`. `action_space = spaces.Discrete(813)`; `observation_space = spaces.Dict` of four `Box`es — `selected_mask [813] float32 [0,1]`, `covered_zones [530] float32 [0,1]`, `remaining_budget [1] float32 [0,k_select]`, `current_coverage_ratio [1] float32 [0,1]`. The two scalar observation fields became shape-`(1,)` float32 arrays so `observation_space.contains(obs)` holds (gymnasium `Box` rejects 0-d / python-scalar entries). `reset()` now calls `super().reset(seed=seed)`; `reset`/`step`/`action_masks` signatures and the PR1 reward logic are unchanged. `from_config` is now polymorphic — accepts a yaml path or an already-parsed dict (the separate `from_config_file` was removed). **`tests/test_vertiport_env.py`**: 6 Gymnasium-conformance tests added (env is a `gym.Env`; `action_space.contains` a valid action; `observation_space.contains(obs)` after reset and after every step; `action_masks` dtype/shape; `step` returns a 5-tuple with the right element types) — the 10 PR1 tests still pass (two `float(obs[...])` assertions updated to index `[0]` for the now shape-`(1,)` fields). **`experiments/run_stage5_maskableppo_smoke.py`** (new): minimal MaskablePPO smoke — builds `VertiportEnv.from_config`, `MaskablePPO("MultiInputPolicy", env, n_steps=64, batch_size=64, gamma=1.0, learning_rate=3e-4, seed=42)`, device auto cuda-else-cpu with a one-shot CPU fallback on a CUDA `RuntimeError`; runs a pre-train masked `predict` sanity check, `learn(total_timesteps=512)`, then 3 deterministic masked evaluation episodes; writes `results/stage5/maskableppo_smoke/metrics.json` and `models/rl/maskableppo_smoke/model.zip`. **Tests**: `pytest -q tests/test_vertiport_env.py` → **16 passed**; full suite `pytest -q` → **197 passed, 2 warnings** (the 2 warnings are pre-existing in `test_posthoc_calibration.py`, unrelated). **Smoke run**: `python -m experiments.run_stage5_maskableppo_smoke --config configs/env.yaml --total-timesteps 512 --seed 42 --output-dir results/stage5/maskableppo_smoke --model-dir models/rl/maskableppo_smoke` — device **cuda**, 512 timesteps / 8 PPO iterations / 70 updates, **train wall 1.76 s**, `ep_rew_mean ≈ 0.103` during training; 3 deterministic eval episodes (scenario_idx 5 / 32 / 42) → coverage_ratio 0.1407 / 0.1418 / 0.1420, **eval mean coverage = 0.1415** (above the PR1 random-policy 0.1047). Note: the deterministic policy selects an identical candidate sequence across all 3 eval scenarios — expected, because the PR1 simplified observation carries no per-scenario demand signal (PR1 Deviation 1), so the policy is scenario-blind; coverage differs only because the OD matrix differs per scenario. **This is a smoke, not a PPO baseline** — the formal PPO baseline and CVaR-PPO are Stage 6 work. **Explicit non-actions**: no long training (capped at 512 timesteps), no CVaR, no Stage 6 work, no `ray`/`rllib`; `models/rl/maskableppo_smoke/model.zip` is NOT committed (`models/` gitignored). **Conclusion**: Stage 5 PR2 passes — `VertiportEnv` is a working `gymnasium.Env`, MaskablePPO consumes the action mask, and a minimal training+eval loop runs end-to-end. See `docs/decisions.md` 2026-05-21 "Stage 5 PR2" for the scope record.
- 2026-05-21 [stage6] PR1 (PPO training harness + 20k MaskablePPO mini run — **training harness / mini run, NOT the final paper baseline; NO CVaR, NO custom policy, NO A0-A4 baselines, NO A5/A7, NO sensitivity sweep, NO SpoNet, NO Stage 7 work**): new files `configs/ppo_vertiport.yaml`, `experiments/run_stage6_train.py`. **`configs/ppo_vertiport.yaml`**: reusable Stage-6 PPO config — `run_name=ppo_a6_bootstrap_20k_seed42`, `seed=42`, `env_config=configs/env.yaml`, `method=A6_bootstrap_expectation`, `policy=MultiInputPolicy`; `train` block (`total_timesteps=20000`, `n_steps=256`, `batch_size=256`, `gamma=1.0`, `learning_rate=3.0e-4`, `ent_coef=0.01`, `vf_coef=0.5`, `max_grad_norm=0.5`, `device=auto`); `eval` block (`eval_episodes=16`, `deterministic=true`, `eval_every=5000`); `output` block (`result_dir=results/stage6/ppo_a6_bootstrap_20k_seed42`, `model_dir=models/rl/ppo_a6_bootstrap_20k_seed42`). **`experiments/run_stage6_train.py`** (new): reusable training entrypoint — loads `configs/ppo_vertiport.yaml` (PyYAML), builds `VertiportEnv.from_config(env_config)` for train + eval, constructs `sb3_contrib.MaskablePPO` with `MultiInputPolicy` and all `train` hyperparameters, resolves `device=auto` to cuda-if-available-else-cpu with a one-shot CPU fallback on a CUDA `RuntimeError` (records `cuda_fallback`), runs a pre-train masked `predict` sanity check (asserts the predicted action is in the valid mask), `learn(total_timesteps=20000)`, then `eval_episodes=16` deterministic masked evaluation episodes. **Outputs** written to `results/stage6/ppo_a6_bootstrap_20k_seed42/`: `metrics.json` (versions, env dims, PPO hyperparams, `train_wall_s`, eval coverage summary), `selected.json` (`selected_candidates_per_episode`, `coverage_ratio_per_episode`, `scenario_idx_per_episode`, `mean/std/min/max_coverage`), `config.yaml` (frozen resolved config); plus `models/rl/ppo_a6_bootstrap_20k_seed42/model.zip` (NOT committed — `models/` gitignored). No WandB, no TensorBoard. **Tests**: `pytest -q tests/test_vertiport_env.py` → **16 passed**. **Run**: `python -m experiments.run_stage6_train --config configs/ppo_vertiport.yaml` — device **cuda** (no fallback), 20k timesteps / 79 PPO iterations / 780 updates, **train wall 39.93 s**, training `ep_rew_mean ≈ 0.35`, `explained_variance ≈ 0.97`; 16 deterministic eval episodes → **eval mean coverage = 0.361545**, std 0.002139, min 0.358145, max 0.366000 (well above the PR2 smoke 0.1415 and the PR1 random-policy 0.1047). Note: as in PR2 the policy is scenario-blind (PR1 Deviation 1 — the observation carries no per-scenario demand signal), so it selects an identical 10-candidate sequence across all eval episodes; coverage varies only by ±0.004 because the OD matrix differs per scenario. **This is a training-harness mini run, not the final paper baseline** — the full A5/A6/A7 ablation ladder, the custom `CandidateTokenExtractor` policy, CVaR, and the A0-A4 baselines are later Stage-6 work that will reuse this harness and its `metrics.json` / `selected.json` schema. **Explicit non-actions**: no CVaR, no custom policy, no A0-A4 baselines, no A5/A7 runs, no sensitivity sweep, no SpoNet, no Stage 7 work, no WandB, no TensorBoard, no new dependency; `model.zip` not committed; no `data/`, `models/`, `.claude/`, `docs/handoff/`, or `tb/` changes staged. **Conclusion**: Stage 6 PR1 passes — a reusable MaskablePPO training entrypoint runs end-to-end and the result schema is in place. See `docs/decisions.md` 2026-05-21 "Stage 6 PR1" for the scope record.
- 2026-05-21 [stage6] PR2 (demand-aware observation + 20k MaskablePPO mini run — **mini run, NOT the final paper baseline; NO CVaR, NO custom policy, NO multi-seed, NO A0-A4 baselines, NO SpoNet, NO Stage 7 work**): added a per-scenario OD demand summary to the `VertiportEnv` observation so the policy can in principle condition on the sampled scenario instead of being scenario-blind (the PR1 failure mode). **`configs/env.yaml`**: new `observation` block — `include_demand_features: true`, `demand_features: [origin_outflow, destination_inflow, total_zone_flow, covered_zone_indicator]`, `normalize_demand_features: true`. **`src/envs/vertiport_env.py`**: `VertiportEnv.__init__` gains `include_demand_features` (default False — old behaviour preserved) and `normalize_demand_features` (default True); `from_config` reads the `observation` block. When enabled, the observation gains `demand_features` — a `[n_zones, 4]` float32 `Box` (`spaces.Box(0, 1, …)` when normalized, `0..inf` otherwise). Columns: `origin_outflow[z] = OD[z,:].sum()`, `destination_inflow[z] = OD[:,z].sum()`, `total_zone_flow[z] = origin+destination`, `covered_zone_indicator[z]`. `reset()` computes a scenario-fixed `[Z,3]` base via `_compute_demand_base()` (origin/dest/total divided by `max(total_zone_flow)+1e-8` when normalized); `_get_obs()` appends the live covered-zone indicator as column 4 each step. The full `[Z,Z]` OD matrix is deliberately NOT exposed (too large) — only the per-zone summary. Reward, action-mask, and `step`/`reset` API are unchanged. **`tests/test_vertiport_env.py`**: 7 new tests (demand_features in observation_space after reset; shape `(Z,4)`; dtype float32; values in `[0,1]`; covered indicator initially 0; indicator flips to 1 after a covering step while columns 1-3 stay fixed; legacy observation intact when `include_demand_features=False`; demand_features differ across structurally-distinct scenarios). **`configs/ppo_vertiport_demand.yaml`** (new): demand-aware training config — `run_name=ppo_a6_bootstrap_demand_20k_seed42`, same hyperparameters as PR1, `env_config=configs/env.yaml`, outputs under `results/stage6/ppo_a6_bootstrap_demand_20k_seed42/` and `models/rl/ppo_a6_bootstrap_demand_20k_seed42/`. **`experiments/run_stage6_train.py`**: harness reused unchanged in structure — `metrics.json` now records the observation `keys`/`shapes` and the `include_demand_features`/`normalize_demand_features` flags; `metrics.json` and `selected.json` now record `unique_selected_sequences` and `scenario_conditioned_policy` (`unique_selected_sequences > 1`). **Tests**: `pytest -q tests/test_vertiport_env.py` → **23 passed**; full suite `pytest -q` → **204 passed, 2 warnings** (the 2 warnings are pre-existing in `test_posthoc_calibration.py`, unrelated). **Run**: `python -m experiments.run_stage6_train --config configs/ppo_vertiport_demand.yaml` — device **cuda** (no fallback), 20k timesteps / 79 PPO iterations / 780 updates, **train wall 40.83 s**; 16 deterministic eval episodes → **eval mean coverage = 0.335205**, std 0.001658, min 0.332218, max 0.338722; **`unique_selected_sequences = 1`, `scenario_conditioned_policy = False`**. **Result vs PR1**: PR1 mean coverage 0.3615 / unique sequence 1 → PR2 mean coverage 0.3352 / unique sequence 1. **The policy is still scenario-blind, and PR2 did not improve coverage.** **Root cause (verified)**: the 64 frozen bootstrap scenarios are structurally near-identical — the bootstrap day-block sampler resamples the same real OD data, so the per-zone normalized demand summary varies by only avg per-element std ≈ 0.00075 (max ≈ 0.011) on the `[0,1]` scale across all 64 scenarios. The `demand_features` channel is therefore correct and present, but it carries almost no per-scenario signal on this scenario set, so the deterministic policy's argmax is unchanged and it emits the identical 10-candidate sequence across all eval episodes. The small coverage drop (−0.026) is attributable to the extra 2120-dim flattened observation block diluting the stock `MultiInputPolicy` MLP; it is not a meaningful regression. **The demand-aware observation infrastructure is kept** — it is correct and will be needed once the scenario set carries real structural variation (diffusion-augmented scenarios) and once a structure-aware custom policy replaces the stock flattening MLP. **Still no CVaR, no custom policy, no formal baseline.** **Explicit non-actions**: no CVaR, no custom transformer policy, no multi-seed, no A0-A4 baselines, no SpoNet, no Stage 7 work, no WandB, no TensorBoard, no new dependency; `model.zip` not committed; no `data/`, `models/`, `.claude/`, `docs/handoff/`, or `tb/` changes staged. **Conclusion**: Stage 6 PR2 delivers a correct demand-aware observation and a clean negative behavioural finding — scenario-blindness on the bootstrap scenario set is bounded by the *scenarios'* lack of structural variation, not by the observation. See `docs/decisions.md` 2026-05-21 "Stage 6 PR2" for the decision record and the next-step options.
