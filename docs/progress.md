# Progress Log

This file is appended to after every completed sub-task by Claude Code.
Format: `- YYYY-MM-DD [stageN] description (any caveats)`

- 2026-05-14 [stage1] bootstrapped python env: `.venv` from system Python 3.10.12 (apt installed `python3.10-venv` + `python3-pip`); pip-installed pandas 2.3.3, pyarrow 24.0.0, numpy 2.2.6, pytest 9.0.3, pyyaml 6.0.3 (caveat: 3.10 not 3.11 — see decisions log).
- 2026-05-14 [stage1] scaffolded package: `src/{constants,data,utils}`, `tests/`, `tests/fixtures/` with `__init__.py`; filled `pyproject.toml` with deps + pytest config.
- 2026-05-14 [stage1] task 1 (load_raw): implemented `src/data/clean.py::load_raw` with explicit `RAW_DTYPES` from `src/data/schema.py`; supports chunked read (default 500k) for the 4M-row file.
- 2026-05-14 [stage1] task 2 (coord fix): `src/data/clean.py::fix_coordinates` divides Int32 coords by `COORD_SCALE` in float64 then casts float32 (avoids precision loss on 9-digit ints); drops raw `fDep*` / `fDest*` columns.
- 2026-05-14 [stage1] task 3 (time parse): `src/data/clean.py::parse_times` uses `%Y/%m/%d %H:%M:%S` (deviation from plan — see decisions log), `errors="coerce"`, drops NaT rows and returns the dropped count for audit.
- 2026-05-14 [stage1] tests: `tests/fixtures/orders_sample.csv` (30 hand-crafted rows covering bbox / time / fare / age edge cases); `tests/test_clean.py` covers haversine, load_raw, fix_coordinates, parse_times, and an end-to-end pipeline check on the fixture.
- 2026-05-14 [meta] doc sync: updated CLAUDE.md §2/§3 (stage 1 in-progress), §5 (Python 3.10 reality, Hydra deferred to PyYAML for now, new "Test scope = current task" rule); fixed `docs/plan/stage1_data_cleaning.md` schema/pitfalls (datetime `%H:%M:%S`, fWaitTime float, encoding confirmed UTF-8); wrote setup section in `README.md`.
- 2026-05-14 [stage1] task 4 (derived cols): `src/data/clean.py::add_derived_columns` adds `duration_min` (from `dest_time - dep_time`), `drive_min` (from `fDriveTime / 60`), `geo_dist_km` (haversine on float32 coords); all three float32, drops `fDriveTime`.
- 2026-05-14 [stage1] task 5 (outlier filters): `apply_outlier_filters` runs (a)-(f) in plan order, returns `(df, audit)` with row counts after each step (`initial`, `after_bbox`, `after_duration`, `after_drive_km`, `after_geo_dist`, `after_geo_drive_ratio`, `after_fare`); thresholds default to plan values, overridable via kwargs.
- 2026-05-14 [stage1] task 6 (column selection): introduced `apply_simple_renames` as a separate step between derive and filter (filters need output names like `drive_km` / `fare_yuan`); `select_output_columns` now does dropna(OUTPUT_COLUMNS) → age clip[0,127] + cast int8 → string/float32 cast → reorder per `OUTPUT_COLUMNS`. Caveat: dropna step covers residual NAs the 6 filters don't touch (NA-age, NA-gender, etc.) so the plan's "isna().sum() == 0" acceptance criterion holds; not a plan deviation, just an implementation choice.
- 2026-05-14 [stage1] tests: added `test_derived_columns_compute_correctly`, `test_outlier_filter_each_step_drops_expected_rows` (asserts the exact audit dict on the fixture: 28→26→24→22→21→20→18), `test_column_selection_dtypes_and_order` (asserts column order, all dtypes, no NaN, NA-age dropped, age=999 clipped to 127, final 17 rows). 15/15 pass.
- 2026-05-15 [stage1] EDA + bbox refinement: `experiments/run_stage1_area_bounds.py` produces `results/stage1/eda/area_bounds.csv` (per-AreaName p1/p99 of all 4 coords). Confirmed data spans 5 AreaNames = full Suzhou metropolitan area; old `SUZHOU_BBOX` (City proper) dropped 47.56% of rows. Widened to `lon[120.37, 121.33], lat[30.88, 32.01]` (union p1/p99 + 2 km padding). Pressure test: 4,257,529 → 4,050,523 rows (4.86% drop, bbox alone now 0.74%). Acceptance HI revised 4.05M → 4.15M to match reality (decisions log + plan acceptance criterion updated). 16/16 tests still pass.
- 2026-05-15 [stage1] task 7 (CLI + parquet): `experiments/run_stage1_clean.py` reads `configs/preprocess.yaml`, runs the full task-1..6 pipeline, prints per-step audit, writes `data/processed/orders_clean.parquet` with snappy + row_group=100k. Verified end-to-end on full 4.26M-row CSV: 20s wall time, 4,050,523 rows, 317.5 MB on disk, 41 row groups, 0 NA in all 16 columns, acceptance PASS.
- 2026-05-15 [stage1] task 8 (EDA): `experiments/run_stage1_eda.py` produces 5 hand-picked plots (PNG@300dpi + vector PDF) — duration before/after, drive_km hist, fare_yuan log-y hist, geo-vs-drive scatter (50k sample), hourly volume — plus `results/stage1/eda_summary.html` via ydata-profiling on a 200k sample (full 4M is too slow). End-to-end: ~33s. Added `matplotlib>=3.7` and `ydata-profiling>=4.6` to `pyproject.toml`.
- 2026-05-15 [stage1] **STAGE 1 COMPLETE (8/8)**. All plan acceptance criteria met: parquet exists at `data/processed/orders_clean.parquet` (4,050,523 rows in [3.62M, 4.15M] target window, 0 NA, all 4 coords inside metro `SUZHOU_BBOX`, dtypes match plan schema exactly); EDA HTML + 5 plots exist under `results/stage1/`; `pytest tests/test_clean.py -v` reports 16/16 green. Three plan deviations recorded in `docs/decisions.md` (datetime format `%H:%M:%S` not `%H:%M`; Python 3.10 not 3.11; `wait_min` filter (g) added; `select_output_columns` raises on NA; `SUZHOU_BBOX` widened to metropolitan area; acceptance HI revised 4.05M → 4.15M). Next: stage 2 — spatial discretization (H3 level 7, ~250 hexagons, candidate vertiport set).
- 2026-05-18 [stage2] task 1 (build_zones): `src/data/zones.py::build_zones` aggregates the union of O/D endpoints into H3 res-7 cells (endpoints deduped at 5 dp before indexing), keeps cells with >= `min_orders_per_zone` endpoints, assigns `zone_id` by lexicographic H3-index sort; writes `data/processed/zones.geojson` + `n_orders` column for the task-6 density map. `min_orders_per_zone` raised 50 -> 2000 on real data -> |Z|=530 (see docs/decisions.md 2026-05-18).
- 2026-05-18 [stage2] task 2 (pull_poi): `src/data/candidates.py::pull_poi` fetches OSM POIs via osmnx v2, classifies by source, size-filters hospital/industrial polygons, dedups to one POI per H3 res-8 cell by source priority (airport>subway>mall>hospital>industrial). Fixed plan tag bug (`subway=yes` -> `station=subway`) and raised industrial floor 50000 -> 200000 m^2 -> 1383 POIs (caveat: re-pull needs the cache deleted; see docs/decisions.md 2026-05-18).
- 2026-05-18 [stage2] task 3 (add_grid_seeds): uniform ~3 km grid inside `SUZHOU_BBOX`, dropping points outside demand zones or within 1 km of a POI -> 99 grid candidates.
- 2026-05-18 [stage2] task 4 (finalize_candidates): merges POI + grid, assigns `zone_id` by spatial join (drops 669 candidates outside demand zones), assigns `cand_id` by lexicographic (lon, lat) sort; writes `data/processed/candidates.geojson`. Final |C|=813 (1482 pre-finalize) — passes revised [600,1500] acceptance. `pytest tests/test_candidates.py` 6/6 green; `tests/test_zones.py` 4/4 green.
- 2026-05-18 [stage2] task 5 (build_matrices): `src/data/spatial.py::build_matrices` builds `dist_zone_zone` [530,530], `dist_zone_cand` [530,813], `cand_covers_zones` [813,530] via `haversine_km` over zone/candidate coordinates only (no order-level loop). Zone-zone matrix symmetrized exactly (`0.5*(D+D.T)`) with a forced-zero diagonal; coverage mask derived from the stored float32 `dist_zone_cand` so it matches a from-disk recompute. Wired into `run_stage2_build.py`: writes the three `.npy` files + `data/processed/spatial_meta.json` (n_zones, n_candidates, walk_radius_km=5.0, h3_resolution=7, source_counts, coverage_ratio, build_timestamp). `coverage_ratio=0.9962` (>= 0.85 acceptance). `tests/test_spatial.py` 6/6 green; full stage-2 suite 16/16. No plan deviations.
- 2026-05-18 [stage2] task 6 (folium maps): `experiments/run_stage2_maps.py` renders 3 interactive maps to `results/stage2/maps/` — `zones_map.html` (530 H3 zones shaded by `n_orders`, quantile-binned YlOrRd since demand is skewed), `candidates_map.html` (813 candidates as source-colored CircleMarkers + HTML legend), `coverage_map.html` (highest-coverage candidate `cand_id=254` covering 23 zones within the 5 km walk radius, covered zones highlighted, walk-radius disk drawn). Map center auto-computed from mean zone centroid; walk radius read from `spatial_meta.json`. Added a `maps.output_dir` key to `configs/spatial.yaml` (config wiring, not a plan deviation).
- 2026-05-18 [stage2] **STAGE 2 COMPLETE (6/6)**. Final results: |Z|=530 demand zones (H3 res-7), |C|=813 candidate vertiport sites, `coverage_ratio=0.9962` (>= 0.85 acceptance). Folium maps generated under `results/stage2/maps/` (zones / candidates / coverage). Tests `tests/test_zones.py` + `tests/test_candidates.py` + `tests/test_spatial.py` = 16/16 pass. Fixed `.gitignore`: `/results/` was missing despite CLAUDE.md §4 — added it and ran `git rm -r --cached results/` to untrack the 13 stage-1 files (local files kept). Next: stage 3 — OD tensor construction & eVTOL trip filtering.
- 2026-05-18 [stage3] R1 (tasks 1-2): `configs/od.yaml` (full Stage-3 source-of-truth: time bin, eVTOL thresholds, weight column, output/eda/sensitivity paths); `src/data/od.py::assign_time_bin` (`slot = floor((dep_time - t0)/bin)`, out-of-range slots are split into a separate `out_of_range` frame (plan Common Pitfalls' "clamp or drop" resolved as drop); later R1.5 revised the final OD window to T=528); `src/data/od.py::assign_zones` + `build_zone_lookup` (O/D H3 res-7 indexing, reuses zones.py's 5-dp endpoint rounding so cells match Stage 2 exactly; unknown-zone orders split into `dropped`, both functions return `(kept, dropped, stats)`). `tests/test_od.py` 5/5 green (synthetic data only, no full-parquet read). Caveat: plan Inputs calls `dist_zone_zone.npy` an input "for filter (b)", but task 4 filter (b) is the order-level `duration_min >= 25` — `dist_zone_zone` does not participate in the eVTOL filter and is unused in R1 (confirmed with user; recorded here, not a code/design change). Not done in R1: OD tensor build, `od_full.npy`, EDA.
- 2026-05-18 [stage3] R1.5 full smoke (`experiments/run_stage3_smoke.py`): **OD time window revised to 11 full calendar days** `[2023-07-10 00:00:00, 2023-07-21 00:00:00)`, **T=528** (planned 336 was wrong — data spans 12.45 days, not 7). Synced `NUM_TIME_BINS` 336→528 in `src/constants.py`, `configs/od.yaml::time` (now has `start_datetime`/`end_datetime`/`time_bin_min`/`num_time_bins`), and CLAUDE.md §7. Part A (full `dep_time`, 4,050,523 rows): only 22 orders out of window (0.0005% — the two residual-day tails) → PASS. Part B (full, all orders): zone-assignment **drop_rate = 10.19%** (412,856 dropped of 4,050,501 in-window; root cause = Stage 2's 530-zone discretization leaving 891 low-density H3 cells unmapped). Plan acceptance ≤2% → **≤12%** (decision: keep |Z|=530, do not retune Stage 2 — see docs/decisions.md 2026-05-18 ×2 and updated `docs/plan/stage3_od_construction.md`). Only 14.24% of dropped orders meet the eVTOL base condition (≥15 km & ≥25 min), so eVTOL-demand impact is limited. `pytest tests/test_od.py` 5/5 green. R1 + R1.5 complete; clear to enter R2 (tasks 3-4: build od_full + eVTOL filter).
- 2026-05-18 [stage3] R2 (tasks 3-4 core): `src/data/od.py` gains `is_evtol_eligible` (mask: `geo_dist_km>=min` & `duration_min>=min` & `o_zone!=d_zone`) and `build_od_tensor` (groupby `(slot,o_zone,d_zone)` → dense `[T,|Z|,|Z|]`; `value_col=None` count→int32, `value_col` set sum→float32; range-checks slot/zone ids, raises on out-of-range / NaN / negative; returns shape/dtype/sum/nonzero/memory stats). `experiments/run_stage3_build.py`: SMOKE (`--nrows>0`) builds tensors in memory + prints stats, writes NOTHING (a partial-data tensor must not land at `data/processed/od_*.npy`); FULL (`--nrows 0`) writes the 3 `.npy` + `od_meta.json` after acceptance checks pass. `tests/test_od.py` 11/11 green (7 new: eVTOL filter dist/duration/intra-zone, count + weighted aggregation, zero-diagonal end-to-end, out-of-range raise). 200k-row smoke: drop_rate 10.16%, evtol_trip_count 9582, **evtol_share 5.33%** (in [0.03,0.20] → PASS), tensors `[528,530,530]` 593 MB each (~1.78 GB total), od_evtol nonzero_ratio 0.0061%, diagonal 0, no NaN/negative — all acceptance checks PASS, 1.2s. Caveat: evtol_share 5.33% sits at the low end of the plan's expected 5–15%; the 200k rows are a non-random head sample (R1.5 found this slice is hour-skewed), so a full run is needed to confirm the real share. Not done: full build, EDA, sensitivity. No design deviation.
- 2026-05-18 [stage3] R2 full build (`python -m experiments.run_stage3_build`, FULL mode): built and wrote the three OD tensors + meta to `data/processed/`. n_orders_total 4,050,523 → n_time_in_range 4,050,501 → n_zone_assigned 3,637,645 (zone drop_rate 10.19%). evtol_trip_count 188,699, **evtol_share 5.19%** (in [0.03,0.20] → PASS). od_full `[528,530,530]` int32 sum 3,637,645 nonzero 1,837,574 (1.24%); od_evtol int32 sum 188,699 nonzero 173,264 (0.117%); od_evtol_weighted float32 sum 14,915,827 (fare_yuan) nonzero 173,264 (0.117%). Each `.npy` = 593.26 MB on disk (~1.78 GB total, all gitignored under `data/processed/`); `od_meta.json` written (T=528, n_zones=530, time_bin_min=30, window 2023-07-10..2023-07-21, evtol params 15 km / 25 min, weight=fare_yuan, share_of_evtol_trips=0.0519). od_evtol diagonal = 0, no NaN, no negative — all acceptance checks PASS, 11.3 s wall. `pytest tests/test_od.py` 11/11 green. **R2 (Stage-3 tasks 3-6) complete.** Next: R3 — share-by-hour stats (task 7) + EDA figures (task 8) and/or the sensitivity sweep (task 9).
- 2026-05-18 [stage3] R3 (task 9): `experiments/run_stage3_sensitivity.py` sweeps `EVTOL_MIN_DIST_KM ∈ {10,12,15,18,20}` km (duration fixed 25 min, 11-day window, 530 zones; reuses `assign_time_bin`/`assign_zones`/`is_evtol_eligible`, rebuilds no `.npy`). `eligible_share_of_zone_assigned`: 10 km **10.04%**, 12 km **7.98%**, 15 km **5.19%**, 18 km **3.36%**, 20 km **2.47%**. A higher threshold yields fewer eligible trips and fewer nonzero OD pairs (66,045 → 34,116) but higher `normalized_od_entropy` (0.902 → 0.930) — short-haul concentration is filtered out, leaving a more spread long-haul OD distribution. 15 km consistency check: sweep `eligible/zone_assigned` = 0.051874 == `od_meta.json` `share_of_evtol_trips` 0.051874 (abs diff 0.000000) → PASS, identical scope to the R2 full build. **15 km kept as the provisional baseline** (NOT proven optimal) — its 5.19% share is inside the [0.03,0.20] acceptance window and consistent with the already-built `od_evtol.npy`/`od_meta.json`. **12 km (7.98%) is the designated fallback**: if Stage-4/5/6 underperforms due to eVTOL OD sparsity, regenerate the tensors at 12 km. The `15 km / 25 min` cut is a threshold-based proxy treated as a sensitivity-tested assumption, not a settled standard — see docs/decisions.md 2026-05-18. Table written to `results/stage3/sensitivity.csv` (5 rows; `results/` is gitignored). No design deviation. **R3 (task 9) complete.** Remaining Stage-3 work: share-by-hour stats (task 7) + EDA figures (task 8).
- 2026-05-19 [stage4] 4A (dataset/normalization/padding smoke): updated `docs/plan/stage4_diffusion.md` to the Stage-3 reality (T 336→528, |Z| ≈250→530, `od_samples.npy` `[64,528,530,530]`, padding to a multiple of 16 not next-pow2, `base_channels` smoke 16/32 not 64, staged 4A/4B execution, new `od_samples_agg.npy` `[N_ω,530,530]` int32 output so Stage 5 never loads the ~19 GB 4-D array). New `configs/diffusion.yaml` (input/data/model/diffusion/train/sample sections; data section is the 4A source of truth, the rest are smoke defaults). New `src/data/od_dataset.py` — `ODDataset` (mmap load of `od_evtol.npy`, log1p+global-scalar-standardize+clip+scale norm, zero-pad |Z|→`pad_size`, `(hour,day_of_week,is_weekend)` condition, `inverse_transform`, JSON norm-stats save/load); pure NumPy, no torch (works as a torch Dataset by duck typing in 4B). `tests/test_od_dataset.py` 19/19 green (pad/unpad + inverse round-trips, split lengths, condition correctness, no-NaN/in-range). Smoke (`experiments/run_stage4_dataset_smoke.py` on the real tensor): T=528, |Z|=530, **pad_size=544** (530→544, padded sample `[1,544,544]` 1.18 MB), splits train/val/test = 432/48/48 samples, nonzero_ratio 0.1168%. **FINDING** (see docs/decisions.md 2026-05-19): the plan's `log1p`+standardize+clip[-3,3] norm degenerates on the 0.117%-sparse eVTOL tensor — train stats `mu=0.00104`, `sigma=0.02787`, so every nonzero entry saturates to +1.0 and the normalized tensor is effectively binary (inverse-transform `max|err|≈3.9`). Stage 4A plumbing is correct; the normalization scheme needs a Stage-4B decision (raise/drop clip, nonzero-only standardize, or switch to the 12 km denser fallback) — flagged for the user, not auto-changed. Three plan-assumption changes recorded in docs/decisions.md (pad-to-16, JSON norm stats, 9/1/1-day split).
- 2026-05-18 [stage3] R4 (tasks 7-8): `src/data/eda_plots.py` (house figure style + PNG/PDF save + zone-choropleth helper; pure matplotlib, no seaborn package — `seaborn-v0_8-whitegrid` style ships with matplotlib) and `experiments/run_stage3_eda.py`. Task 7: share_total = 5.19% (== `od_meta.json` share_of_evtol_trips); `results/stage3/eda/share_by_hour.csv` written (24 rows: full/evtol counts + all/weekday/weekend share); `od_meta.json` gains `share_by_hour` / `share_by_hour_weekday` / `share_by_hour_weekend` keys (existing fields preserved, still valid JSON). Task 8: 7 figures × (PNG 300 dpi + PDF) = 14 files in `results/stage3/eda/` — od_share_by_hour, top20_od_pairs, heatmap_o_volume, heatmap_d_volume, temporal_pattern, distance_distribution, fare_distribution. Key observations: eVTOL share_by_hour spans ~3.4%–9.9%, peaks at 05:00 (weekday 9.9% — pre-dawn long-haul / airport trips), bottoms at 00:00–03:00 (~3.4%), steady ~6% across 12:00–16:00; weekday share runs slightly above weekend. The `15 km / 25 min` eVTOL cut stays a **provisional baseline** — figure titles label it so, distance_distribution marks the 15 km cut-off — see docs/decisions.md 2026-05-18. `results/` is gitignored. No design deviation. **R4 (Stage-3 tasks 7-8) complete — Stage 3 tasks 1-9 all done.**
- 2026-05-20 [stage4] 4B-0 (normalization ablation): `experiments/run_stage4_norm_ablation.py` compares 5 normalization schemes on the train split (slots 0..431, 9 days) without modifying `ODDataset`. Schemes — **A** `current_global_clip3` (log1p + global mu/sigma + clip=3 + /clip → [-1,1]); **B** `global_clip30`; **C** `global_clip100`; **D** `nonzero_global_clip3` (mu/sigma over nonzero log1p only, clip=3, zeros pinned to -1.0); **E** `log1p_minmax_p999_nonzero` (linear scale via p99.9 of nonzero log1p as vmax). Stats: global `mu=0.001038`, `sigma=0.027869`; nonzero `mu_nz=0.7272`, `sigma_nz=0.1260` (173,264 nonzero train cells); all-cells log1p p99.9 ≈ 0.693 (degenerate, ~0); nonzero log1p p99.9 = 1.6094. **Result**: only **C** distinguishes all five probe counts {1, 2, 5, 10, 20} — A saturates everything to +1, B saturates from count=2, D saturates from count=5, E saturates from count=5 (p99.9 of nonzero log1p ≈ log1p(4) so the linear scale clips at count ≈ 4). C maps {1, 2, 5, 10, 20} → {0.248, 0.394, 0.643, 0.860, 1.000}, min adjacent gap 0.140; 0% real-train saturation (pct_sat=0.00%); mean inverse-transform error ≈ 0 on train. Caveat: scheme C uses only the upper half of [-1, 1] (zeros at -0.0004, nonzeros at 0.25..1.0) because global mu is tiny on this sparse data — the zero/nonzero gap is ~0.25 rather than ~2. **Proposed for Stage 4B model smoke**: `clip_val=100` (scheme C) as the primary baseline (see `docs/decisions.md` 2026-05-20). Table written to `results/stage4/norm_ablation.csv` (5 rows, gitignored). `ODDataset` defaults unchanged; the 4A `clip_val=3.0` config value will need to be updated to 100 if/when the user confirms. No new deps.
- 2026-05-20 [stage4] 4B-0 (clip100 adoption): `configs/diffusion.yaml::data.clip_val` flipped `3.0 → 100.0` per the 4B-0 ablation (decisions.md 2026-05-20 promoted from "proposed" to "accepted for Stage 4B model smoke"). Stale `data/processed/od_norm_stats.json` (computed at clip=3) deleted; re-cached at clip=100 by the next smoke run. **Re-runs**: (a) `experiments/run_stage4_dataset_smoke.py` → norm stats `mu=0.001038, sigma=0.027869, clip_val=100.0`, train[0] normalized range `[-0.0004, 0.3938]` (was saturated at +1 under clip=3), **inverse round-trip on train sample 10 `max|err| = 0.0000`** (was ~3.9). Splits 432/48/48 unchanged. (b) `pytest tests/test_od_dataset.py -v` → **19/19 PASS** (no test depends on the config; `ODDataset.__init__` keeps `clip_val=3.0` as its in-code default, used by tests via the synthetic fixture). (c) `experiments/run_stage4_norm_ablation.py` re-run → scheme C still `primary`, identical numbers as before (deterministic given the same train tensor). `ODDataset` defaults unchanged. No new deps.
- 2026-05-20 [stage4] 4B-1 prep: PyTorch CUDA environment installed and verified ahead of writing any 4B-1 model code. WSL2 sees the RTX 5070 Ti via the Windows-side driver 591.86 (CUDA 13.1 host); installed `torch-2.11.0+cu128` from the cu128 wheel index (`pip install torch --index-url https://download.pytorch.org/whl/cu128`) — needed because RTX 50-series is Blackwell sm_120, which only the cu128 toolchain ships kernels for. Verified `torch.cuda.is_available()=True`, `get_device_capability(0)=(12,0)`, 1024×1024 GPU matmul + sync OK. Added `torch>=2.11` to `pyproject.toml` (no `torchvision` / `torchaudio` — Stage 4 does not need them). Pulled triton 3.6.0 and the nvidia-cu128 runtime wheels (~1.7 GB total disk). See `docs/decisions.md` 2026-05-20 (cu128 wheel rationale). No 4B-1 model code written yet — environment-only step.
- 2026-05-20 [stage4] 4B-1 (small U-Net + DDPM forward/sample smoke): added `src/models/__init__.py`, `src/models/unet_od.py` (sinusoidal time emb + `ConditionEmbedding` for hour/dow/is_weekend + `ResBlock` (GroupNorm + 2× Conv3×3 + FiLM-style emb bias) + `UNetOD` (input_conv → 2 down-blocks → bottom → 2 up-blocks with skip-concat → out_conv)), `src/models/diffusion.py` (`GaussianDiffusion` Module: cosine/linear betas, `add_noise`, `training_loss` epsilon-MSE, `ddim_sample` deterministic DDIM with `clip_sample` on by default — see `docs/decisions.md` 2026-05-20). Added `einops>=0.8` to `pyproject.toml` (small; the U-Net does not currently import it but it's the standard companion lib for the diffusion code path and is reserved for the production U-Net). **Tests** `tests/test_diffusion.py` 15/15 green on tiny [2,1,32,32] fake tensors: sinusoidal+condition embed shapes, ResBlock residual, U-Net forward/backward, cosine/linear schedule shapes + monotonicity, `add_noise` at t=0 is identity-like, training loss finite + backward, DDIM sample shape no-NaN, bad inference-step rejection, `.to(device)` moves buffers. **Smoke** `experiments/run_stage4_model_smoke.py` against the real `od_evtol.npy` train[0] on GPU (RTX 5070 Ti, sm_120): model 304,137 params (1.22 MB float32); DDPM `num_train_timesteps=100`, cosine betas, `alpha_cumprod_T=2.4e-7`; 3 AdamW steps lr=2e-4 → loss 1.118 → 0.952 → 0.870, grad norms 3.57/2.96/2.94 (step 0 wall=734 ms cold, step 1-2 wall=16 ms each); DDIM 5-step sample wall=67 ms, range `[-1.029, 1.024]` (clip_sample working — uncapped values blew to ±10k); inverse transform `unpad → expm1` → shape `[1, 530, 530]` float64, nonneg + finite, min=0, max=16.35 counts, mean=3.81 counts. **GPU peak** 638.6 MB allocated / 843.1 MB reserved — comfortable under the 16 GB budget. NO checkpoint saved, NO `models/` write, NO `data/synthetic/` write, NO `od_samples.npy`. Stage 4C (real training) still gated on user go-ahead.
- 2026-05-20 [stage4] 4B-2 (training-loop smoke): added `src/models/ema.py` (`EMA(nn.Module)` with `update` / `copy_to` / `store` / `restore`; shadows live as `register_buffer`s so `.to(device)` moves them and `state_dict` rides along with checkpoints — parameter names with `.` are mapped to `_` for buffer naming, rebuilt at construction from the model), `tests/test_ema.py` (10/10 pass: buffer count = trainable params, init buffers equal model params, decay correctness on the `0.9*init + 0.1*0` math, copy_to / store / restore, state_dict round-trip, `.to()` moves buffers, invalid-decay + restore-before-store rejections), and `tests/test_diffusion.py::test_checkpoint_save_load_roundtrip` (full `{model, ema, optim, step, val_loss}` save+reload via `weights_only=True`, forward outputs bit-equal after reload). New `experiments/run_stage4_train_smoke.py` exercises the full training scaffold on the real `od_evtol.npy`: DataLoader (wrapped `_TorchODDataset`, `batch_size=1`, `num_workers=0`) → `torch.amp.autocast(device_type="cuda", dtype=bfloat16)` → DDPM training loss → backward → `clip_grad_norm_(1.0)` → AdamW step → `EMA.update` → every `eval_every=10` swap EMA weights in via `store`/`copy_to`/`restore` and eval over the 48-sample val split with a fixed-seed `t` generator → every `save_every=10` write `last.pt` + (if val improved) `best.pt`. Outputs land under `models/diffusion_od_smoke/`: `last.pt`, `best.pt`, `config.yaml`, `train_log.jsonl` (`models/` is gitignored — nothing reaches commits). bf16 chosen over fp16+GradScaler because Blackwell sm_120 supports bf16 natively and skipping the scaler keeps the loop simpler. **Smoke run** on RTX 5070 Ti, `max_steps=20`, `num_train_timesteps=100`, `ema_decay=0.99`, seed=42: model 304,137 params; step-1 wall 1307 ms cold (CUDA + DataLoader spin-up), steps 2-20 wall 14-18 ms each; train loss `1.043 → 0.307` (descending cleanly), grad norms `3.31 → 1.48` (no spikes, no clipping required), val_loss_ema `0.990 (step 10) → 0.914 (step 20)`, all finite. Checkpoint files: `best.pt` 4.965 MB, `last.pt` 4.965 MB, `config.yaml` 0.001 MB, `train_log.jsonl` 0.003 MB. **Best.pt reload check**: load fresh `UNetOD` + `EMA`, `load_state_dict` for both, `ema.copy_to(model)`, re-evaluate on val with the same fixed-seed `t` → saved 0.914288, reloaded 0.914612, `|diff| = 3.24e-4` (within the 1e-3 tolerance — bf16 autocast is not bit-deterministic across runs but the gap is well under one part in 2000). GPU peak: **532.3 MB allocated / 755.0 MB reserved** on a 16 GB card. NO real training, NO `od_samples.npy`, NO sampling, NO Stage-5 entry. **Tests**: `pytest tests/test_diffusion.py tests/test_ema.py -v` → 26/26 PASS. Plan deviation: none structural; the `bf16 + no GradScaler` choice is a routine AMP-dtype call and is mentioned here but not promoted to `docs/decisions.md`.
- 2026-05-20 [stage4] 4B-3 PR-1 (training entry point + CFG + metrics, NO real training): four confirmed decisions (1) bf16 / no GradScaler, (2) TensorBoard logging, (3) pilot uses `base=32` `mults=(1,2,4)` `bs=4`, (4) full run 12-hr budget at 20k steps; see plan emitted in chat. Added classifier-free guidance backward-compatibly: `src/models/unet_od.py::UNetOD.forward` now accepts an optional `cond_drop_mask: torch.Tensor | None` (per-sample bool; `True` zeros the conditional embedding); `src/models/diffusion.py::training_loss` accepts `cond_dropout_prob: float = 0.0` (default unchanged → smoke bit-equal), and `ddim_sample` accepts `guidance_scale: float = 1.0` with an explicit `scale == 1.0` branch that takes the original single-forward path verbatim and a `scale > 1.0` branch that does cond + uncond forwards and combines as `eps_uncond + scale * (eps_cond - eps_uncond)`. New `src/utils/metrics_dist.py` (`nonzero_ratio`, `row_sums`, `col_sums`, `ks_stat_1d`, `marginal_compare`) — pure-NumPy 1-D K-S statistic and marginal stat dictionary, no scipy. New `tests/test_metrics_dist.py` (13 tests: KS identical → 0, disjoint → 1, separated → high, close → low, empty rejected; `marginal_compare` real-vs-self → 0, all-zero gen distinguishable, returns flat floats, low-dim rejected, unequal-N allowed). Extended `tests/test_diffusion.py` with 5 CFG tests (cond_drop_mask changes output, per-sample mask is per-sample, full cond_dropout=1.0 still finite + backward, guided sample shape + finite, **`guidance_scale=1.0` bit-equal to no-arg via `torch.equal`**). New `experiments/run_stage4_train.py`: profile table (`pilot`/`medium`/`full` with the four agreed configs), `--profile` required, `--resume <ckpt.pt>` optional, TensorBoard writer at `results/stage4/train_<profile>/tb/`, linear LR warmup, per-step / per-eval / per-save event logging to `train_log.jsonl`, in-loop sample diagnostics via `_sample_diag` (EMA-swap → chunked DDIM with `guidance_scale=2.0` → `inverse_transform` → `marginal_compare` vs `_collect_real_val_counts(val_base)`), end-of-run plots (`loss_curve.png`, `sample_grid.png`, `marginal_match.png`) and `metrics.json` under `results/stage4/train_<profile>/`, checkpoints under `models/diffusion_od_<profile>/` (gitignored). Pilot profile diag fires once at the final step (`diag_at_end_only=True`); medium / full diag every save. `configs/diffusion.yaml`: comment-only block at top describing the three profiles and stating that the YAML values remain SMOKE defaults (`run_stage4_train_smoke.py` / `run_stage4_model_smoke.py` still rely on them). `pytest tests/test_diffusion.py tests/test_ema.py tests/test_metrics_dist.py -v` → **44/44 PASS** (16 + 10 diffusion+ema → 21+10+13). 4B-2 smoke regression `python -m experiments.run_stage4_train_smoke` → reproduces previous numbers exactly: train_loss range `[0.3074, 1.0429]`, val_loss 0.9143, reload diff 3.24e-4, GPU peak 532.3 MB — CFG path is bit-equivalent when `cond_dropout_prob=0` and `guidance_scale=1`. **NO real training launched**, no `od_samples.npy`, no Stage-4C entry. CFG implementation is a textbook Ho-Salimans-2022 plumbing addition with no plan deviation; `docs/decisions.md` unchanged. Ready for Stage 4B-3 PR-2 (pilot run) on user go-ahead.
- 2026-05-20 [stage4] 4B-3 PR-2 attempt 1 (pilot under OLD 9/1/1 split — **invalidated**): pilot run launched `python -m experiments.run_stage4_train --profile pilot`, 1000 steps, 98s wall, train_loss `1.34→0.002`, val_loss_ema `0.77→0.024`, best.pt saved at step 1000. End-of-run `_sample_diag` reported `real_nonzero_ratio=1.0`, `gen_nonzero_ratio=23.14%`, `row_sum_ks_stat=1.0`, `col_sum_ks_stat=1.0`. Both numbers looked wrong; investigation followed.
- 2026-05-20 [stage4] 4B-3 PR-2 diagnostics: built `experiments/run_stage4_diag_posthoc.py` (load `models/diffusion_od_pilot/best.pt`, EMA weights, DDIM N=16 sample with `guidance_scale=2.0` / 50 inference steps, inverse_transform, dual nonzero ratio + quantile + per-sample stats). Result: continuous-float `nonzero_ratio=25.32%` (matches in-loop figure), rounded-int-count `nonzero_ratio=0.0754%`, value_counts `{0:4 491 009, 1:3 383, 2:8}`, gen_max=2. Root-cause analysis identified **two** independent issues: (i) `_collect_real_val_counts` round-trips through float32, so `inverse_transform` of an integer 0 returns ~1e-9 and `np.count_nonzero` reads 100% of cells as nonzero, masking the real sparsity; (ii) the in-loop ratio compares **continuous** gen floats to the noisy "real" reference, not rounded integer counts. **AND** per-day raw OD inspection (`data/processed/od_evtol.npy`) showed days 0–6 carry essentially all signal (22–27k nonzeros/day), day 7 has only 40 nonzeros, days 8–10 are **identically zero** — the old `train_days [0,9]/val_days [9,10]/test_days [10,11]` split had train day 8 = 0 contribution, val + test = 0 ground truth, so val_loss decay was meaningless (model just denoising back to the zero slice). Documented in `docs/decisions.md` 2026-05-20 ("Stage 4 split revision" + "Stage 4B-3 metric口径"). **Pilot run is invalidated as acceptance evidence.**
- 2026-05-20 [stage4] 4B-3 PR-2 fixes (user-approved, no retrain yet): (a) `configs/diffusion.yaml::data.split` now `train_days [0,5] / val_days [5,6] / test_days [6,7]` — Mon–Fri train, Sat val, Sun test; day 7 dropped (40-nonzero tail), days 8–10 excluded. Deleted stale `data/processed/od_norm_stats.json`. `python -m experiments.run_stage4_dataset_smoke` re-run on new split: lengths train=240 / val=48 / test=48, raw nonzero ratio train **0.1897%** (127 885), val **0.1729%** (23 313), test **0.1634%** (22 026), train sum 139 116 / val 25 365 / test 24 177, train inverse round-trip `max|err|=0.0000`, all splits non-empty. New norm stats `mu=0.001378, sigma=0.032088, clip_val=100.0` (was `mu=0.001038, sigma=0.027869` under the 9-day train). Caveat: val/test are both weekend now (was both weekday); cross-pattern generalization is not measured under the new split — recorded in decisions.md, not gated. (b) `experiments/run_stage4_train.py::_collect_real_val_counts` rewritten to read raw int64 OD slices directly from the memmap (no normalization round-trip). (c) `_sample_diag` now rounds gen counts to int64 before calling `marginal_compare`; acceptance keys (`gen_nonzero_ratio`, row/col mean/std, `row_sum_ks_stat`, `col_sum_ks_stat`, `gen_min/max/mean`) are integer-domain; continuous-side stats moved under `gen_cont_nonzero_ratio` / `gen_cont_min/max/mean`. `_plot_marginal_match` consumes rounded counts; `_plot_sample_grid` keeps continuous (log1p heatmap smoother that way). (d) Renamed `results/stage4/train_pilot/` → `results/stage4/train_pilot_invalid_old_split_debug/` so the directory cannot be mistaken for a Stage 4B-3 acceptance pilot; the same is true for `models/diffusion_od_pilot/` → `models/diffusion_od_pilot_invalid_old_split_debug/`. No commit. `pytest tests/test_metrics_dist.py tests/test_diffusion.py tests/test_ema.py` still green (the口径 fix is on the caller side; `marginal_compare` itself is unchanged). NO retrain launched; a fresh pilot on the new split is gated on user sign-off.
- 2026-05-20 [stage4] 4B-3 PR2b (fixed-split pilot run): `python -m experiments.run_stage4_train --profile pilot` on the PR2a split `train_days [0,5] / val_days [5,6] / test_days [6,7]`, 1000 steps, 105.3 s wall, RTX 5070 Ti / bf16 / seed 42, 920k params. Train loss `1.3434 → 0.0028` (smooth decrease then noisy plateau ~0.002–0.010, no NaN). val_loss_ema strictly decreasing every eval: `0.7775 → 0.3453 → 0.1367 → 0.0556 → 0.0280` (best == final == 0.0280). End-of-run `_sample_diag` (4 EMA-DDIM samples, `guidance_scale=2.0`, 50 inference steps) under the PR2a口径 fix: **`gen_nonzero_ratio = 0.0535%`** (rounded-int basis) vs real val `0.1729%` (≈3.2× sparser than ground truth); `gen_cont_nonzero_ratio = 12.17%`; gen rounded `min=0, max=3, mean=5.40e-4`; `row_sum_ks_stat = 0.211`, `col_sum_ks_stat = 0.345` (col further off — gen_col_sum_std 5.13 vs real 2.74, heavy single-column tail). GPU peak allocated **5428.8 MB** / reserved 5809.1 MB. Outputs landed at `results/stage4/train_pilot/` (`loss_curve.png`, `marginal_match.png`, `metrics.json`, `sample_grid.png`, `tb/`, `train_log.jsonl`) and `models/diffusion_od_pilot/` (`best.pt` = `last.pt` = 14.819 MB, both saved at step 1000; `config.yaml`, `profile.yaml`, `train_log.jsonl`; gitignored). No `od_samples.npy`, no medium/full launched. **Conclusion**: pipeline and PR2a fixed diagnostics (split + rounded-count口径 + raw-int real reference) pass end-to-end on the new split and produce non-negative integer counts with no NaN; generated OD is still sparser than real val at this step budget — consistent with 1000-step under-fitting / mode-bias toward zero on a 0.17%-sparse target (`docs/plan/stage4_diffusion.md` §Common Pitfalls). This run is a **pilot baseline**, not Stage 4 final model acceptance.
- 2026-05-20 [stage4] 4B-3 PR3a (medium architecture canary + CLI overrides): added three debug-only CLI flags to `experiments/run_stage4_train.py` — `--eval_every`, `--save_every`, `--output_suffix` (`--max_steps` was already present from PR1). `--output_suffix` (string, default `""`, leading underscore included by the caller) is appended to both `run_dir` and `out_dir` basenames so a debug run cannot clobber the canonical profile dirs (`models/diffusion_od_medium/` etc.). Smoke-checked via `python -m experiments.run_stage4_train --help`. Canary run: `python -m experiments.run_stage4_train --profile medium --max_steps 20 --eval_every 10 --save_every 20 --output_suffix _canary`, 53.1 s wall, RTX 5070 Ti / bf16 / seed 42. Model: **13,340,281 params** (53.36 MB, vs pilot 920k); architecture `base=64, mults=(1,2,4,8), batch=2`, profile warmup_steps=500 so LR ramps `4e-7 → 8e-6` over the 20-step canary (target 2e-4). **No OOM**: GPU peak allocated **7455.7 MB** / reserved 7780.4 MB on the 16 GB card. train_loss `1.1717 → 0.9073` (modest decrease, LR still in warmup ramp). val_loss_ema essentially flat: `1.1440 (step 10) → 1.1418 (step 20)` — EMA buffer still ~98% random init at decay=0.999. Backward normal (grad_norm 5.0–8.3, finite). EMA `update()` every step + EMA-swap eval at steps 10/20 both ran. Checkpoint plumbing OK: `best.pt` and `last.pt` (213.6 MB each, 14× pilot) saved at step 20 under `models/diffusion_od_medium_canary/` (gitignored). In-loop `_sample_diag` (16 samples, DDIM 50 steps, cfg=2.0) executed without OOM and reported `nonzero_ratio=42.62%`, `row_ks=1.000`, `col_ks=1.000` — **garbage as expected** (model effectively untrained at 20 warmup-only steps); not acceptance evidence, only plumbing-runs-without-crashing evidence. Step time ~125–140 ms steady-state (first step 1052 ms = JIT/cudnn warmup), projecting **~11 min training + ~4 min for 5 in-loop sample_diags = ~15 min total** for the full medium 5000-step run (well under the file header's "~1-2 hr" budget). Artifacts at `results/stage4/train_medium_canary/` (`loss_curve.png`, `marginal_match.png`, `metrics.json`, `sample_grid.png`, `tb/`, `train_log.jsonl`). Canonical `models/diffusion_od_medium/` was NOT touched. **Conclusion**: memory and training/eval/save/EMA/sample_diag plumbing all pass on the medium architecture; safe to proceed to medium 5000 steps. Reminder: drop `--eval_every` / `--save_every` overrides for the full medium run so the profile defaults (`eval_every=500`, `save_every=1000`) apply. No `od_samples.npy`, no Stage-4C entry, no full medium launched.
- 2026-05-20 [stage4] 4B-3 PR3b (15 km medium 5000-step run — **failed diagnostic, not acceptance**): `python -m experiments.run_stage4_train --profile medium` on the PR2a fixed split with the 15 km eVTOL OD tensor, 5000 steps, **916.1 s wall (15.3 min)** (matches PR3a canary projection ~15 min), 13.34 M params (base=64, mults=(1,2,4,8), batch=2), seed 42, bf16. Average wall-per-step 183 ms (incl. 10 evals + 5 in-loop sample_diags); pure step time ~123 ms steady-state. GPU peak allocated **7455.7 MB** / reserved 7780.4 MB (identical to canary). Train loss `1.1717 → 1.9e-4` (smooth monotone decrease across all milestones: step 50 = 0.321, 500 = 0.054, 1000 = 0.009, 2500 = 0.0013, 5000 = 0.00019). val_loss_ema strictly decreasing every eval over 10 evals: `0.2861 → 0.0390 → 0.00904 → 0.00547 → 0.00421 → 0.00363 → 0.00312 → 0.00308 → 0.00296 → 0.00213` (final == best == step 5000 = 0.00213); `best.pt` was overwritten 5× because val_loss kept dropping. **In-loop sample_diag (every 1000 steps, EMA-DDIM 16 samples, cfg=2.0, rounded-int口径) tells a completely different story** — generated OD became progressively **denser**, not sparser, as training progressed: gen_nonzero_ratio `2.78% (step 1000) → 28.72% (2000) → 87.39% (3000) → 78.64% (4000) → 12.68% (5000)`; row_ks `0.962 → 1.000 → 0.876 → 0.853 → 0.290`; col_ks `0.965 → 1.000 → 0.875 → 0.837 → 0.242`. Final step-5000 numbers vs real val (0.1729 %, std mean 0.997, std ≈ 2.07 row / 2.74 col): **`gen_nonzero_ratio = 12.68%` ≈ 73× over-dense vs real 0.173%**; `gen_cont_nonzero_ratio = 99.98%` (essentially every cell carries continuous mass > 0); gen rounded `min=0, max=6, mean=0.127`; gen continuous `min=0, max=6.22, mean=0.337`; **`row_sum_ks_stat = 0.2905`** (vs pilot 0.211 — slightly worse); **`col_sum_ks_stat = 0.2416`** (vs pilot 0.345 — better); `gen_row_sum_mean = gen_col_sum_mean = 67.3` (vs real 0.997 — ~67× too much mass per row/col); gen_row_sum_std 172.8, gen_col_sum_std 173.1 (vs real 2.07 / 2.74). **Two red flags**: (i) val noise-MSE is misaligned with sample quality on this 0.17 %-sparse target (model "improves" by 130× on val while sample density swings 30× the wrong way); (ii) `n_samples_diag = 16` gives a noisy diag — the 7× swing from step 4000 (78.6 %) to step 5000 (12.68 %) over essentially-flat val_loss (0.00308 → 0.00213) reflects either real sample-distribution shift between save points or large estimator variance. This is the exact "mode collapse to dense" pitfall flagged in `docs/plan/stage4_diffusion.md` §Common Pitfalls (the dual of "mode collapse to zeros"), and is consistent with the `docs/decisions.md` 2026-05-20 §Stage-4 normalization note that scheme C's zero/nonzero gap is only ~0.25 — noise-MSE rewards predictions in the same flat region whether they emit zeros or small dense floats. **Outputs archived (renamed to prevent overwrite when re-running)**: `results/stage4/train_medium/` → `results/stage4/train_medium_15km_failed_dense_debug/` (`loss_curve.png`, `marginal_match.png`, `metrics.json`, `sample_grid.png`, `tb/`, `train_log.jsonl`); `models/diffusion_od_medium/` → `models/diffusion_od_medium_15km_failed_dense_debug/` (gitignored; `best.pt`=`last.pt`=213.6 MB at step 5000, `config.yaml`, `profile.yaml`, `train_log.jsonl`). **Conclusion**: the 15 km medium run is **not** Stage-4B acceptance evidence — it is a failed diagnostic that confirms the val-MSE / sample-quality misalignment on the 15 km eVTOL OD's 0.117 %-sparse target. **Next step decision**: switch to the **12 km eVTOL fallback** (7.98 % share, ~3× denser; designated fallback recorded in `docs/decisions.md` 2026-05-18). No commit of `models/`, no `od_samples.npy`, no Stage-4C entry, no further training launched.
