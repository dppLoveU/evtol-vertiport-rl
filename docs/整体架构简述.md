阶段 0：方案核心思想
整个项目有两层空间表示，必须先想清楚：

需求分区（Demand Zones, Z）：用于聚合 OD 需求的相对粗的单元，数量级在 100–300，例如苏州的街道级 TAZ 或 H3 level 7 六边形。OD 矩阵的大小是 ∣Z∣×∣Z∣|Z| \times |Z|
∣Z∣×∣Z∣
候选 Vertiport 位置（Candidate Sites, C）：相对细的可选点位，数量级 200–500，可以来自 POI（地铁站、商业综合体、产业园）或地块筛选

Coverage 的关系：一对 OD 需求 (o,d)∈Z×Z(o, d) \in Z \times Z
(o,d)∈Z×Z 被"覆盖"，当且仅当 \exists v_1, v_2 \in $ 已选 vertiports 使得 $\text{dist}(o, v_1) \leq R_{\text{walk}}
 且 dist(d,v2)≤Rwalk\text{dist}(d, v_2) \leq R_{\text{walk}}
dist(d,v2​)≤Rwalk​。这是双端覆盖（bilateral coverage），是 hub location 的标准定义，比单端覆盖严格，也是你比 baseline 更扎实的细节。

阶段 1：原始数据清洗与统一
输入：原始 CSV，4M 条订单
输出：清洗后的标准化订单表
主要操作：

坐标修正：fDepLongitude / fDepLatitude / fDestLongitude / fDestLatitude 是整数表示（例如 120557806），除以 10610^6
106 得到十进制度
时间解析：fDepTime / fDestTime 转成 datetime，计算 trip_duration_min
字段筛选：保留 OD 经纬度、时间、行驶里程 fDriveMile、票价 fFactPrice、AreaName，其余删掉减小内存
异常过滤：

经纬度落在苏州 bounding box 外的丢弃
行程时间 < 2 min 或 > 180 min 丢弃
里程 < 0.5 km 或 > 100 km 丢弃
起终点距离与 fDriveMile 偏差过大的丢弃（绕路或 GPS 错误）


采样审计：用 pandas.describe() 看清洗后字段分布，必要时画几张直方图存到 results/eda/

预期产出：data/processed/orders_clean.parquet，约 3.5–3.8M 条（清洗损耗 ~5–10%）

阶段 2：空间离散化
输入：清洗后订单
输出：需求分区映射 + 候选 vertiport 位置
具体做法：

确定苏州研究区：bounding box 大约 lon [120.45, 120.95]、lat [31.20, 31.50]，但你应该先用 EDA 看订单实际密集区域，可能砍掉极少数远郊区域
构建 demand zones：推荐两种方案二选一

方案 A：H3 hexagons（推荐，工程上最干净）—— 用 h3-py，level 7 在苏州约产出 200–300 个六边形，每个边长 ~1.2 km
方案 B：苏州街道边界（论文叙事更好讲）—— 从 OpenStreetMap 或天地图下载 admin level 8 边界，~150 个街道
给每个订单的 O/D 打上 zone id


构建 candidate vertiport sites：

从 OSM 拉取苏州的地铁站、大型商场、产业园、医院、机场等 POI
加上一些规则生成的网格中心点作为补充
过滤掉水域、保护区、机场净空区（这个等论文写到再加，先不做）
最终得到 ∣C∣≈200–500|C| \approx 200\text{–}500
∣C∣≈200–500 个候选点


构建距离矩阵 DzcD_{zc}
Dzc​：每个 zone centroid 到每个 candidate site 的欧式距离（你可以后期换成路网距离，但欧式先够用）

关键产出：

zones.geojson：所有 demand zones 的几何
candidates.csv：候选 vertiport 的 (lat, lon, poi_type)
dist_zone_candidate.npy：∣Z∣×∣C∣|Z| \times |C|
∣Z∣×∣C∣ 距离矩阵


阶段 3：OD 矩阵构建与 eVTOL 候选筛选
输入：订单 + zone 映射
输出：时序 OD 张量 + eVTOL 候选 OD 子集

时间分桶：把 7 天数据按 30 min 切片，共 T=336T = 336
T=336 个时间片（如果数据稀疏可放粗到 1 小时，168 片）
聚合 OD 张量：每个 (t, o_zone, d_zone) 累加订单数，得到 M∈ZT×∣Z∣×∣Z∣\mathbf{M} \in \mathbb{Z}^{T \times |Z| \times |Z|}
M∈ZT×∣Z∣×∣Z∣
eVTOL 候选订单筛选（这里是你的论文里要解释清楚的关键决策）：
   一条订单 (o,d)(o, d)
(o,d) 进入 eVTOL 候选池的条件：

geo_dist(o,d)≥Lmin⁡\text{geo\_dist}(o, d) \geq L_{\min}
geo_dist(o,d)≥Lmin​，例如 15 km
trip_duration≥Δtmin⁡\text{trip\_duration} \geq \Delta t_{\min}
trip_duration≥Δtmin​，例如 25 min（说明地面拥堵或路途确实长）
可选：fare≥Pmin⁡\text{fare} \geq P_{\min}
fare≥Pmin​，例如 50 元，体现支付意愿

筛选后得到 eVTOL eligible OD 张量 MeV\mathbf{M}^{\text{eV}}
MeV，这才是 RL 真正要覆盖的需求
EDA 与可视化（必做，进论文 Case Study）：

全量需求与 eVTOL 候选需求的时空对比图
eVTOL 候选订单数占比（预期 5–15%）
最热门的 top-20 OD 对



关键产出：

od_full.npy：shape [T, |Z|, |Z|]
od_evtol.npy：shape [T, |Z|, |Z|]，eVTOL 子集
od_meta.json：包含 zone id ↔ centroid 映射、时间片定义


阶段 4：Diffusion 模型训练（生成合成 OD）
输入：od_evtol.npy（也可以训全量 OD 再 filter）
输出：训练好的 diffusion 模型 + 合成 OD 样本
技术路线选择（按推荐度排序）：
路线 A：把 OD 矩阵当 image，标准 DDPM

把每个时间片的 MteV∈R∣Z∣×∣Z∣\mathbf{M}^{\text{eV}}_t \in \mathbb{R}^{|Z| \times |Z|}
MteV​∈R∣Z∣×∣Z∣ 当作单通道图像
用 U-Net + DDPM 训练，参考 lucidrains/denoising-diffusion-pytorch
条件信号：hour-of-day (sinusoidal embedding) + day-of-week + 是否工作日
优点：实现成熟，调参少；缺点：忽略了 OD 矩阵的图结构，但工程上最稳

路线 B：把 OD 当 graph，graph diffusion

节点 = zones，边权 = OD 流量
用 graph diffusion model（如 DiGress 风格）
优点：方法学新颖度高；缺点：实现复杂，调参痛苦，不建议你一个人 + 时间紧时做

强烈推荐路线 A。需要做的工程细节：

数据归一化：OD 流量长尾，先做 log⁡(1+⋅)\log(1 + \cdot)
log(1+⋅) 再做 min-max 归一化到 [−1,1][-1, 1]
[−1,1]（DDPM 默认值域）
稀疏性处理：单个 30-min slot 的 OD 矩阵会非常稀疏；可以考虑把多个 slot stack 成多通道（如 4 个连续 slot = 4 channel input），让 U-Net 学时间相关性
条件机制：U-Net 的 time embedding 拼接上 hour/weekday embedding，作为 cross-attention 的 condition
训练目标：标准 ϵ\epsilon
ϵ-prediction loss，1000 step diffusion 训练，DDIM 50 step 推理
评估：

统计指标：生成的 OD 总量、稀疏度、行/列和的分布与真实数据对比
分布指标：JSD、MMD between 真实 OD 集合和生成 OD 集合
时空模式：早高峰/晚高峰的热门 zones 是否一致



关键产出：

models/diffusion_od.pt：训练好的模型
data/synthetic/od_samples.npy：shape [N_ω, T, |Z|, |Z|]，NωN_\omega
Nω​ 推荐 50–100 个场景
results/diffusion_eval/：评估指标和可视化


阶段 5：RL 环境设计（你的核心工作）
MDP 形式化：
元素定义State sks_k
sk​三部分拼接：(1) 已放置 vertiport mask $\mathbf{m} \in {0,1}^{Action aka_k
ak​从候选集 CC
C 中选一个尚未放置的点，离散动作空间 ∥C∥\|C\|
∥C∥Reward rkr_k
rk​rk=ΔCovk−λ1cland(ak)−λ2redundancy(ak)r_k = \Delta\text{Cov}_k - \lambda_1 c_{\text{land}}(a_k) - \lambda_2 \text{redundancy}(a_k)
rk​=ΔCovk​−λ1​cland​(ak​)−λ2​redundancy(ak​)Episode长度 KK
K（要放置的 vertiport 数量），K=10K=10
K=10 起步可调
Coverage 的精确定义（论文公式部分要写清楚）：
定义 candidate cc
c 的"覆盖区"为 Γ(c)={z∈Z:dist(z,c)≤Rwalk}\Gamma(c) = \{z \in Z : \text{dist}(z, c) \leq R_{\text{walk}}\}
Γ(c)={z∈Z:dist(z,c)≤Rwalk​}。
已放置 vertiport 集合 V⊆CV \subseteq C
V⊆C 覆盖的 zones 为 Γ(V)=⋃c∈VΓ(c)\Gamma(V) = \bigcup_{c \in V} \Gamma(c)
Γ(V)=⋃c∈V​Γ(c)。
eVTOL 双端覆盖率：
Cov(V)=1∑t,o,dMt,o,deV∑t∑o∈Γ(V)∑d∈Γ(V),d≠oMt,o,deV\text{Cov}(V) = \frac{1}{\sum_{t, o, d} M^{\text{eV}}_{t, o, d}} \sum_{t} \sum_{o \in \Gamma(V)} \sum_{d \in \Gamma(V), d \neq o} M^{\text{eV}}_{t, o, d}Cov(V)=∑t,o,d​Mt,o,deV​1​t∑​o∈Γ(V)∑​d∈Γ(V),d=o∑​Mt,o,deV​
这就是要最大化的核心指标。
Reward 的两种实现：

Marginal coverage gain：ΔCovk\Delta\text{Cov}_k
ΔCovk​ 是放置第 kk
k 个 vertiport 时新增的覆盖订单量。优势：reward dense，训练稳。推荐
Terminal sparse reward：rk=0r_k = 0
rk​=0 除非 k=Kk=K
k=K，最后一步给 Cov(V)\text{Cov}(V)
Cov(V)。优势：理论干净。不推荐，太稀疏

Action mask：每步把已放置的候选点 mask 掉，避免重复选择。用 sb3-contrib 的 MaskablePPO 直接支持。
鲁棒训练（论文核心创新点 C2）：

每个 episode 开始时，从 diffusion 生成的 NωN_\omega
Nω​ 个场景里随机抽一个作为当前 MeV\mathbf{M}^{\text{eV}}
MeV
或者一次抽多个，用 CVaR 作为优化目标：CVaRα[r]\text{CVaR}_\alpha[r]
CVaRα​[r] 而不是 E[r]\mathbb{E}[r]
E[r]，让 policy 对最差 α\alpha
α 分位的场景也有保障
这是论文 method 章节 4.4 节的内容

网络架构：

输入：把 candidate 看作序列长度为 ∣C∣|C|
∣C∣ 的 token，每个 token 的特征 = (mask, mean demand near it, std demand near it, static feats)
Encoder：3 层 self-attention（Transformer encoder）
Policy head：每个 token 输出 1 个 logit，配合 action mask 做 categorical 采样
Value head：tokens 经 mean pool 后接 MLP 输出 V(s)V(s)
V(s)


阶段 6：RL 训练
算法：MaskablePPO（动作 mask + clip-PPO + GAE）
训练配置（先给个起点，后续根据 reward 曲线调）：

learning rate 3×10−43 \times 10^{-4}
3×10−4，linear decay
nsteps=2048n_{\text{steps}} = 2048
nsteps​=2048, batch size 256
γ=0.99\gamma = 0.99
γ=0.99（虽然 episode 短 K=10K=10
K=10，但有意义）
GAE λ=0.95\lambda = 0.95
λ=0.95
entropy coef 0.01，避免过早收敛
total steps 2M，约 4–6 小时单卡 3090 训完
seed 跑 3–5 个不同种子，论文里报告均值±标准差

工程要素（你的偏好里都有）：

Hydra 管理 configs，每个 run 单独输出目录
WandB 记录 reward / coverage / loss / entropy / value loss
每 100k steps 保存 checkpoint
EvalCallback 在固定的 real demand 上每 20k steps 评估一次，保存 best model
early stopping：100k steps eval reward 不涨就停

消融实验（写论文必跑）：

A0: random placement
A1: K-means clustering on demand（FoEM 2026 那篇 baseline）
A2: Genetic algorithm
A3: CPLEX 精确解（小规模 K=5 可解，大规模不可解，正好衬托 RL）
A4: SpoNet（克隆代码，迁移到你的数据）
A5: 你的方法（real demand only，不用 diffusion）—— 测 RL 单独的贡献
A6: 你的方法（real + diffusion-augmented，期望均值优化）
A7: 你的方法（real + diffusion + CVaR robust）—— 完整版

A5–A7 的对比就是你的论文核心 ablation table。

阶段 7：评估与论文 case study
评估场景：

In-distribution：用 holdout 出来的真实 demand 评估
Out-of-distribution：人为制造 demand shift（比如把某个 zone 的需求 ×2 模拟新建商圈），看哪种方法仍稳健
Time generalization：训练时只用前 5 天，测试用最后 2 天

核心评估指标：

Coverage rate：主指标，eVTOL OD 流量覆盖比例
Demand-weighted coverage：按订单费率加权（高价值订单优先覆盖）
Geographic equity：覆盖在各 zone 间的 Gini 系数（避免全堆在市中心）
Robustness：worst-case scenario 下的 coverage（来自 diffusion 长尾样本）
Solving time：你的 RL 推理 < 1 秒，CPLEX 可能小时级，这是你的卖点

可视化（论文里至少 4 张图）：

苏州 eVTOL 候选 OD 的时空热力图
Diffusion 生成 vs 真实 OD 的对比（show fidelity）
各方法选出的 vertiport 在苏州地图上的位置叠加
Reward 训练曲线 + ablation bar chart